# 会话交接：2026-09-20 · 睡眠缺口 → 跨项目盘点 → A 系列判别 + 三个落地件

> **给下一轮的一句话**：本会话把「大堆/塔不入睡」这条线从"结构重做（多会话）"推进到
> **机制清楚 + 两个可用的保真/性能档 + 一堆被钉准的归因**；最重要的方法论结论是
> ——**本仓档案里的"已否证"经常真正的含义是"当时的实现或判据不对"**（本会话三次翻案）。
> 工作区干净、门禁全绿、三哈希 `0x6219d186…`/`0x63e5eb35…`/`0xd8601988…` 逐位不变。

## 0. 起点（上一轮交接档给出的入口）

上一轮交接档列的"下一步入口"是 **「先只做趟间位置积分」**。**该件已于 2026-09-14 否证**
（`DESIGN-staged-solver.md` §5：125 dE 18.2→176.2、塔 |v| 18.2、|Δpos| 3.1 m）。
⇒ 在 `OPEN-PROBLEMS.md` P1 与 `SESSION-2026-09-15-PERF.md` 就地更正（**教训：入口档会
陈旧，动手前先核否证日志**）。

## 1. 本会话 13 个 commit 的产出（按类型）

### 落地（3 件，全部默认关或位不变）

| 件 | 数值 | commit |
|---|---|---|
| **无偏置末趟**（Rapier TGS-Soft 末趟；`stabilization_iterations`，默认 0） | pile5 **1735→1960/2000 入睡**、KE 10→1.6；tower25 KE −28%、穿透 0.017→**0.009**；col45 中性；**125 体退化**（KE 1.6→18.1）⇒ 场景依赖，默认关 | `a4103d0` |
| **A3 冷热分离**（`group_iw`/`group_im` 按组紧凑；解决热路径的全局散点读） | **位不变**（三哈希）+ pyramid **9/9 轮 −4.3%**；8B 无收益；省 7.2 MB；热路径与 `BodySet` 解耦 | `6bf917d` |
| **速度充气视野**（`narrow.set_predict_dt`；判据 `sep ≤ skin + max(0,v_接近)·dt_detect`） | 把「检测每步一次」的**崩修掉**：塔 KE 299 134 → **1 078**（÷280）、y 带完整；pyramid 检测类 295→**155 µs**、p50 **0.982 ms**；125 体 KE 1.6→**0.5** | `20919d7` |

### 诊断设施（landed，零行为影响）

| 件 | 用途 | commit |
|---|---|---|
| `warm_match_stats_take()` | warm 匹配分支计数（精确/回退/未匹配）——流形特征稳定性 | `1a22e58` |
| `warm_fallback_kind_take()` + `narrow::feature_kind` | 回退命中按位域拆成因（侧别/裁剪路/哈希） | `5232146` |

### 否证（6 项，均留读数且代码回退）

| 项 | 判决 | commit |
|---|---|---|
| **A1 摩擦不看求解器自造速度**（Unity + Rapier 双源点名） | 两种读法都否定：Rapier 读法（摩擦移出带偏置趟）**塔爆 KE ×255**；Unity 读法（用冲量前速度）塔上 wash、125 体 **KE ×14** | `10db544` |
| **A2 岛内染色 + 大岛切批**（Jolt/Box2D 招牌） | 三重否决：可并行面仅 73%（理想 2.76×）× 同扫掠数下收敛退化（动能 ×2.3）× **3D 装箱色数 20–25**（Box2D 的 8 色是 2D 的） | `c73cf84` |
| **A5 接触点裁量**（DESIGN 阶段 2） | **点数是承重件不是冗余**：4→3 工作量 −19% 但动能 +80%、125 体 KE ×24；→2 推向垮塌 | `1767fd1` |
| 无偏置末趟改刚性 cfm | 堆/塔持平或略差，125 体 KE 退化不变 | `f88f6c0` |
| 复用模式放宽回退半径 | 塔 KE +35%（1 078→1 451），金字塔时序也退 | `05fb4cb` |
| （附带）`dump` 参数位回归 | a4103d0 把金样 `dump` 挤到第 10 位 ⇒ render_compare 配方静默失效；已修回第 9 位（`-` ＝ 不转储） | `10db544` |

### 归因修正（三次翻案，价值最高）

1. **§9/§10 的"流形是裁剪产物 ⇒ 配对漂移"过强**：窄相盒-盒裁剪**本就是特征稳定的**
   （`feat_intersect(入射棱对, 参考侧平面)`，注释明写"跨帧稳定"）；实测平面堆稳态
   **85.1% 精确命中**、塔 **69.6%**（回退 18.7%）⇒ 结论是"**场景相关**"，且塔的回退
   78% 是"接触仍在、面选择变了"（`1a22e58`/`5232146`）。
2. **§9 的 P1 实现从未入档**（`git show 00a1ebf --stat` 只有两份文档）⇒ 结论无法复核，
   遂重测：**崩可复现但 warm 精确率反而 92–96.6%** ⇒ "配对/暖启动"被排除为崩因。
3. **崩因坐实为"检测视野不足"**：skin 0.01→0.02/0.04/0.08 使塔 KE 299 134 →
   **2 386/1 992/1 271**、y 带全完整 ⇒ 「检测冻结不可行」应改写为
   **「检测视野与检测节奏不匹配」**；随后用**速度充气视野**落地修复（见 §1）。

## 2. 当前状态（可直接对照回归）

- **门禁**：fmt/test/clippy/vocab/typos 全 0；三哈希 `0x6219d1866b20c002d806d3d699a487ff`
  （门槛）/ `0x63e5eb35b71b8c84ade4a053aeecb900`（压力）/ `0xd8601988ad7989ffb58ba8b956c2f8db`
  （确定性）。
- **两个默认关的档**（都可以开，各有代价，均已量化）：
  - 保真档：`stabilization_iterations ≥ 2` 配 `substeps=8`（最佳：sub8+stab2 ⇒ 塔 |v|max
    0.119、可睡体 1411/2500）；
  - 性能档：`detect_once_per_tick`（`crates/vxl-phys/src/lib.rs` 顶部 const fn；开启即换代
    `0xb8d4bb4e…`/`0x94983ea1…`）＋自动生效的速度充气视野 ⇒ 检测类 −14% 帧、塔 KE +19~22%。
- **A2 的账面**（供复用）：pyramid 宽相 3% / 窄相 21% / 求解 76%；求解内 构建 35% /
  热启动 3% / **扫掠 61%**。

## 3. 下一轮入口（排序；每条含判据）

1. **复用余价的最后一环**（最小、纯诊断/单变量）：复用在塔上仍留 KE ×1.2–1.7（stab=2
   时 ×1.7）。已否证两条候选（未匹配点冻结几何、放宽回退）。剩余候选：① spec 额度在
   复用下的发放语义（本仓 `depth` 每子步从锚点重算，**理论上自洽但未实测**——可加"每子步
   实际 spec 值"的计数器）；② 冻结点集的累积效应；③ **复用与无偏置末趟的交互**
   （实测非加性：stab 单独省 300、复用下只省 55 ⇒ 两者互相干扰，机理未明）。
   **若修掉 ⇒ detect-once 有望转默认档（−14% 帧）**。
2. **A6 窄相改"特征稳定流形"**（大件，但已被本会话收窄）：前置不是"重写窄相"，而更可能
   是**参考面选择的跨帧稳定性**（`ref_base` 翻转 ⇒ 特征 ID 变）。入口：先做"每子步实际
   spec 值/参考面索引"的计数（与 1 共用设施）。
3. **A7 平台侧**（静态地形分层网格 + 动态 TLAS/BLAS）——M2 排期，与本仓 M1 判据无关。
4. **A4（Unity 两条启发式）建议直接标否证**（量级上不够：速度截零要 ≥0.2 才碰到塔的 |v|，
   角惯量要放大 ~10× 才能把 |ω| 压到阈值下——都属"毁掉物理才够得着"）。

## 4. 方法论教训（本会话新增，务必沿用）
1. **否证实验的实现不入档 ⇒ 结论无法复核**。本会话两次修正 §9/§10 归因都撞在这上面
   （`00a1ebf` 只提交了文档）。**今后否证至少留补丁**（分支 / stash / EXPERIMENTS 附录一行 diff）。
2. **"已否证"要读成"当时的实现/判据不对"**：本会话三次翻案（归因、P1 重测、检测视野）。
   动手前先问"当时的实现还在吗？能复核吗？之后落地过什么相关能力？"。
3. **先算账再动手**：A2 用账面（可并行面 73%）+ 一个**零并行判别**（只重排）就否掉了，
   没沉进实现；A5 用"截断上限做成旋钮"三步之内否掉；速度充气视野用 **skin 扫描（零代码）**
   先证机制再实现。
4. **搬外部做法要核两件事**：它的**前提**（A1：Rapier 的带偏置趟只有 1 遍，本仓 64 遍）
   与它的**维度/规模**（A2：Box2D 的 8 色是 2D 的，3D 装箱 20–25 色）。

## 5. 遗留约束：安全审计状态（本会话已实测，**不必再重跑**）

Mimosa 在每次 commit 前后都会警告 `scanner_enobufs`（"没有得到完整扫描结论……请尽快重新
运行完整审计"）。本会话**把两档都跑了**（`security_scan_start`，project = 本仓）：

| 档 | findingCount | 依赖包 | 依赖告警 | evidenceBoundary | verdictEffect |
|---|---|---|---|---|---|
| normal | **0** | 88 | 0 | `static_only_no_runtime_execution` | **none** |
| deep | **0** | 88 | 0 | `static_only_no_runtime_execution` | **none** |

⇒ **结论：本环境下扫描器产不出带结论效力的审计**（两档都带 `verdictEffect: none`），
所以那条警告是**结构性的、重跑不会消除**。**归档**：`C:\Users\lbx13\.mimosa\security-scans\
vxl-phys\`（deep 的 seal `sha256:8b31e8aedc45ffe121bbb171844c54f23838b81b8aedb51bc891fb6b8cff0816`）。
**纪律**：① 不要把"重跑审计"再列为待办；② **不要宣称项目安全**——0 命中 ≠ 安全，
缺的是 verdict 而不是扫描次数；③ 要真结论需要能产出 verdict 的扫描环境（或人工审）。
