# 会话档：GPU 千万粒档（2026-09-26）—— 两个障 → 验收完成 → 4.14× 提速 → 往返的账

> 一句话：**SPEC §4.8 的"1000 万粒"档从"跑不起来"做到"验收通过且判据全绿"**，并在过程中
> **三次修正自己的判断**（都记在正文）。全部读数与推导在 `PLAN-gpu.md` §18–§20（本文只做索引 + 交接）。

## 1. 这一段做了什么（提交顺序，分支 `feat/solver-limits`）

| 提交 | 内容 | 关键读数 |
|---|---|---|
| `411cb11` | 清两个障：① 存储缓冲绑定上限按 `adapter.limits()` 申请（WebGPU 默认 128 MiB 装不下 10M 档的 238 MB 逐粒反作用缓冲）；② 单维分派上限 65535 ⇒ `probe::split_2d` + 核里 `gid.x + gid.y*(65535*64)` 展平 | 10M 从"跑不起来"到"跑得起来"；**既有档逐位不变**（125k 表哈希 = 冻结值）|
| `a8251e7` | 探针 `--cpu-threads`（对照档走并行，与串行**逐位相同**）；`gpu_grid_probe` 拆 `parse_args`（解 120 行阈） | 10M 网格**逐位同表**（⌈9.94M/64⌉=155 288 组 ⇒ 二维分派实跑）|
| `2e87c89` | CPU 引擎大档实测（`sph_scale`） | 10M：串行 91.4 s/tick、8 线程 13.5 s/tick（并行 6.8×）；**逐粒退化 3–4×，只有密度/力两个邻域相位**（访存签名）|
| `75fd664` | 10M 整 tick 验收 + 探针分段计时（`⏱`）+ 计时下限按规模让路 | **934 ms/tick**、显存增量 ≈1.15 GB、`overflow` 0、漂移 t=1 干净 |
| `8b9e8a9` | **根因：格边预算悬崖** —— `grid_box`「格边从 h 起、总格数超 `GRID_MAX_BINS`(1<<20) 就加倍粗化」；10M 域越界 ⇒ 格边 0.2 m ⇒ ≈63 粒/格 ⇒ 候选 ×8。抬到 `1<<21` | **934 → 226 ms/tick（4.14×）**；默认档三处机器无关判据**一字未动** ⇒ 非换代；全量门禁 + CI 双绿 |
| `4985312` | 新箱子下的 10M 网格复核 | `start` 1 259 713 项、**0 项不同**、哈希 CPU≡GPU、格数 8× 耗时只 +9% |
| `9a3bc30` | §17.10 拍板前补证据：流体侧同一根往返杠杆 + 规模改写 (A)/(B) 的账 | — |
| `7fb9276` / `f4e6c98` / `4f92830` | 「减往返」的三轮账（**两次自我更正**，见 §3） | 回读 78 ms ≈ 按映射区间计价 + 随缓冲走的固定项 |
| `450360f` | §19 待裁决项的落地清单（两种回答 × 接口/判据/工作量） | — |
| `c96f9df` | 回读口径判别（映射区间可指定） | 10M：全量 84.44 vs 只映射 1 MB 24.11 ⇒ **按映射区间拷贝** |
| `fa1e0c9` / `e38dd1c` | 管线档：`run_deferred` + `readback_pending_wait`（只发起不等待） | 含回读 tick **286 → 227.70 ms**（口径更正见下）|
| `9f38987` | §20 邻域访存的**先算账**（用已有两档消融解 `t=a+c·k`） | 两个邻域相位 = **52% 每粒固定 + 48% 候选收集** |
| `e1c061b` / `a799913` | 真消费方路径（`take_deferred_state`）+ **口径更正** | 管线档真实收益 **−9.9%**（不是 −20.4%）|
| `2c57ab5` | 回读取数**并行解析** | 管线档 257.71 → **245.09 ms/tick**（端到端 **−14.7%**），但只值 1.35× |

**10M 档当前读数（10M 粒、4 子步、RTX 4060 Ti）**：GPU 常驻 **208–226 ms/tick**、
含回读（同步）287 ms、含回读（管线档+并行解析）**245 ms**；显存增量 ≈1.15 GB（预算 ≤4 GB）；
`overflow` 0、NaN 0、漂移 t=1 `Δpos 0 / Δv ≤7e-5 m/s / 动能比 1.0000`。

## 2. 下一步（不需要拍板，按收益排）

1. **§19.5.1 ② 不拷出**（改消费方直接读映射切片）：剩余暴露 ~37 ms 是**未缓存内存读的硬底**
   （并行只值 1.35× ⇒ WC 读不随线程扩）⇒ 只有"不搬"能再降。
2. **§20 核内候选收集**（共享内存按格分块）：上限 **−45 ms（整 tick ≈ −20%）**，只改核内、不碰语义；
   判据现成（网格表逐位同表 + 整 tick 漂移表 + 10M 读数），**细格/粗格两档都要复验**（收益结构不同）。
3. **§20 每粒固定工作**（SIMD/减指令）：上限 **−35 ms（≈ −15%）**。
4. 把**管线档接进产品路径**（渲染走 `run_deferred`、耦合保持同步）——让那 −14.7% 落到实际路径。

## 3. 本会话的三次自我更正（**这一段最值得读**）

1. **§18.3.1**：看到"两次 `poll_wait`" + 一次读数 ⇒ 把 86% 的回读开销归给**同步**。
   §18.3.2 用四档标度分析否掉（每 MB 成本恒定 ⇒ 无固定项）⇒ 教训：**标度推断只能排除，不能定性**。
2. **§19.5.1**：报"管线档 −20.4%"时只测了**等待**，没测真消费方那一步（映射 + 拷出 119 MB）
   ⇒ 真实 **−9.9%**；补 `take_deferred_state` 后重测并更正。
3. **§19.5.1 ① 的预测**：说"并行解析 6–8× ⇒ ~7 ms"，实测**只 1.35×**（WC 内存的多线程读不扩）。

⇒ 三条同族：**归因/预测必须先做定向实验或标度分析；"看起来像"和"加线程就该快"都会翻车。**

## 4. 仍等你拍板的三条（全部带好了账）

1. **§17.10 的 (A) 停线 / (B) 转"少往返"架构**——补记已按大档重算（GPU 侧不随规模退化、CPU 侧 3–4× 退化）。
2. **§16.3 投影换代**（`srf=0` 阈值上的静置层：改法候选已列，属换代级）。
3. **`PLAN-boolean.md` §3 的六条规格**（体素序先行的建议已给）。

## 5. 复现用的命令（都在仓内，判据自带）

```bash
# 10M 档整 tick（含分段计时 / 回读口径判别 / 管线档；--cpu-threads 8 让对照档跑得完）
cargo run --release -p vxl-phys-gpu --example gpu_tick_probe -- 215 1 --cpu-threads 8 --pipe
# 10M 档网格对拍（逐位同表判据）
cargo run --release -p vxl-phys-gpu --example gpu_grid_probe -- 215 --cpu-threads 8
# CPU 引擎在大档的成本（PLAN-0.3 §6 那把尺）
cargo run --release -p vxl-phys-fluid --example sph_scale -- 215 4 2 8
# 全量门禁（含金样门 / 确定性三哈希 / 规模档门）
bash scripts/gate_all.sh
```

⚠️ 计时类读数一律 **perf-lock 独占**（`python <workspace>/perf_lock.py acquire --label ...`）。
