# 否证日志（EXPERIMENTS.md）

> 记录「测过且无收益/负收益/被证伪」的路线。每条 = **假设 → 方法 → 结果 → 裁决**。
> 作用：下一轮勿重做。细节叙事链回 `M1-PLAN.md` 对应段；有决策后果的升格进 `adr/`。
> 纪律：探针改动均为两行级小步，测完即回退；落地（accept）条目单独标注。

## 宽相（T2）

| # | 假设 | 方法/结果 | 裁决 | 依据 |
|---|---|---|---|---|
| W1 | 查询峰 = 睡眠翻转帧全缓存失效的代价 | 「唤醒只清自己、入睡才全体失效」，行为逐位等价；查询峰 22.64→22.96ms | **回退**（无收益） | M1-PLAN T2 尾证伪① |
| W2 | 查询峰 = 候选数过多，fat 边距 0.5 是源头 | 上限 0.15：树 +14%、查询 +8%（盒更小→逃逸更早→重查更多）；候选反 +9% | **回退**（反噬）。**⚠️ 解释已被 W9 修正**：当时读成「边距不是杠杆」，实际是「**收窄**边距有害」——放大才是方向 | T2 尾证伪② |
| W3 | 8 路宽叶树（≤8 体/叶）可同时达标 | 树峰 −58% 达标，但候选 5.9× ⇒ 查询均 +90%，合计净亏 6.4ms/帧（4ns/候选×+172 万） | **回退**；代码保留（ADR 0001） | T2 尾证伪③ |
| W4 | 宽内部+窄叶（BVH8）按 W3 结论应赢 | 孤立对拍全胜（−33..45%），生产 broad 10.5→11.9ms：生产候选过滤 32ns/条（冷数组）吃掉遍历收益 | **回退**；代码保留（ADR 0001）。**⚠️ 机制归因已被 W9 拆段计时修正**（过滤实测 3.8ns/条，那 32ns 的差来自刷新侧） | T2 尾证伪④ |
| W5 | `fix_upwards` 提前退出可把树峰 12.2→~5ms | 首版结构测试 4/6 红（原因未知）⇒ 当时回退 | **✅ 后续已落地**（7b36caa）：最小复现定位根因 = 提前退出误用在 insert/remove；拆分 `fix_upwards_grow`（仅生长路径）后树峰 12.17→7.34ms、哈希逐位不变 | T2 尾 + M1-PLAN §9 |
| W6 | 树峰 12.2ms 来自链化超限全量重建 | 用已有数据判定：tree_h 恒 18 ≪ 阈值 68.9 ⇒ 重建分支从未触发；真构成 = 10 万体×18 层 refit | **归因修正**（非实验） | T2 尾 |
| W7 | 降候选要靠收紧边距 | 见 W2：边距不动候选总量（35.4→38.6 万反升） | **证伪**；正确方向=叶盒紧致度 | T2 尾 |
| W8 | 候选过滤环加**硬件预取**（SSE2 `_mm_prefetch`，预取下一候选的精确盒）可压查询成本 | 逐位安全（无语义影响）但同窗口无收益：查询 均 10.07→**10.33** / 峰 31.96→**34.78**（噪声内偏负） | **回退**（SPEC §7 的 ≥5% 收益律；机制：过滤只占 1.22ms、不是成本所在——见 W9） | M1-PLAN §9 后续 |
| W9 | **查询成本在「逃逸重查」而非候选过滤** ⇒ 放大速度项边距可压成本 | 拆段计时：refresh 均 **8.08ms** vs filter 仅 **1.22ms**（3.8ns/条，与隔离档一致）；速度项 K=1.5→6：broad 均 17.03→**14.7–15.1**、树 均 2.65→**1.79**、m0_gates p50 0.54→**0.37**；峰 43.0–45.2（基线 43.5，噪声内）；K=12 峰反涨 48.97 | **落地 ✅**（K=6；逐位中性：m0_gates 哈希不变、twin_match ✓、determinism 不变） | M1-PLAN §10 |

**已定量的机制结论**（W9 拆段计时修正后）：查询相位 = **逃逸重查（均 8.08ms，主导）**
+ 候选精确过滤（1.22ms，**3.8ns/条**——与隔离档一致；旧文档的「生产 32ns/条」是
把刷新侧的差误记到过滤上）。⇒ 杠杆是**逃逸频率**（× 每次树查询 ~0.27µs），
不是过滤局部性。树峰 = 每清醒体祖先链 refit（提前退出已落地，见 W5）。

## 解算（T1 稳定性 + T4 并行）

### 稳定性（泵定位，125 体复现 = `diag_min 5 5 0.52 16 0.5 300 box 8 0.52 0.2 0 4`）

| # | 假设 | 方法/结果 | 裁决 | 依据 |
|---|---|---|---|---|
| S1 | 泵 = 迭代不足 | iters 32/64、shock 4：只降能量（148.9→22.5）不消环 | **排除** | T1 消融 |
| S2 | 泵 = erp 偏置 | baumgarte=0：dE 逐位不变 | **排除** | T1 消融 |
| S3 | 泵 = skin 带 | skin 0.01–0.05 扫描无差异 | **排除** | T1 消融 |
| S4 | 泵 = 切向回拉强度/死区 | 关→塔弹射（\|v\|97）；1mm 死区→堆好塔坏 | **排除**（承重件） | 第十一段 |
| S5 | 泵 = 回拉无上钳 | 钳 0.05/0.2：125 体族全睡 ✓ 但 m0_gates 门槛 p50 恶化 2.5–5.3×、末态不再全睡 | **回退** | T1 第二修复 |
| S6 | 泵 = 未承载接触仍在回拉 | 未承载不回拉：125 体族 0/125 全睡 ✓ 但 m1_pile 微退、col45 25→20 | **回退**（留档：若该族成为验收主体可取用） | T1 第一修复 |
| S7 | 泵 = 锥投影释放/重加 | 关锥投影：dE 148.9→4472.9（+30×） | **反向证伪**——锥投影在压能量 | 第十九段后探针 |
| S8 | 泵 = 回拉符号 bug | 代码核对：`v_t = drift/dt` 方向正确 | **排除** | T1 清单⑧ |
| S9 | 泵源 = 近邻回退匹配 | 关回退匹配：dE 148.9→**16.2（−89%）** | **定位成功**（泵=错配 warm 冲量本身） | 第五段后 |
| S10 | 关掉回退暖启动即可修 | col45 45/45 全睡 ✓、pile5 1985 ✓，但**塔崩**（\|v\|15.6） | **回退**——塔依赖回退 | 第五段后 |
| S11 | 泵 = 回退点的漂移目标 | 回退点不回拉（锚点照旧）：dE 148.9→144.2 不降 ⇒ 泵是 warm 冲量不是 trhs | **归因修正** | 第二次探针 |
| S12 | 回退半径收紧可分开有害匹配 | 4×skin→**1×skin**：塔更好、col45 +10 ✅ 但 125 体 dE 148.9 不变 ⇒ 有害匹配在 1cm 内 | **落地 ✅**（唯一净正向）+ 距离类门全部失效 | ADR 0003 |
| S13 | 有害成分只在切向 | 只零化回退匹配切向暖冲量：col45 45/45 ✓、pile5 1825 ✓、塔完好 ✓，但 **m0_gates 末态 0→75** | **回退**——须自适应判别式 | 切向拆分探针 |
| S14 | 深度一致性门（|有效深度−裁剪深度| ≤ 阈值）可判别 | **零效果、dE 逐位不变**；根因：`depth0 + sep·n ≡ cp.depth` 恒成立 ⇒ 恒真式 | **实现级错误**（恒等式陷阱，见 KNOWLEDGE §1.8）；改正后见 S18 | 本轮 |
| S15 | 切向锥饱和门（拒 \|pt\| > 0.9·μ·pn）可判别 | 过滤生效（贴锥携带被拒）但 125 双指标无变化 | **回退**（极限环不由贴锥携带维持） | 本轮 |
| S16 | 分离状态门单独可用（depth ≤ 0 拒配） | 无效果（有害匹配普遍在正深度） | **保留为组合的一半**（并入 S18） | 本轮 |
| S17 | ⑦未承载不回拉（S6 复测） | 历史效果不复现：\|v\| 仅 0.32→0.19、仍 125/125 不睡、dE 峰值反升 20.9 | **回退**（引擎已变，勿再引历史结论） | 本轮 |
| S18 | **深度跳变门**（\|cp.depth − depth0\| ≤ 0.25mm，回退分支专属） | 125 dE 148.9→**18.2** ✅；pile5 入睡 1195→**1700**；tower \|v\| 0.283→**0.236**、Δpos 0.0595→0.0590；m0_gates 全睡保持；T4 4.80× 逐位一致；col45 位置 0.0065→0.0105（容差内） | **落地 ✅**（ADR 0007） | 本轮 |
| S19 | 125 体末态入睡可由匹配侧达到 | **全拒档**（FB_DEPTH_JUMP=-1）跑 600 tick：仍 **125/125 不睡**、\|v\| 0.05–0.28 持续振荡（dE 峰值 16.8） | **匹配线关闭**——睡眠侧须整组 staged 语义（与第十二/十三段互证） | M1-PLAN §9 |
| S20 | 解算的「固定成本」= 建约束等每岛开销 | 插桩拆解（8B 同窗口）：迭代环 **95%**（245.9ms/组）、建约束 5%（12.2ms/组）、收集 0.3%、暖启动 ≈0 | **归因修正**——iters 扫描外推的「固定 87ms」是误读（=不随迭代变化的部分：建岛+scatter+warm 表重建） | M1-PLAN §12 |
| S21 | warm 表重建的 33.5ms 是哈希计算成本 ⇒ 换快速哈希（FxHash 同族，自实现零依赖）可省 | 换后 **33.51→33.64ms（零收益）**；该段 = 220k 次随机桶访问 ≈75ns/条 ⇒ **内存延迟主导** | **回退**（换数据结构才有意义：排序合并/稳定槽位；查找路径同吃） | M1-PLAN §12 |
| S22 | staged 逐迭代位置积分（Rapier 趟间积分同族）可让 iters 16→4 且质量不降 | **阶段 0 逐位通过**（pos/rot scratch 搬运，哈希不变）；**阶段 1 全档变差**：125 dE 18.2→34.4（iters=16）/77.5（8）/176.2（4）；塔 KE 819→66 万、\|Δpos\| 0.059→3.10（16）且 8/4 崩。隔离：iters=1 更差（1377，非瞬态累积）；只推位置不刷新 rhs 仍崩塔（13.1m）⇒ **位置推进时机本身**为病因 | **回退**（机制：本引擎 erp/spec 是每 tick 目标速度，逐迭代推进让位置在瞬态段先走；Rapier 的双趟位置通道本引擎已两次否决 ⇒ 不建议再撞） | DESIGN-staged-solver §5 |
| S23 | `normal_inner` 4→3（歧管内层扫掠少一遍）可省成本且质量不降 | 扫描：inner=3 质量≈持平或更好（塔 \|v\| 0.230<0.236、125 dE 17.9<18.2、col45 平、pile5 −20 体），成本 总 −9%/解算 −11%；**但 m0_gates 末态活跃 0→5**（破全睡硬约束）；inner≤2 双侧均劣（125 dE 24.3/26.2、col45 30/45） | **未换默认**（9% 不换健康不变量；inner=3 记为速度预设） | M1-PLAN §14 |
| S24 | `PointConstraint` 热/冷分区（`repr(C)` 热字段前置，~96B 热区落两条行内）可减少扫掠触行 | 逐位中性（只改布局）但**无收益**：8B 解算 均 305/318（两次）vs 基线 290.8（同会话）⇒ 略负 | **回退**（推论：扫掠的访存大头是 `bodies` 侧数据（inv_mass/世界惯性张量应用——该侧缓存已在 §7-6 被否 +11%），点自身数据不是瓶颈） | M1-PLAN §15 |
| S25 | P1（碰撞检测上提到 tick 级、接触集跨子步存活）可让子步型排列变便宜 | `substeps=1` 逐位不变（哈希/测试同值）；配方（substeps=4）下 col45 **45/45 全睡**、pile5 **1970/2000**（都更好）但 **tower25 崩**（\|v\| 44.4、y 带 [−96.4,12.2]）；成本侧 8B 总均 **628ms**（同 64 扫掠的现状 329.8）——每子步固定成本（warm 表 33.5 + 建约束 12 + 建岛 3）被付 4 遍 | **回退**（P1 单独不成立：陈旧流形须由 P2 prepare 按当前位姿重推导；成本须先消灭每子步的 warm 表/建约束 ⇒ P1+P2 同批，见 DESIGN §9） | DESIGN-staged-solver §9 |
| S26 | warm 表换「稠密槽位表」（连续内存 + 键→u32 索引 + 空洞复用）可省成本，且逐位不变 | 同机背靠背 A/B：总均 **351.2→316.4（−34.8ms / −9.9%）**、解算均 309.4→275.8（−10.9%）；**逐位不变**（m0_gates/determinism/T4 哈希同值、金样塔逐项同数）。前置判别：印章法替代 binary_search 无收益 ⇒ 成本在桶访存而非查找算法 | **落地 ✅**（M1-PLAN §16） | M1-PLAN §16 |
| S27 | 被替换旧值的释放 churn 是 warm 表残余成本 | 泄漏探针（`mem::forget` 旧值，仅计时用）：解算均再降 **≈11.4ms**（273.3 vs 284.7）⇒ 22 万次小 Vec 分配+释放/ tick 是残余大头 | 真修法 = 点数据扁平 arena（下一步；探针已撤，未落地） | M1-PLAN §16 |
| S28 | 点数据进**扁平 arena**（off/len + 双缓冲）可省 warm 表的残余分配成本 | 收益真实（8B 总均 316.4→**298.6**，−17.8ms）**但 m0_gates 压力场景 panic**：回写被排在剪枝之后 ⇒ 本 tick 要回写的槽在剪枝时 `seen` 尚未更新而被误剪（索引被摘）⇒ 回写复活槽 ⇒ **同键双槽**（offset 越界） | **回退**；改走 S29 | M1-PLAN §17 |
| S29 | 点数据**定长内联**（`[WarmPoint; 4] + n`）可等效省成本且无新不变量 | 8B 总均 316.4→**298.7（−17.7ms / −5.6%）**、解算 275.8→258.9；**与 arena 收益相当**（⇒ 收益来自「消灭分配/释放 churn」，非「点连续布局」）；逐位不变（m0_gates 两场景/determinism/T4/41 测试/clippy 全过） | **落地 ✅** | M1-PLAN §18 |

**已排除清单（12 项，勿重做）**：机制缺失①、迭代不足②、erp 偏置③、皮肤④、
回拉强度/死区⑤、回拉上钳⑥、未承载不回拉⑦（本轮复测仍不行，连历史效果都不复现）、
回拉符号⑧、锥投影⑨、距离类判别式（半径/位移）⑩、全局开关（任一形态）⑪、
**恒真式判据（深度一致性|有效深度−裁剪深度|——数学上恒等，见 S14）⑫**。
**已落地**：回退半径 1×skin（ADR 0003）+ 深度跳变门/分离状态门（ADR 0007）。
**场景需求梯度（实测）**：tower > m0_gates ≫ col45/pile5。

### 并行与数据布局

| # | 假设 | 方法/结果 | 裁决 | 依据 |
|---|---|---|---|---|
| P1 | 负载不均是 4.7×/8 的病因 | 按流形数加权分组：216.6→214.6ms（−1% 噪声） | **回退**（岛分布本就均匀） | §6 |
| P2 | 窄相并行度差（读数） | 同窗口重测：1→8 线程 ≈3.2× | **读数误判纠正** | §6 |
| P3 | 约束扁平化可提速 | `ConstraintBatch`：216.6→215.8（−0.4% 噪声）；逐位透明 | **回退**（只与 SIMD 同批有意义） | §7-5 |
| P4 | 旋转矩阵缓存可提速（省 ~30 flops） | 逐位透明但 216.6→240.9（**+11%**）——36B 矩阵读 > 16B 四元数读 | **回退**；结论=解算访存延迟主导 | §7-6 |
| P5 | 4 岛轮转交错可提速 | 三步搭建；lane「相位耗尽判定在步进后」bug（步数 17/16），自检定位修复后哈希逐位回基线 ⇒ 透明但无收益（−3% 反向） | **路线关闭**（ADR 0006） | §7-7 |
| P6 | 同岛 4 点 4-lane 上界可观 | 直方图均值 2.52 点/流形、40% 填满 ⇒ 上界 ≈ 解算 30%×行为变化 ⇒ 净 ≤10% | **不开工**（先量化结论） | §7-8 |
| P7 | SoA+单岛向量化上界可观 | 3 分量 ⇒ 3/4 利用率；布局类改动已实测 −0.4% | **不开工** | §7-8 |

**成本结构（iters 扫描，三点共线残差 <1%）**：解算 = **23.0ms 固定 + 3.88ms/迭代**
（iters=16 时迭代占 73%；138ns/点·迭代 ≈ 2.6 ops/cycle——后被 P4 修正为访存偏乐观）。

## 窄相（T3）

| # | 假设 | 方法/结果 | 裁决 | 依据 |
|---|---|---|---|---|
| N1 | 成本在多面体测试 | 分相量测：SAT 扫描本身占大头，第十五段成本假设被纠正 | **归因修正** | 第十八段 |
| N2 | SSE2 4 轴 SAT 可达标 | p95 25.19→**23.30** ✅，四哈希逐位不变（标量孪生 + 4000 对逐位测试） | **落地 ✅** | ADR 0002 |
| N3 | 体轴缓存（每帧 3 次旋转算一次）可再省 | 均 11.54→**11.05**、峰 28.68（−4%），逐位透明 | **落地 ✅** | §6 |
| N4 | 盒对专用路径可达 25 | 峰 −30% 但 28.68 仍 >25；剩余 13% 触地板（标量算术+访存） | 已尽，余缺口核口径 | 第十九段 |

## 汇总口径

- **落地**（净正向，保留至今）：N2、N3、S12（回退半径 1×skin）、**S18（深度跳变门 +
  分离状态门）**、T2 的就地生长 refit + 查询缓存（树 p95 49.6→10.3、查询 p90 44.9→6.0）、
  漂移回拉满强度、软接触 + 子步 4、接触特征 ID、接触回收局部锚点。
- **否证且回退**：W1–W5、S1–S8/S10/S11/S13、P1/P3/P4/P5、以上各项全部留档。
- **结构性结论**：8B 单 tick ≈254ms 中解算占 85%，M1 约束内 ≥30FPS 不可达
  （详见 OPEN-PROBLEMS P3）。

## 2026-09-14 · 演示管线与双引擎对照（本节数字均为本机实跑）

**四域同场演示**（`examples/showcase` → `scripts/render_demo.py`）：
600 tick（30 盒 + 8 凸碎块 + 76 喷溅核 + 体素地板/墙 + 炮弹）单 tick 均 **1.33 ms
（752 FPS）**、峰值 2.17 ms（本机；体素面/渲染走 Python 侧，不计入）。产物
`docs/demo/showcase_full.gif`（12.2 s：片头 + 10 s 场景 + 对照卡）。

**vs Rapier 0.35（gold-sample，同场景同 tick，活跃 tick 口径）**：

| 场景 | vxl-phys | rapier 0.35（默认 TGS-Soft） | 末态 max\|Δpos\|（参照体） |
|---|---|---|---|
| col45（45 盒） | 0.98 ms/活跃tick（1021 FPS） | 0.14 ms/活跃tick（7142 FPS） | 0.036 m |
| pile5（2000 盒） | 38.2 ms/活跃tick（26 FPS） | 8.35 ms/活跃tick（120 FPS） | 0.025 m |

- 口径修正（教训）：首版按「全期均值」比 ⇒ rapier 在 ~50 tick 后全体入睡、step 空转，
  得出 0.01 ms/tick（78k FPS）的假数据。**必须按活跃 tick 计时**并在表里注明入睡面。
- 行为差异（如实）：vxl 本场景**全程不入睡**（45/2000 体全清醒），rapier 全部入睡；
  vxl 的 SPEC §3 休眠判据（重复唤醒 < 1 次/秒/体）合规，但「该睡时是否睡」另有差距。
- 性能位次：45 盒 7.0×、2000 盒 4.6×（活跃期）；与既有 8B 结论一致（10 万 dynamic
  @30 FPS 不可达，结构性）。
- 确定性：既有哈希基线（m0_gates / stress / determinism）**逐位不变**（新增形状/域
  未触碰既有路径）。

## 2026-09-14 · 限制清除（L1–L4，附负结果）

- **L1 外壳 × 高度场**：顶点采样接上（此前不受理 ⇒ 穿地）。新测试静置 y 带通过。
- **L2 外壳世界点缓存**：`(体号, 姿态指纹)` 命中即复用世界点；外壳三测试逐位不变。
- **L3 喷溅均匀网格**：格边长 = 3·max(σ)，格内按注册序登记 ⇒ **与全扫逐位相等**
  （`to_bits()` 断言）；微基准 2000 核 × 2000 点 × 10 轮：全扫 234.1 ms → **网格 5.1 ms
  （45.8×）**。`grid_min_splats = 64`（小场全扫更快）；超 4M 格不建（防内存灾难）。
- **L4 压测求解环两项微优化：证伪并回退**（p50 基线 305 ms / 本机同状态复测 299–301 ms）：
  - 实验 A（缓存旋转矩阵，64B 行）：366 ms（−20%）；
  - 实验 B（32B 行：四元数 + inv_mass + 逆惯量）：383 ms（−25%）；
  - 两次哈希最终都与基线一致（**逐位安全成立**），但**都更慢** ⇒ 净负，已回退。
  - **副产物教训**：实验 A 期间因「新并行数组漏清」（gather 只清了 lv/av）导致哈希变化 ——
    被四哈希门禁当场抓住（这是本项目把「逐位不变」当门禁的直接收益）。
  - 归因：原三次散查按约束序推进 ⇒ 流式 + 预取友好；`local_of → 稠密行` 是依赖加载链，
    多一跳串行延迟、工作集又没真降 ⇒ 该环内「加 per-body 数组」是净负。
  - ⇒ L4 剩余杠杆 = 岛内接触图着色并行（改序 ⇒ 需整体重定标），见 PLAN-0.2 附录 A。

## 2026-09-14 · L4 穷举（五个实验的完整记录）

| 实验 | 改动 | 压测 p50（同窗口基线 296.7–300.4） | 判定 |
|---|---|---|---|
| A | 缓存旋转矩阵成 64B/体稠密行（local_of 间接） | 366 ms | 证伪（−20%），回退 |
| B | 32B 行（四元数+im+ii），去三次散查 | 383 ms | 证伪（−25%），回退 |
| C | 48B 矩阵按**体号直取**（去间接） | 361–366 ms | 证伪（−20%），回退 |
| D | 冷热分离（PointConstraint 188→100B） | 295.5 ms | 未达门槛（≈持平），回退 |
| E | 切向联立解预计算（k11/k22/inv_det 构建期一次） | 290.8–294.2（另窗口 299–305.6） | 噪声量级（±3% 漂移），按纪律回退 |

结论：该迭代环不是算术/布局受限，而是「每点每轮 4 次速度数组访问 × 点数 × 轮数」
本身；逐位安全空间已穷尽。10× 需算法面改动（Δ点数/轮数、着色并行、staged 睡眠），
均须重定标四哈希与金样容差 ⇒ 独立轨道（PLAN-0.2 附录 A）。

**副产物**：实验 A 期间「新并行数组漏清」导致哈希变化被四哈希门禁当场抓住（见
KNOWLEDGE）；D 的失败教训 = 「预估 cache 行数」必须实测（环内原本只有 2 条线）。

## 2026-09-15 · 液体域切片 1（WCSPH）实测与负面结论

- **边界口径三次误诊**（全记录 PLAN-0.3 §4.1）：①「盆泄漏」首查实为零厚度墙
  typo（`fill_box` min==max）⇒ 修掉仍漏，非根因全体；②截断 SDF 门在壳体近面
  内侧误报「外部」⇒ 内点被沿最近面推出壳面，逐 tick 单向外移 = **外向棘轮**；
  ③终版 `contacts_point_voxel_solid`：占据位图判内外 + 内点恒推最近开放面 +
  壳面/截断 SDF 双回退。终版 4×4×4 盆 300 tick 零穿壁零外逸。
- **驻留瞬态顶心喷泉**（负面结论，未修，以铸装初值规避）：任何带落差入盆水块
  （0.8 m 重落 / 0.1 m 轻落 / 触底就位**皆复现**）⇒ 底部镜像鬼影密度尾（底压
  ≈1.65× 静水）⇒ 压实波块顶心聚焦 ⇒ 粒子以 ~9 m/s（近 vmax≈9.6 钳制）垂直
  喷过敞口。**非边界失效**（全程零穿壁），是显式压缩求解器的驻留瞬态。铸装
  近平衡初值（沉降几何反推晶格直接就位，[8,8,5] 口径）瞬态消失。
- **dam-break 自由柱对照**：紧凑盆（1.5×1.5 m 内域）504 粒柱前 2 tick「闪塌」
  （柱顶 1.70→1.32，初值压缩释放），波前 ~2.6 m/s，~2.7 s 扫满全盆；全程
  y ≥ 1.000（零穿底）、终态恰为内域（零越堰）、仅 1 粒受瞬态喷出后回落。
  7 m 大盆对照：同水量摊 1–3 mm 水膜 ⇒ 演示尺度定律（0.063 m³ 水 ↔ ≈1.5 m
  内域盆）。
- **性能**：showcase 六域 320 流体粒 ×4 子步全场景 4.23 ms/tick（236 FPS）；
  dam_break 504 粒 3.80 ms/tick。四哈希门禁逐位不变（流体 = 纯新增路径）。

## 2026-09-15 · 接 PhysArena 同台对比（引擎侧改动与实测）

背景：把本引擎作为第 9 个参战引擎接进 `D:\开发\physarena`（浏览器物理引擎测试场），
与 Rapier / Jolt / PhysX 5 / Bullet / Havok / Crashcat / cannon-es / Oimo.js 同场景对比。
桥 = `physarena/wasm-bridge/`（独立工作区的工具 crate，零 unsafe：Rust 侧缓冲 +
导出数据指针，JS 建视图一次性拷出；引擎 crates 仍 `#![forbid(unsafe_code)]`）。

- **新增 `vxl-phys-core::probe`（跨目标计时探针）**：`wasm32-unknown-unknown` 没有
  时钟实现，`Instant::now()` 会 panic——在 wasm 桥里表现为 `unreachable` 陷阱
  （首次接入即命中）。原生走真实 `Instant`，wasm 退化为 0 时长。计时是诊断量、
  不参与状态哈希：改后 `m0_gates` 压测哈希 `0x2cc8c488…` 与 `determinism`
  `0x8142fe05…` **逐位不变**（原生行为零扰动）。门面 `substep` 与 broad/solver
  共 14 处生产计时点改走 `probe`。
- **流体静水压测试顶带闸 5× → 3×（标定修正）**：release 实测顶带 157 / 中带 646
  ≈ 4.1×（旧 5× 闸门在此误报失败），debug 下同一 5× 闸门通过 ⇒ 两 profile 的
  绝对值不同（顶带含镜像鬼影密度尾，PLAN-0.3 §4.3）。3× 仍能抓住真实缺陷
  （"顶带与中带同量级"），且不被 profile 差异误报；两个 profile 现均 7/7。
- **同台成绩（19 正确性探针）**：0 失败 / 10 降级（关节未接桥 = 声明不支持，
  复合体/三角网按盒、圆柱/圆锥/胶囊按凸包——全部显式标注）。通过的含
  **CCD 拦 240 m/s 弹丸**（同一探针下 PhysX-WASM 实测拦不住，见该仓 README）、
  静置休眠、5 层堆叠不塌、8 体位姿互不串扰、自由落体能量守常。
- **同台成绩（吞吐，p50 单步 · 9 引擎 × 8 场景平均）**：PhysX 0.29 / Havok 0.35 /
  Oimo 0.56 / Jolt 0.63 / Rapier 0.72 / Crashcat 1.09 / Bullet(asm.js) 2.06 /
  **vxl-phys 2.71** / cannon-es 47.5（含一格 30 s 超限）。本引擎当前定位：正确性
  全绿、吞吐中游（16 速度迭代、无 SIMD、无并行；金字塔 210 体 9.47 ms/步）。

## 2026-09-15 · 求解预算标定（12×2）——本次唯一的**行为改变**优化

背景：接 PhysArena 同台对比后定位到差距——金字塔 210 体 9.5 ms/步 vs PhysX
0.52 / Rapier 1.29 / Jolt 1.77。相位剖析（`examples/arena_bench`，复刻 PhysArena
同场景）显示**求解环占 97%**（宽相 1%、窄相 3%、积分 0%），而我们的求解扫掠总量
= 外层 16 × 法向内层 4 = **64 遍**，Rapier 4 遍、Jolt 10+2 遍。

**方法**：`arena_bench` 提供 `--iters N --inner K --steps M`，判据不是"180 步窗口内
好看"，而是**窗口后再跑 900 步**看堆是否还站着（这一条立刻推翻了第一版结论：
8×2 在 210 步窗口内更快更静，900 步后 210 体金字塔垮塌——堆顶 19.4→3.5）。

| 配置 | 扫掠 | 金字塔 p50 | 900 步后堆顶 | 残留 Σv² | 判定 |
|---|---|---|---|---|---|
| 16×4（旧默认） | 64 | 13.46 ms | 19.402 | 1.17 | 站住（基线） |
| 16×3 | 48 | 7.50 | 19.402 | 0.65 | 站住 |
| 12×4 | 48 | 7.20 | 19.402 | 1.41 | 站住 |
| 16×2 | 32 | 5.21 | 19.405 | 0.69 | 站住 |
| **12×2（新默认）** | **24** | **4.11** | **19.409（最高）** | **0.39（最低）** | **站住 ✓✓** |
| 8×3 | 24 | 4.05 | 6.04 | 1591 | **垮** |
| 4×4 | 16 | 2.89 | 5.48 | 3133 | **垮** |
| 8×2 | 16 | 3.14 | 3.49 | 53 | **垮** |

**结论**：决定量是**总扫掠预算**，且预算 ≥24 才托得住 210 体金字塔；12×2 在同预算
里既快（比 16×4 快 3.3×）又最稳（长跑抖动 0.39 vs 1.17、堆顶 19.409 vs 19.402）。
三场景复测（12×2 vs 16×4，各含 900 步长跑）：

- 金字塔 210 体：13.46 → **4.11 ms**（3.3×），堆顶 19.402 → 19.409，Σv² 1.17 → 0.39；
- 砖墙 200 体：8.67 → **3.37 ms**（2.6×），堆顶 6.946 → 6.947，Σv² 5.03 → 0.52；
- 球坑 400 体：2.22 → **1.35 ms**（1.6×），穿透/堆高一致；
- 压力场景 10001 盒：p50 **183.4 ms**（`m0_gates` report-only 档）。

**代价与配套**：末态改变 ⇒ 三个门禁哈希换代（m0 `0xa2b4a080…`、压力
`0x72c6e73b…`、determinism `0x98d32c4a…`），旧值在 `RECIPES.md` 里保留可回溯；
全工作区 43 个测试套件在 12×2 下全绿（含堆叠/休眠/能量/CCD/流体/确定性）。
默认值与注释同步更新在 `PhysConfig`（含依据引用）。

**未做（下一步候选）**：每点每轮的内层开销（4 次速度读 + 4 次世界逆惯量变换）
在 L4 已穷举布局类优化（五实验全否），剩余杠杆是**算法面**：接触图着色并行
（改序 ⇒ 需重定标）与 SIMD（本仓纪律禁内建，需先议）。

### 追加：收敛早退（同日，第二处行为改变）

在上述 12×2 基础上加**收敛早退**：每次扫掠累计"最大速度级修正"残差
（`Δλ·有效质量`，法向 + 摩擦通道取最大），跑满 6 次外层后残差 < 0.002 m/s
即提前结束剩余外层迭代。判据是状态的纯函数（同状态 ⇒ 同退出点），确定性、
串并行等价性不受影响。

- native：金字塔 4.42 → **4.07/4.11 ms**（+8%，两轮重复）；长跑 900 步抖动
  0.39 → **0.30**（更稳）；
- **wasm 侧 ≈ 中性**（同进程交错 A/B 各 3 轮：开 3.22/3.98/3.96 vs 关
  3.97/3.86/4.01 —— 差异在噪声内）；保留的理由是 native 收益 + 抖动下降。
- 代价：末态再变 ⇒ 门禁哈希再次换代（最终值见 RECIPES）。

### 标定后的最终成绩（同机、同场景）

| 场景（体数） | 原 16×4（wasm） | 现 12×2+早退（wasm） | 现（native） | 900 步后堆顶 |
|---|---|---|---|---|
| 金字塔 210 | 9.47 ms | **3.99 ms（2.4×）** | 3.93 ms | 19.401（原 19.402，站住） |
| 砖墙 200 | 7.44 ms | **3.32 ms（2.2×）** | 3.33 ms | 6.938（原 6.934，站住） |
| 球坑 400 | 2.11 ms | **1.26 ms（1.7×）** | 1.24 ms | 1.500 |

同台坐标（PhysArena 8 场景 p50，wasm）：球坑已进**前三**（1.26 vs PhysX 0.60 /
Rapier 1.08 / Oimo 1.19 / Jolt 1.86）；金字塔与砖墙仍落后第一梯队约 2–2.5×
（Rapier 1.29 / Jolt 1.77 / PhysX 0.52）。剩余差距在"每点每轮的内层成本"，
需算法面改动（着色并行 / SIMD 决策）。

## 2026-09-15 · 第二轮优化尝试：三实验（两中性一证伪）+ 一个真障碍

继续追第一梯队，做了四个实验，**只有第四个产出真结论**——其余如实记负：

1. **编译器级 SIMD（wasm `+simd128` / 无源码改动）**：`RUSTFLAGS=-C target-feature=+simd128`
   构建桥，同进程交错 A/B（每项 3 轮）：金字塔 6.31–6.42 vs 6.40–6.55 ms、
   球坑 1.79–1.87 vs 1.82–1.90 ms ⇒ **零收益**；末态**逐位一致**（位指纹相同）
   ⇒ LLVM 不自动向量化"经 `local_of` 索引聚集 + 依赖链"的内层循环。保留结论：
   SIMD 若要收益必须写显式内建（本仓纪律禁），且内层不是 4 宽友好的 SoA 形状。
2. **每点世界逆惯量/旋转矩阵缓存**：每帧 gather 一次 `Mat3::from_quat(rot)`，
   求解环内复用（表达式与 `apply_world_inv_inertia` 同序 ⇒ **逐位一致**，
   三哈希不变）：金字塔/wall/ballpit 在噪声带内（±10%）⇒ **中性**。与 L4
   五实验结论一致：该环不是算术受限。代码保留（更少运算、零风险），但不计收益。
3. **TGS 式双子步（形状实验，非算术）**：`substeps=2` 下扫掠可降到 6–10 且
   **更快更稳**（3600 步长跑判据）：

   | 配置 | 扫掠 | 金字塔 p50 | 墙 p50 | 金字塔长跑抖动 | 墙长跑抖动 |
   |---|---|---|---|---|---|
   | 1×12×2（当前默认） | 24 | 4.32 ms | 3.72 ms | 1.45 | 0.61 |
   | 2×3×1 | 6 | **2.32** | **1.86** | **1.27** | 2.59 |
   | **2×5×1** | **10** | **3.28** | **2.50** | **0.59** | **0.44** |
   | 2×5×2 | 20 | — | — | — | — |

   即"把大步长拆小后，收敛所需扫掠显著下降"——本可再拿 1.3–1.5×。
4. **真障碍（本轮最重要的发现，含一次自我推翻）**：2 子步档下炮弹撞体素墙不再
   挖洞（破坏测试 debris=0/carved=0），初判为"摩擦界含去穿透偏置冲量导致粘滞"。
   **随后用新基准 `arena_bench slide` 实测推翻了该假设**：滑行盒的减速度在
   **所有配置**下都是 **0.94× μ·g**（0.107 vs 理论 0.1145 m/s/tick，误差 6%）
   ——摩擦保真度是好的。再上 `arena_bench approach`（12 m/s 撞静态薄墙）：
   **静态盒接触在两种配置下都正确**（贴到表面间距 0.0000 停住）。
   真正的回归点收窄为 **体素 provider 接触路径**：2 子步下弹体停在墙前
   **0.122 m**（体速被吃掉但没有接触力），1 子步下同一场景能撞上并挖洞。
   两者合起来给出结论：**摩擦与通用接触都没问题，问题在 provider（体素）边界的
   近距接触生成/provider 推回逻辑与 dt 的耦合**——这是"降到 10 扫掠"的**唯一
   前置障碍**，修法方向：provider 边界在近距（skin 带内）的接触/推回按**帧 dt**
   归一（不随子步变硬），并用下面两个基准做回归。
5. **两个新基准（已入库，供修复验证）**：
   - `--example arena_bench -- slide`：滑行减速 vs μ·g（摩擦保真度）；
   - `--example arena_bench -- approach`：接近-贴合间距轨迹（接触响应刚度；
     期望贴到 ≈skin，被挡在外 = 回归）。

6. **根因下钻 + 一次被证伪的试修（同日）**：定位到窄相 provider 分支的
   `band = max(skin, |v_rel|·dt·1.5)`（12 m/s ⇒ **0.30 m**），而 provider 侧
   按**该带**报深度（`depth = band − 距离`）⇒ 求解器把"带内但未接触"当作
   真实穿透：2 子步下 12 m/s 弹体在离墙 **0.122 m** 处被吃掉速度（1 子步时
   单步位移 0.2 m 盖过推出量，所以能撞上——同一个带在两种 dt 下表现不同）。
   **试修被证伪**：把深度重基准到物理 skin（`depth − (band − skin)`，
   带只用于检测）后，`approach` 基准立刻转好（贴合 0.0000 ✓），但
   `box_falls_and_rests_on_voxel_provider` 直接**穿地**（y=−488）——说明
   这个"带内正深度"同时承担着**抗隧穿**职责（袋内即压出）。
   ⇒ 结论：要改成"检测带 + 真实 skin 深度"必须**同时**给求解器补
   **预测接触（负深度）行**（按 distance/dt 限接近速度），否则抗隧穿失效。
   该试修已回退（43 套件全绿、工作树干净），改动范围与回归基准（approach +
   该体素静置测试）已在本节钉死——这是"启用 2 子步档（+1.3–1.5×）"的前置工程。

## 2026-09-15 · provider 墙角丢面（真根因）与 2 子步档落地

上一条的"带深语义"判断**再次被自己的测量修正**：读 provider 源码确认
`contacts_box_voxel` 报的是**真实深度**（`depth = −d`），带只用于"接受窗口"。
真因是**另一个**局部缺陷：

- **provider 只发「主导面」**：`contacts_box_voxel` 内部按"带内采样数最多、
  并列取最深"选**一张面**发射。墙角（地板 + 墙同时命中）时两面点数并列，
  按深度决胜选中**地板面**，**墙面整张被丢**⇒ 体的水平动量被倾斜地板法线吸收、
  撞墙事件不登记。复现基准 `arena_bench wall_provider`：2 子步下 12 m/s 弹体
  在墙前 0.14 m 处 vx 11.4→−0.02（墙面法线缺失）；1 子步恰好奇选中墙面 ⇒ 能挖洞
  ——这也解释了为什么"破坏测试"只在一个档位失败。
- **带宽刀锋**：`band = |v|·dt·1.5` 与"样点到墙面距离"几乎相等（0.1398 vs 0.14）
  ⇒ 接触的有无在速度上不连续。修法：带加一个 skin 余量（`+ self.skin`），
  预测接触由求解器的 `spec = sep/dt` 行限接近速度，不会假制动。
- **修法（两处配套）**：
  ① provider 侧**逐面发射**（6 张面各自把带内样本发出，法线按面、特征 = 面号×16+采样号）；
  ② 窄相侧选择次序改为「**闭合速度最大的面**（正在撞上去）→ 否则只考虑
  **带面心样本**的组（排除只有角点的伪面）→ 组内点数最多、并列取最深」。
  两处合起来：墙角两面都进候选、按接近方向选墙、静置时仍选地板（底面才有面心）。
- **回归**：43 套件全绿（含破坏测试、体素静置、堆叠、CCD、流体、确定性）；
  基准读数——落体静置高度 1.470（单子步）→ **1.500（双子步，恰为物理值）**；
  贴墙不再停在外侧。

**落地效果（2 子步 × 5 × 内层 1 = 10 扫掠，取代 16×4=64 / 12×2=24）**：

| 场景（体数） | 16×4 | 12×2+早退 | **2×5×1（新默认）** | 长跑抖动（新 vs 旧） |
|---|---|---|---|---|
| 金字塔 210 | 9.47 ms（wasm） | 3.99 | **3.31 ms（native）** | 0.59 vs 1.45 ✓ |
| 砖墙 200 | 7.44 | 3.32 | **2.77** | 0.44 vs 0.61 ✓ |
| 球坑 400 | 2.11 | 1.26 | **1.22** | 持平 ✓ |

即：**比 16×4 快 2.9–2.9×，比 12×2 再快 1.3×，且三个场景的堆高/穿透/抖动全面更好**
（堆顶 19.428 vs 19.408；静置高度 1.500 = 物理值）。三哈希换代，旧值全部留在
`RECIPES.md` 可回溯。

### 追加：世界逆惯量预积（同日，每点求解成本的第一刀，−25%）

相位剖析的结论（求解环 ≈97%）之后，本轮对**每点每轮的工作量**下刀：旧路径每次冲量
施加要做 `Rᵀ·x → diag(inv_local) → R·(...)`（**两次矩阵乘** ≈21 mul），而姿态在一帧内
不变（解算只改速度、姿态在解算之后积分）⇒ 每帧 gather 阶段把
`M = R·diag(inv_local)·Rᵀ` 预积一次（`Mat3::world_inv_inertia`），求解环内每点每轮
只剩**一次** `M·(r×imp)`。

- native：金字塔 3.31→**2.51 ms**（−24%）、墙 2.77→**2.03**（−27%）、球坑 1.22→**1.04**（−15%）；
- wasm（同进程交错 A/B 各 3 轮）：金字塔中位 ≈2.55→**2.34**、球坑 ≈1.23→**1.06**；
- 长跑质量不变（金字塔堆顶 19.427、抖动 0.52；墙 6.959/0.68）；
- 43 套件全绿；**哈希换代**（数学等价、舍入不同：`0x7d5f3723…` / `0x8984a075…` /
  `0x6120a78c…`）。
- 与早先"旋转矩阵缓存"的区别（后者实测中性）：只缓存 `R` 并不减少矩阵乘次数
  （仍要 Rᵀ、R 两次），**预积成 M 才真正把每点两次矩阵乘降为一次**——这也是为什么
  本次有收益而上次没有。

**累积成绩（wasm，同参场景）**：金字塔 9.47→**2.08 ms**（4.6×）、砖墙 7.44→**1.72**
（4.3×）、球坑 2.11→**0.97**（2.2×，同台第 2，仅次 PhysX 0.60）。距第一梯队的
剩余差距（金字塔 vs Rapier 1.29 / Jolt 1.77）：**≈1.2–1.6×**。

### 追加：扫掠阶梯复查 → 6 扫掠档（同日，再 1.2–1.26×，进第一梯队区间）

provider 修复 + 世界逆惯量预积之后**重扫阶梯**（3600 步长跑判据；此前的"6 扫掠
会垮"是旧代码的结论，已不适用）：

| 配置 | 扫掠 | 金字塔 p50 / 堆顶 / 抖动 | 墙 p50 / 堆顶 / 抖动 |
|---|---|---|---|
| 2×5×1（上一档） | 10 | 2.56 ms / 19.427 / 0.52 | 1.81 ms / 6.959 / 0.68 |
| 2×4×1 | 8 | 2.15 / 19.433 / 0.64 | 1.66 / 6.963 / 4.84 |
| **2×3×1（新默认）** | **6** | **2.04** / 19.424 / 1.63 | **1.52** / 6.962 / 0.90 |
| 3×3×1 | 9 | 2.94 / 19.437 / 0.42 | 2.32 / 6.961 / 0.71 |

**结论**：6 扫掠即可站住（金字塔 19.424 站着、墙 6.962），43 套件全绿；代价是
金字塔长跑微动 0.52→1.63（≈旧 16×4 档的 1.45，"站着微动"而非垮塌）。10 扫掠档
（`--iters 5`）保留为保守选择。wasm 侧最终读数：

| 场景 | 16×4（最初） | 现在（2×3×1 + 逆惯量） | 提速 |
|---|---|---|---|
| 金字塔 210 | 9.47 ms | **1.78 ms** | **5.3×** |
| 砖墙 200 | 7.44 | **1.36** | **5.5×** |
| 球坑 400 | 2.11 | **0.95** | 2.2× |

同台坐标：金字塔 **1.78 vs Jolt 1.77 / Rapier 1.29 / PhysX 0.52**（与 Jolt 持平，
首次进第一梯队区间）；砖墙 1.36（第 4）；球坑 0.95（第 2）。三哈希换代
（`0x4dcf5d46…` / `0x1e855f89…` / `0x3a8c778e…`）。

**顺带修好的一件真缺陷（与提速无关，dt 无关性）**：破坏管线改为消费
**解算前**的冲击快照（新增 `World::record_impacts` / `ImpactRecord`，窄相后、
求解前记录接近速度/接触点/速度方向）。旧实现读"解算后速度"，子步/迭代把
法向速度解掉后管线就看不见这次冲击了——同一场景在 1 子步下能挖洞、2 子步下
挖不动（实测 debris=0/carved=0）。修后破坏与子步数解耦；三哈希不变（记录
不改物理），43 测试套件全绿。

**方法学注记**：本机连续长跑后会降频（同码同场景 20 分钟内漂移 ~15%），
因此**所有性能结论都来自同进程交错 A/B 或同批交替测量**，不采信跨时间点的
绝对值比较。

## 2026-09-15 · 关节族落地（M2 首切片）与三个真缺陷

落地面：`vxl-phys-solver::joints`（球/转动/固定/棱柱/距离）→ 门面
（`World::joints`/`add_joint`，接触解算之后、位置积分之前求解）→ 桥
（`vxl_joint_add`，锚点/轴均为**体局部量**）→ PhysArena 适配器（5 种 IR 关节）。

**PhysArena 自检矩阵：vxl-phys 10 通过 / 9 降级 → 15 通过 / 4 降级、0 失败**
（5 个关节探针全部转正；剩余 4 项降级全是几何近似：胶囊/圆柱/圆锥→凸包、
复合体→并集凸包，与本轮无关）。

### 落地时踩到的三个真缺陷（全部会"静默错"或"爆炸"，都不会自己报错）

1. **K 矩阵填列覆盖对角线 ⇒ 约束静默失效**。`point_k` 先在对角写 `invMa+invMb`，
   随后按列写角向贡献时用了 `row[j] = col[i]`（应为 `+=`）——质量项被冲掉，
   矩阵退化成**纯角向项**：与 `r` 平行的分量对角为 0 ⇒ 判异守卫直接返回 `None`，
   **约束一帧都不施加**，体按自由飞行走（症状：分离按 2 m/s×dt 线性增长、
   速度分毫不变，看起来像"关节没接线"）。
2. **有效质量的叉乘顺序写反 ⇒ 约束变放大器**。点约束的 (i,j) 元是
   `e_i·((Ia⁻¹(ra×e_j)) × ra)`（**先叉 → 过惯量 → 再叉 r**）。写成
   `ra × (Ia⁻¹(ra × e_j))` 会整体反号：初速 2 m/s 的悬挂体 3 步内涨到 1e5。
   （单行通道 `row_mass` 用的顺序一直是对的，所以"距离关节通过、球形爆炸"
   曾把排查引向错误方向。）
3. **角冲量未按逆惯量缩放 ⇒ 静态端积分出角速度**。角行施加写成
   `ω_a -= λ`：静态体 `I⁻¹ = 0` 本应纹丝不动，实际被写入了 λ；下一迭代
   `wrel = ω_b − ω_a` 里越积越大 ⇒ 固定/棱柱关节 3 步炸到 1e33。修法 =
   成对施加一律过 `apply_world_inv_inertia`（与线性通道同口径）。

### 复刻探针时的假通过（值得单独记一笔）

`arena_bench joints` 逐字复刻 PhysArena 的 `jointProbe`（同几何/同容差/240 步）。
第一版用"密度 0 的盒"当静态锚——但 `mass_props` 对 `density <= 0` **回退 1.0**，
于是锚变成普通动态体：锚与悬挂体**一起自由落体**，相对锚点分离恒 ≈0
⇒ 5 个探针全部"PASS 0.0000 m"。改用 `add_static` 后才是真读数。教训：
**关节类判据必须同时报告"体是否真的没动"**（现在探针输出里带悬挂体末态 y：
9.150 = 未动 = 关节真的在顶住重力；掉到 0.4 = 自由落体的假通过）。

### 离线自检（引擎侧，不依赖前端）

| 场景 | 判据 | 读数 |
|---|---|---|
| 5 个关节探针（复刻） | 锚点分离 ≤ 容差（0.2/0.2/0.3/0.2/0.35） | **全 PASS，峰值 0.0000 m**（悬挂体 y 不变） |
| 铰链链 40 环（动态） | 全链最大锚点分离 | **0.020 m**（3.4% 链节长），无 NaN，0.135 ms/步 |
| 悬挂塔 40 段（固定关节） | 最大锚点分离 + 相邻姿态同向 cos | **0.025 m**，cos = **1.0000**，0.127 ms/步 |

**关节迭代预算单列（`PhysConfig::joint_iterations`，默认 8）**：接触档 3 迭代是
标定过的接触预算，关节敏感度完全不同（单向 GS 传播）：

| 迭代 | 链最大分离 | 塔最大分离 | 步耗时（40 关节） |
|---|---|---|---|
| 3 | 0.0481 m | 0.0730 m | 0.075 ms |
| **8（默认）** | **0.0204** | **0.0253** | 0.144 ms |
| 16 | 0.0133 | 0.0117 | 0.233 ms |
| 32 | 0.0108 | 0.0053 | 0.428 ms |

**唤醒语义**：静态端**不触发**唤醒（静态体 `awake` 恒 true）——否则每个挂在
静态锚点上的关节都会把关着的体每子步踢醒，悬挂摆永不入睡、岛级休眠对关节
场景失效。整组沉睡/双静态 ⇒ 关节跳过（对沉睡体写冲量会破坏休眠语义）。

### 浏览器侧：同批闲时扫描 + 一个测量纪律事故

9 引擎 × 8 场景**同一会话闲时**重测（关节接桥后关节场景必须重测）。同时确认了
一个此前记过的坑有量化下界：

| 条件 | vxl 金字塔 p50 | 状态哈希 |
|---|---|---|
| 闲时（重复 3 次） | 1.880 / 1.885 / 1.887 ms | `36faadba` |
| 4 个 node 满核燃烧 | 1.932 / 1.992 ms | `36faadba` |
| **全核 `cargo test`** | **3.138 / 2.203 ms** | `36faadba` |

**同一份 wasm、同一个哈希**，读数被机器竞争抬高最多 ≈1.7×。旧跑分表（vxl 金字塔
3.00 / 平均 1.36）是在有编译负载的会话里测的，**与闲时表不可比**；新表 vxl 平均
**1.17 ms**（9 引擎第 4，金字塔 1.92 / 砖墙 1.55 / 球坑 0.99 / 三角网 2.09）。
结论写进 README：跑分必须闲时同批做，跨表只比"同批内的相对位置"。

### 一处诚实更正：clippy 门早就红了

本轮跑门禁链时发现 `cargo clippy -- -D warnings` **失败**在
`vxl-phys-solver::solve_constraint`（8 参数 / 上限 7）。该签名在 HEAD 上逐字相同，
即**这不是本轮引入的**：`normal_inner` 落地时给它加了第 8 个参数，而漏挂仓库既有的
`#[allow(clippy::too_many_arguments)]`（热路径签名上已有 7 处同款），
此前若干轮"门禁全绿"的说法对 clippy 这一项**是错的**。本轮按同风格补挂，
门禁链（fmt/test/clippy/vocab/typos）现在**真的**全绿。

**哈希记录**：本轮三个门哈希**未变**——门槛 `0x4dcf5d460298e316ede2a846391c1990`、
压力 `0x1e855f89f30fe8e40ae1ccbff98bc4a6`、确定性 `0x3a8c778ebaf76a9a2be25bc1f95857de`
（关节不参与这些场景；`PhysConfig` 新字段对它们零影响）。T4 碎片雨 3.95× ≥ 3×、
串并行逐位一致。

## 2026-09-15 · 每点求解成本（第一刀：内联点数组）与一次被撤回的读数

**方法先把两半分开**：求解相位对"扫掠数"做线性拟合（`arena_bench pyramid --iters k`，
扫掠 = 2×k）：2 扫掠 1010.6 µs、4 → 1382.6、6 → 1664.9、12 → 2253.4 µs ⇒
**每扫掠 ≈124 µs、每帧固定 ≈762 µs**——固定部分（不是扫掠）才是大头。
再加永久诊断计时 `ImpulseSolver::last_detail_us`（建岛 / 约束构建 / 热启动预施加 /
迭代扫掠），金字塔上定位到：**约束构建 51%（735.6 µs）+ 迭代扫掠 47%（679.7 µs）**，
建岛 ≈0。约束构建 = 1140 次/帧（570 流形 × 2 子步），即 **645 ns/流形（161 ns/点）**
——其中每流形一次 `Vec::with_capacity(4)` 堆分配 + 释放（2280 次分配/帧）是明确嫌疑。

**改动**：`ContactConstraint.points` 从 `Vec<PointConstraint>` 改为**定长内联数组
[PointConstraint; 4] + npts**（与 `WarmManifold.points` 同一课——那次是每 tick 22 万次
小 Vec ≈11.4 ms）。**逐位等价**（同样的浮点运算顺序、同样的接受/拒绝分支）。

**可信读数（同进程交错 A/B，wasm 双构建，3 轮交替，`vxl-simd-ab.mjs`）**：

| 场景 | A（旧）p50 | B（新）p50 | 变化 | 末态位指纹 |
|---|---|---|---|---|
| 金字塔 210 | 2.102 / 2.059 / **1.985** | 2.028 / 1.964 / **1.903** | **−4.1%** | `ac3de6dd22750d6f` **A=B** |
| 球坑 400 | 1.006 / 0.989 / 0.995 | 1.004 / 0.979 / 0.976 | −1.1% | `3eda4988302d139d` **A=B** |

**被撤回的读数（重要，写在这里防复发）**：native 单跑先给出"约束构建 735.6 → 391.7 µs、
金字塔 1.91 → 1.38 ms（−27%）"——但同一份**新**代码在 40 分钟后重测又回到
462–490 µs / 1.62–1.68 ms（会话内机器降频）。**跨时间点的绝对值差不可信**，
真值是交错 A/B 的 **−4.1%**；此前的 −27% 是漂移假象，已在提交信息与 README 里按 −4% 记。

**负面结果（同样重要）**：`warm_index`（体对 → warm 槽号）的哈希从 std SipHash 换成
自写乘-异或（每帧 1140 次查找），预估 30–45 µs/帧；交错口径实测 **≈1%（噪声内）**
⇒ **回退**，不留"没测出收益的复杂度"。（键是引擎内部索引对，无安全要求，
将来若大场景实测出收益可再捡起。）

**剩余杠杆（都已量化，但都会换代哈希，故本轮不动）**：
1. **质量项融合**：`contact_mass(n/t1/t2)` + `contact_mass_cross` 四处各自做
   "叉 → 过 I⁻¹ → 再叉 → 点乘"；用恒等式 `dir·((I⁻¹u)×r) = (I⁻¹u)·u`（u = r×dir）
   可省掉每项一次叉乘（≈19% 的质量项运算），且四处共享 `I⁻¹(ra×dir)` 中间量；
   预估 ≤5% 帧内（构建相位现状 ≈28% 帧）。
2. **扫掠内预积角向量**：`group_apply` 每次冲量做一次 `M·(r×imp)`（9+15 flops +
   36 B 矩阵加载）；按点按轴预积 `I⁻¹(r×axis)`（6 个 Vec3/点）后，每次冲量只剩
   一次标量乘加 ⇒ 预估省下扫掠相位的 30–40%（wasm 上扫掠占 ≈40–45% 帧），
   代价是 `PointConstraint` 增 ≈72 B/点（但省掉的矩阵加载更多，净访存下降）。
   **这一项是下一步的首选**。

### 追加：第二刀（扫掠内预积角向量）——**做了、量了、回退了**

按上面的"首选"实现：`contact_basis` 一次算完四个有效质量 + 六个角向因子
（`M·(r×axis)`，axis ∈ {n,t1,t2}×两侧体），存进 `PointConstraint`；求解环改用
`Δω = dλ·w`（`apply_point_pair`），删掉 `contact_mass`/`contact_mass_cross`/
`group_apply` 三个旧函数。

**实现中踩到的坑（下次必看）**：第一版把金字塔一步打崩（堆顶 19.4 → 9.0、动能
10 万）。原因：`inv_i_world` **只对清醒岛成员填充**，静态体槽位留的是占位默认值
`Mat3::IDENTITY` ⇒ 静态侧的预积因子变成 `ra×n` 这种非零垃圾，**污染有效质量项**。
旧 `contact_mass` 的 `if im_a > 0.0` 守卫正是为此存在（施加路径另有
`local_of == u32::MAX` 守卫，两条构成完整防护）。补回守卫后恢复正常。

**交错相位对比（旧/新二进制交替，同一温窗）**：

| 相位 | 旧（fa04641） | 新 | 变化 |
|---|---|---|---|
| 约束构建 | 416.2 / 420.2 µs | **316.3 / 329.4 µs** | **−23%** |
| 迭代扫掠 | 701.2 / 723.3 µs | 681.2 / 699.1 µs | **−3%** |
| 整帧（金字塔 / 球坑） | — | — | **−3.8% / −4.5%** |

**结论一（通用）**：**GS 扫掠是依赖链/延迟受限，不是算力受限**——砍掉每冲量的
一次叉乘 + 一次矩阵乘（≈24 flops + 36 B 加载）只换来 3%。预积因子的收益被
(a) 扫掠的串行依赖链 + (b) 每点 +72 B 的访存一起吃掉了。以后优化扫掠应走
**减少扫掠次数/改善收敛**（shock、排序、歧管内层）或**降访存**，不是削算力。

**结论二（回退理由，保真度门）**：金样 `col45` 末态**决定性**退化——
`45/45 入睡、|v|=0.000、最深穿透 0.000` → `40/45 入睡、|v|=0.051、最深 0.005`
（正是本仓反复斗过的"微抖永不入睡"形态）。同一改动既有 −3.8% 的时序收益、
又让保真度门退档 ⇒ 按"保真度优先于微速度"整项回退，**哈希回到
`0x4dcf5d46…` / `0x1e855f89…` / `0x3a8c778e…` 代**（回退后三哈希逐位复现，
确认回退彻底）。**教训**：微优化必须同时过"时序门 + 保真门"，两条都要贴读数。

（副作用留档：`gold-sample/Cargo.lock` 在本轮被更新，补上液体域阶段新增的
`vxl-phys-fluid` 依赖——锁文件此前是陈旧的，这处更新与本次回退无关。）

## 2026-09-15 · 关节热启动：**做了、量了、回退了**（第三个负结果）

关节 v1 明确记为"无热启动"，链长对迭代数敏感（8 迭代下 40 环链分离 2.04 cm）。
按教科书补上：迭代前预施加上一子步累积冲量（`Joint::p`/`ang`），迭代中只累积增量。

| 场景（600 步最大锚点分离） | 无热启动（基线） | 线性+角向热启动 | 只线性 |
|---|---|---|---|
| 铰链链 40 环（球形） | 0.0204 m | **0.0119 m（−42%）** | 0.0119 m |
| 悬挂塔 40 段（固定） | 0.0253 m | **0.4758 m（×19）** | 0.1656 m |
| 塔的相邻轴同向 cos | **1.0000** | −0.0909 | −0.6711 |
| 塔尾 y（初 3+40×0.62） | 3.289 | 21.729 | 6.610 |

**结论**：球形链确实受益，但**固定关节塔直接崩**（相邻块相对翻转）。机制：角行
**没有姿态偏置**，旧姿态下算出的累积冲量被原样施加到新姿态上 ⇒ 注入能量。接触
通道的热启动之所以成立，是因为它有**特征匹配 + 锚点距离门 + 深度跳变门**三道
守卫；关节要热启动，必须先把这三类守卫补上（角行的姿态偏置 + 位姿突变门）。
在补齐之前**整项回退**（v1 保持无热启动），理由与数字写进 `joints.rs` 模块文档
——省得下一个人再走一遍。

**本轮三个负结果的一致教训**：引擎当前阶段的瓶颈不是"少算了什么"，而是**约束
通道的收敛结构与访存**（扫掠延迟受限、预积因子被访存吃掉、热启动需要位姿门）。
下一批改动的首选顺序：
① **窄相**（占帧 25–28%）——**已定位：盒对裁剪是大头**（见下）；② **关节限位/马达**
（功能残缺：`motor-wheel`/`slider-crank` 两个场景在 vxl 上完全不动，ragdoll 的肘/膝
限位被忽略）；③ 关节热启动（**前置条件**：角行姿态偏置 + 位姿突变门，见上一节）。

### 追加：窄相成本归属（裁剪段占 60% 窄相）＋ 一个计时方法学坑

在盒对路径的 `clip()` 两侧加临时探针（测完回退），金字塔实测：

| 读数 | 值 |
|---|---|
| 窄相总计 | 434 µs/步 |
| 盒对裁剪（含探针开销） | **315 µs/步 = 60% 窄相 ≈ 13% 帧** |
| 裁剪调用 | 1361 次/步 ⇒ ≈232 ns/次（扣掉探针 ≈50 ns ⇒ 真实 ≈180 ns/次） |

⇒ 下一刀落在**盒对裁剪**（Sutherland–Hodgman：参考面 4 平面 × ≤8 顶点 + 4 点归约），
不是 SAT、不是多面体填充。注意裁剪实现为通用/专用路径共用同一份代码，改一处两处都受益。

**计时方法学坑（已修进 `probe::ns`）**：探针原先只有 `us()`，**逐次计时亚微秒区段会
被截断成 0**——第一版读数给出"裁剪 2.7 µs / 1361 次 = 窄相 1%"，与真值差 100×。
亚微秒区段必须用 `probe::ns` 累计（已加并在文档里写明用途）；相位级（ms 量级）
继续用 `us` 即可。

### 追加：裁剪内的第一刀——取模换分支（**噪声级，已回退**）

`clip()` 内层用 `(i + 1) % m` 取环形下一顶点（`m` 是运行期值 ⇒ 每次顶点迭代一次
整数除法，每次裁剪 ≈26 次迭代）。改成 `if i + 1 == m { 0 } else { i + 1 }`（逐位
相同）后交错 A/B（3 轮）：

| 读数 | 旧（HEAD） | 新 | 判定 |
|---|---|---|---|
| 窄相 | 395.5 / 396.3 / 400.5 µs | 397.9 / 381.2 / 388.9 µs | **2 轮更低 1 轮更高 ⇒ 噪声** |
| p50 | 1.531 / 1.710 / 1.537 ms | 1.537 / 1.523 / 1.520 ms | R2 旧版是离群点，剔除后 ≈ 持平 |

⇒ 回退。**纠正的模型**：整数除法不是裁剪的热点（除法可流水化，且内层只有 ~26 次），
削它换不来可测收益——这与"扫掠砍算力不收益"是同一类结论：**该段是访存/依赖受限，
不是指令数受限**。下一轮进裁剪内部必须先用 `probe::ns` 做**逐段**归属
（平面准备 / 内层行走 / 主平面过滤 / `select_contacts` 归约），再决定动哪里；
优先怀疑 `clip_in`/`clip_out` 的 `Vec` 逐点 push/clear（定长数组化是逐位等价的）。

### 追加：裁剪内部计数归属（零开销计数器，已入库）

计时探针在 1361 次/步的调用密度下自伤（6 次时钟 ≈200 µs/步，会淹没 ~250 µs 的被测段），
改用**整数计数器**（`DefaultNarrowPhase::probe_stats`，逐位无影响、哈希未变）：

| 计数（金字塔，每步） | 值 |
|---|---|
| 裁剪调用 | 1361 次 |
| **内层顶点迭代** | **16.9 次/调用**（4 侧平面 × 平均 4.2 顶点） |
| 交点插值 | 4.48 次/调用 |
| 进归约前的候选点 | 4.57 个/调用 |

**成本归属推算**：单次裁剪 ≈232 ns（含探针 ~50 ns）里，内层行走 16.9 × ~5–9 ns
≈ **85–150 ns** 是大头；setup（12 次面法线点乘 + 4 个顶点构造）与
`select_contacts`（≈4.6 点去重排序）占剩下的零头。而内层每次迭代都做
**`Vec::push`**（含容量检查/可能重分配）——每裁剪 ≈21 次 push ⇒ **每步 ~2.9 万次
push**。⇒ 下一步的确定目标：**把 `clip_in`/`clip_out` 换成定长数组 + 计数**
（盒面裁剪多边形上界 8 顶点、插值后 ≤8；通用路径取 16 留余量），**逐位等价**，
只需过 43 套件 + 三哈希，不动金样。

### 追加：定长数组化的**容量上界考证**（动手前必读）

新增"裁剪多边形峰值"计数器（`probe_clip_max`），三场景实测：

| 场景 | 裁剪调用/步 | 裁剪多边形峰值 |
|---|---|---|
| 金字塔 210（全盒） | 1361 | **7 顶点** |
| 球坑 400（全球） | 0（不走裁剪路径） | 0 |
| 三角网 200（盒+网格） | 1 | 6 |

**盒对快路径的上界可证**：入射面 4 顶点，4 次半平面裁剪（凸 m 边形裁一次 ≤ m+1）
⇒ **≤8 顶点**，实测 7 与之一致 ✓ 这一条可以无条件定长 16。

**通用路径没有小常数上界**：圆柱面是 **n 边形**（`polytope.rs` 的 `cylinder_poly(n)`，
n = 细分档）、凸包面顶点数可达数十（`add_hull` 收 ≤256 点）——且**上面三个场景都没
覆盖通用路径的裁剪**（球坑不走、三角网只有 1 次/步）。⇒ 定长数组化有两种安全做法：
(a) 数组取 **64** + 溢出时落回一个小 `Vec`（实际不触发、计数器可证不触发）——
保持逐位等价；(b) 在构建期给面顶点数**设上限**（例如 16）——这会改几何行为、
必须换代哈希并重跑金样。**推荐 (a)**：改动局限在裁剪一处，两条路径仍共用同一份实现
（`polytope.rs` 注释里已记过"两份易漂移的裁剪代码"的教训）。

### 追加：定长数组化**实测反了**（+26% 窄相，已回退）

按上面 (a) 实现：`ClipPoly` 定长 64 + 溢出 `Vec`，为把改动收敛到"只换缓冲类型"，
给它实现与 `Vec` **同形的调用面**（`clear/push/len/is_empty/Index/IntoIterator`）
⇒ 裁剪实现里 18 处引用**一行未改**、零警告编译通过、43 套件全绿、门槛哈希逐位未变
（确认逐位等价）。但交错 A/B 三轮**全部同向变慢**：

| 读数 | 旧（`Vec`） | 新（定长 64 + 溢出） | 变化 |
|---|---|---|---|
| 窄相 | 375.9 / 378.0 / 365.1 µs | 459.0 / 482.8 / 464.3 µs | **+26%** |
| p50 | 1.412 / 1.480 / 1.406 ms | 1.478 / 1.618 / 1.481 ms | **+6.5%** |

⇒ 回退。**推断机制**（未再深挖）：`ClipPoly` 给 `DefaultNarrowPhase` 增了 ~2 KB
内联数组（两处）⇒ 热字段被推远、预取与缓存局部性变差；`Index`/`IntoIterator`
的 `if i < n` 分支与 `Chain` 迭代器也比"切片迭代"更难被优化。**"删掉 push 的开销"
输给了"缓存局部性 + 代码形状"**——与前面几次同类：这一段的瓶颈是访存/依赖，
不是指令数。

**下一步（若要继续动裁剪）**：不要动缓冲的*存储形态*，改从**算法**上减少工作量——
① 先算入射面顶点到参考面 4 个侧平面的距离下界，一次否掉必然全在外侧的平面
（省掉整遍裁剪）；② 参考面/入射面选择与 `select_contacts` 的归约（≈4.6 点）可在
一个融合循环里完成，减少一遍遍历。两者都会改浮点顺序 ⇒ 需换代哈希 + 金样。

### 追加：裁剪"距离表复用"**也反了**（+6.8% 窄相，已回退）——第五次证伪

按上面 ① 方向的第一次尝试：侧平面裁剪里，同一顶点的距离在一个平面内被算了两次
（`da`（顶点 i）与 `db`（顶点 i+1）），改成"每平面先落一张 m 元素距离表再行走"，
**逐位相同**（点乘表达式与顺序未变，只是复用；43 套件 + 门槛哈希逐位未变验证过）。
交错 A/B 三轮**全部同向变慢**：

| 读数 | 旧 | 新（距离表） | 变化 |
|---|---|---|---|
| 窄相 | 365.8 / 376.0 / 385.0 µs | 396.9 / 409.3 / 396.7 µs | **+6.8%** |
| p50 | 1.373 / 1.451 / 1.528 ms | 1.407 / 1.556 / 1.414 ms | ≈ 持平（噪声内） |

⇒ 回退。**推断机制（这次相当可信）**：我以为的"重复计算"**编译器早就消除了**——
`db` 在上一轮就是 `da`，寄存器复用/CSE 天然覆盖；我加的距离表反而把值赶进内存、
并在循环内引入 `m <= CLIP_DIST_MAX` 分支 ⇒ 破坏了寄存器布局。

**五次证伪给出的一致结论（写在这里当拐点）**：
- 裁剪段（也含扫掠段）的**局部微优化已到尽头**——删 push、去取模、省点乘，
  三次都反或噪声；本段唯一成立的优化是**去掉每流形一次堆分配**（−4.1%）。
- 继续在这一层抠指令数**不再有期望收益**；要动就动**结构性量**：
  减少候选对/接触点（窄相）、减少扫掠次数（求解）、或把裁剪整段换成更省的
  歧管生成算法（都属换代哈希 + 金样两道门）。**下一次进这一段之前，
  先问"它减少的是工作量还是指令数"——只做前者。**

## 2026-09-15 · 金样三场景复核（本段全部改动之后）

本段改动（关节族 + 接触点内联数组 + 诊断计数器）之后，用 RECIPES 的固定配方
（`gold-sample/` 独立 workspace，`600 16 0.01 4 3.0 30 4`）把**三个**场景都跑了一遍
——此前本段只跑过 col45，pile5/tower25 从未在最新构建上验证过：

| 场景 | 本段实测（vxl） | 记录基线（OPEN-PROBLEMS P1 段，2026-09-14） | 判定 |
|---|---|---|---|
| col45（45 盒 5 层） | |v| 0.000、**45/45 入睡**、最深 0.000、y∈[0.25,2.25] | T5 行记 col45 入睡 **35/45** | **不差于**（是段内两次尝试的对照读数） |
| pile5（2000 盒） | |v| 0.096、KE 13.0、**1690/2000 入睡**、最深 0.006 | 入睡 **1195 → 1700/2000**（P1 落地后） | **与基线一致**（1690 ≈ 1700，差 10 体） |
| tower25（2500 盒 25 层） | |v| **0.200**、KE 819、0/2500 入睡、最深 0.015、y∈[0.25,12.13] | |v| 0.283 → **0.236**（P1 落地后） | **优于基线**（0.200 < 0.236），塔形完好 |

**结论**：本段的三项改动**没有让金样保真度退化**——pile5 与记录基线一致（1690/2000），
tower25 比基线更好（|v| 0.200 vs 0.236）、塔高区间完好（12.13 ≈ 25 层 × 0.5 − slop）。
tower25 的 **0/2500 入睡**与 col45/pile5 的入睡缺口一致，属 P1 已记录的**已知开口**
（需 Rapier 式整组 staged 语义），非本段引入。

**口径提醒**：基线数字来自 OPEN-PROBLEMS 的 P1 落地段（同配方），本表是**跨时间点**
比对——按本仓纪律，跨时间点只用于"是否明显退化"的粗判（此处差值远小于稳定性档位
差异），精确对照请用同进程交错 A/B。

## 2026-09-15 · 关节马达落地（功能补齐，非性能改动）

PhysArena 的 `motor-wheel`（电机车轮）与 `slider-crank`（曲柄滑块）两个场景此前在
vxl 上**完全不动**（适配器如实标注"电机未实现"）。本轮补上**速度级马达**：

- 引擎：`Joint::with_motor(target_velocity, max_force)`；自由轴（转动 = 绕轴相对角速度、
  棱柱 = 沿轴相对线速度）驱到目标，冲量按 `max_force · dt` 上钳；
  `motor_max_force <= 0` = 关（**默认关 ⇒ 无马达场景逐位等价**，门槛哈希未变复核过）。
- 桥：**新增导出** `vxl_joint_motor(joint, target, max_force)`（索引 = `vxl_joint_add`
  调用序号）——不动既有 arity，老调用方无需改。
- 适配器：建完关节即装马达、去掉"电机未实现"的注释（限位仍如实标注）。

**测试（引擎侧，严苛口径）**：强马达 60 步内把 ω_y 驱到目标 3.0 rad/s（|误差| < 0.1）
且锚点保持 < 0.02 m；**力钳负向用例**——`max_force = 0.05` 时同样步数**达不到**目标
（ω_y < 1.0，防"上钳失效"）；无马达时自由轴零自转（等价旧行为）；棱柱马达在重力反向
作用下仍驱到 −1.0 m/s（|误差| < 0.15）。

**回归**：43 套件 + clippy 全绿；arena 自检 vxl-phys **15/4/0**（0 失败）不变；
门槛/压力/确定性三哈希**未变**（马达不参与门禁场景）；浏览器 `motor-wheel` 场景
不再有"电机未实现"注释、无错误。

**仍未实现**：关节限位（角度/行程）——需要累计相对转角/位移状态（跨帧状态 + 迟滞门
才能稳定），留给下一轮。

## 追加：结构性工作量的两条审计（一条靠读契约关闭，一条留作入口）

1. **宽相候选对是否重复**——**关闭**：`BvhBroadPhase::compute_pairs` 末段
   `pairs.sort_unstable(); pairs.dedup();`（`broad/src/lib.rs:665`）⇒ 同一对不会
   进两次窄相，"重复对白付一遍求解"这条路口不存在。
2. **候选对的产出率**——已量：金字塔 600 候选对产出 570 流形（**95% 有用**）
   ⇒ 也不是"筛太松"。
3. **每流形的点是否选多了**（**唯一剩下的入口**）：求解后统计"法向冲量 ≈ 0"的点
   占比。若占比可观，说明 `select_contacts` 的归约可以更省——**少一个点 =
   每扫掠少一次迭代**（本段唯一被证明有效的方向：减少工作量）。
   仪器化方案（未做完，留给下一轮）：把 `solve_island_group` 的 `detail: &mut [u64; 3]`
   扩到 `[u64; 5]`（+ 总点数、+零冲量点数），在 warm 回写循环里顺带计数，
   `solve()` 归并到 `ImpulseSolver::last_points`；`arena_bench` 打一行。
   注意两处调用点（串行/并行）都要改，别只改一处。

### 追加：零冲量点占比**已测**（22–35%，唯一开着的工作量入口）

按上面的仪器化方案落地（`detail` 5 元 + `ImpulseSolver::last_points` + arena_bench
一行；**只数 `npts` 内的有效点**，padding 槽不算），三场景实测（每子步一次解算）：

| 场景 | 被解算接触点 | 法向冲量 ≈ 0 的点 | 占比 |
|---|---|---|---|
| 金字塔 210 | 2233 | 784 | **35.1%** |
| 砖墙 200 | 1779 | 474 | 26.6% |
| 球坑 400 | 541 | 115 | 21.3% |

⇒ **1/3 左右的接触点在解算后不承载任何法向冲量**，但它们在每个扫掠里都要付
"法向 + 摩擦"两段迭代（金字塔扫掠占帧 ≈50%）。这解释了为什么"减指令数"屡试不中：
**真正的大头是"点在不在"，不是"点算得快不快"**。

**为什么不能直接把 pn≈0 的点删掉**（下一轮动手前必读）：
- pn 是**解算末态**；多点在解算过程中承担过转动约束（4 点面接触是冗余约束，
  少一个点会削弱抗转动刚度）⇒ 直接删会产生"塌角/弹跳"；
- 业界做法（Bullet/Box2D 的 contact reduction）是在**迭代若干轮后**按累计冲量
  淘汰，且带**迟滞门**（下一帧不许立刻复活），并只对"已知是稳定接触"的流形做；
- 本引擎的 warm 回收按 feature 匹配锚点 ⇒ 淘汰点会在下一帧被 feature 重新匹配回来
  （**需要同时处理 warm 侧的淘汰**，否则等于没删）。
⇒ 正确形态是"**参与式降点**"：解算前 N 轮全点参与，之后按累计冲量降点 + 迟滞；
**换代哈希 + 金样两道门**，且要盯 col45 位置容差（T5 口径 12.5 mm）与 tower25 不崩。

### 落地：参与式降点（`point_reduce_after = 3`）——本段**第二个真收益**

实现：`solve_constraint` 从第 N 轮外层扫掠起，跳过"至今零冲量**且 `cfm < 1.0`
（浅缝/软接触）**的点；前 N−1 轮全点参与（4 点冗余约束的抗转刚度在早期建立）；
点保留在 warm 槽表里、`pn/pt1/pt2` 不清零 ⇒ 下一帧被 feature 匹配回来时起点是
历史值，不会"复活式弹跳"。**穿透硬投影（cfm = 1）绝不跳**。判据只依赖本帧已累积
冲量与轮次序号（纯状态函数）⇒ 串并行逐位一致仍成立（T4 复核 4.39× 逐位一致）。

**读数**：

| 门 | 关闭态 | 开启态（第 3 轮起） | 判定 |
|---|---|---|---|
| 金字塔 p50（交错 A/B 三轮） | 1.528 / 1.525 / 1.491 ms | **1.397 / 1.414 / 1.403 ms** | **−7.3%**（三轮同向） |
| 金样 col45 | 45/45 入睡、\|v\| 0.000、最深 0.000 | **完全相同** | ✅（同一判据曾否掉预积角向量改动） |
| 金样 pile5 | 1690/2000 入睡、\|v\| 0.096、KE 13.0 | **1735/2000 入睡**、\|v\| 0.075、KE 9.9 | **更好** |
| 金样 tower25 | \|v\| 0.200、KE 819 | \|v\| 0.224、KE 822 | 略高但都优于记录基线 0.236；塔形完好 |
| 金字塔长跑 3000 步 | 堆顶 19.407、末态动能 1.74 | 堆顶 **19.426**、末态动能 **1.47** | 更稳 |
| 43 套件 + clippy | — | 全绿 | ✅ |
| 浏览器自检（arena） | 15/4/0 | **15/4/0**（0 失败） | ✅ |

**换代哈希**（ADR 0004）：`0x6219d1866b20c002d806d3d699a487ff`（门槛）/
`0x63e5eb35b71b8c84ade4a053aeecb900`（压力）/
`0xd8601988ad7989ffb58ba8b956c2f8db`（确定性，10 轮逐位一致）；
旧世代 `0x4dcf5d46…` / `0x1e855f89…` / `0x3a8c778e…` 仍可用 `point_reduce_after = 0`
逐位复现（回归对照用）。

**为什么这次成、前五次不成**：前五次都在"让每点算得更快"（减指令数），这次是
**让一部分点不必算**（减工作量）——与零冲量点占比 22–35% 的实测完全对得上。
这也是本段唯一被读数支持的优化方向。

### 追加：点降点后**重扫扫掠地板**（4 扫掠仍垮 ⇒ 地板未移动）

**假设**：既然第 3 轮起有 1/3 的点被跳过，每扫掠的**实际工作量**下降 ⇒ 也许
4 扫掠（`velocity_iterations = 2`）现在够用，那能省掉整个扫掠相位的一半。

**实测**：金字塔 +3000 步长跑 ⇒ **垮塌**——堆顶 19.4 → **5.484**、末态清醒
**59/210**（151 体垮塌后入睡）、流形 514 → **120**。⇒ **地板没动**：6 扫掠是
**稳定性**边界（步长/耦合决定），与"每点算多少"无关。回退后逐位复核：
堆顶 19.426、动能 1.4718、514 流形、门槛哈希 `0x6219d186…` 全复现。

**顺带查出的验证覆盖缺口（方法论，值得记住）**：金样配方自带 `16` 迭代参数
（`col45/pile5/tower25 ... 16 0.01 4 ...`）⇒ **开启/关闭默认档的改动，金样读数
不会变**（这次三场景与 6 扫掠档逐字相同）。也就是说：**金样门覆盖的是"16 迭代
那一档"的保真度，默认档（6 扫掠）的稳定性必须由 arena 长跑（`--steps 3000`）
来守**。改默认档时两道门缺一不可，别只看金样。

> **2026-09-20：该缺口已闭**——`crates/vxl-phys/tests/default_tier_stability.rs` 把"默认档
> 长跑"变成门禁链的一部分（`cargo test --release` 即覆盖）：
> - `default_tier_stays_stable_on_long_run`：6×6×6=216 体、3000 步、默认档，冻结读数
>   **top_y 2.7223 / Σv² 2.1488 / awake 216 / manifolds 835**（两次连跑逐位一致）；
>   其中 `awake 216`（全醒）**记的是已知的睡眠缺口，不是目标值**。
> - `gate_is_sensitive_to_sweep_reduction`（金丝雀）：降到 4 扫掠后读数必须明显不同 ⇒ 实测
>   **流形 835 → 514、Σv² 2.1488 → 2.5626、清醒 216 → 192**（**514 流形与本段上面记录的
>   扫掠地板退化读数一致** ⇒ 本场景复现了同一个退化模式，门的灵敏度可信）。
> 更新冻结值 = 有意改动默认档时的规定动作（按 ADR 0004 记录换代理由）。

**结论**：默认档保持 **2 子步 × 3 迭代 × 内层 1（6 扫掠）**；本段两次"减工作量"
的收益（内联数组、点降点）与这次被否的"减扫掠"互不影响。

### 提案（未做，已量化）：检测每步一次、求解每子步一次（对齐 Rapier 的 TGS 结构）

**账（金字塔默认档，实测相位）**：宽相 47 µs + 窄相 382 µs，**当前每个子步各跑一遍**
⇒ 检测类开销 ≈ **858 µs/帧 ≈ 55% 帧**，其中 **429 µs（≈27% 帧）是"同一帧内第二遍
重复检测"**（第一遍在子步 0，第二遍在子步 1，两者只差 v·dt/2 的位移）。

**对照**：Rapier 的 TGS 子步在**一个 step 内复用同一批接触体**（检测每步一次、
求解每子步一次），这正是它同场景 0.98 ms 的结构性来源之一。

**方案**：把 `substep()` 拆成"每步一次"（力场 → 速度积分 → 宽相 → 窄相 → 冲击快照）
与"每子步一次"（速度积分 → 求解 → 位置积分 → CCD），即**宽相/窄相/冲击快照只做一次**；
约束构建（316 µs）仍按子步重建（锚点随姿态变，不能复用）。

**预期收益**：金字塔 ≈ **−430 µs ≈ −27% 帧**（1.55 → ~1.1 ms），且**只减工作量**。
**风险与前置**：接触在子步 1 里是"差 v·dt/2 的旧几何"——用 speculative margin + 软接触
（本引擎两者都有）吸收；但**这是物理改动 ⇒ 换代哈希 + 三道门**：金样 col45/pile5/tower25、
**arena 长跑 `--steps 3000`（默认档唯一有效的门）**、以及 arena 自检 19 探针。

**为什么列在"提案"而不是直接做**：它是**管线结构改动**（facade 的 `step/substep` 拆分 +
CCD/冲击快照的时序归属），需要一整轮预算才能按纪律收口（含两道门的读数与回退备案）。
按本段纪律：**先把"预期收益从哪来"算清（本文），再动手**——这一条已经完成。

### 追加：降点档位标定（2/4/6 vs 3）——**噪声限制，判为分不开**

把 `point_reduce_after` 做成三个独立二进制（2/4/6，另有 0 = 关闭作同窗口基线），
同窗口交错各跑一轮金字塔：

| 变体 | R1 | R2 | R3 | 均值 |
|---|---|---|---|---|
| 0（关） | 1.523 | 1.461 | 1.381 | 1.455 |
| 2 | 1.481 | 1.337 | 1.411 | 1.410 |
| 4 | 1.368 | 1.381 | 1.526 | 1.425 |
| 6 | 1.520 | 1.405 | 1.510 | 1.478 |

**每个变体内部的散布（±5%）大于变体之间的差（≈3%）** ⇒ 单轮单次的口径分不开
2/4/6。**判定：保留 3**（它是唯一做过完整验证的档：三道金样 + 长跑 + 43 套件 +
浏览器自检都过）。**要真正标定这个旋钮，得把每格改成"每轮多跑取中位数"**
（同窗口、每变体 ≥3 次），否则再测多少次都只是读噪声——这条留给下一轮，
别用单轮读数做档位决策。

## 睡眠缺口的三个判别实验（2026-09-18）：判据差**否**、偏置速度**否**、子步分辨率**是**

**背景**：真实度线唯一未过的开口 = 大堆/塔不入睡（tower25 0/2500、pile5 1735/2000、
金字塔 210/210）。开工前先核两件事：① 交接档列的"下一步入口"是否仍开（见文末
"陈旧入口"）；② Rapier 的睡眠判据到底是什么（本仓此前的表述有误）。

### A. Rapier 睡眠判据（逐行核 rapier3d 0.35.3 源码）——**不是阈值差**

`RigidBodyActivation`（`rigid_body_components.rs:1295-1365`）：

| 字段 | 默认 | 语义（源码注释原文） |
|---|---|---|
| `normalized_linear_threshold` | **0.05** | 与体的**最远点速度** `|linvel| + |angvel|·max_extent` 比较（*不是* `|linvel|`）；**并且**与最远点**位移率**比较（"solver position corrections count as motion too"，`sleep_prev_pose`） |
| `angular_threshold` | 0.5 | **对有碰撞体的体，角速度被折进上面那条点速度判据，本字段只对无碰撞体的体生效** |
| `time_until_sleep` | 0.5 s | — |

⇒ 两条结论：
1. **社区/搜索得到的"linear 0.4"是错的**（那次读到的是被 `length_unit` 缩放的旧文档口径）；源码是 0.05。本仓 SPEC §4.11 的 0.04/0.05 与之相比：线性略严 25%，**角速度实为更松**（我们 0.05 rad/s 硬判；Rapier 折算后有效上界 ≈0.115 rad/s）——且 Rapier 还**多**一条位移率判据。
2. **对齐阈值不是解药**：塔的残余是 |v|≈0.2、|ω|≤0.5 ⇒ Rapier 点速度 = 0.092+0.5×0.433 ≈ **0.31 ≫ 0.05** ⇒ **Rapier 的判据同样不会让它睡**。差距是真实运动差，不是判据差。

**旁读（阈值分解，tower25 @600）**：仅线性超阈 448 | 仅角速超阈 326 | 双超 1405 |
**阈值下 321**（共 2500）。那 321 个体已同时满足两条入睡阈值却醒着——因为本仓是
**岛级原子入睡**（整塔一个岛，一处动则全岛不睡）；Rapier 的
`IslandManager::commit_sleeping_chunks` 允许把**岛内子块**入睡。
**记账不动**：它到不了验收（tower25 要 2500/2500），且与本仓"原子入睡排除反复唤醒"
的设计取舍直接冲突（SPEC §3 判据就是反复唤醒率）。

### B. 双引擎对拍读数（金样同场景同 tick，本机）

| tick | vxl \|v\|max | vxl KE(J) | rapier \|v\|max | rapier KE(J) | rapier 入睡 |
|---|---|---|---|---|---|
| 50 | 0.330 | 1127 | **0.046** | 65.7 | 0 |
| 100 | 0.213 | 871 | 0.000 | 0.0 | 2500 |
| 600 | 0.224 | 822 | 0.000 | 0.0 | 2500 |

两个此前没记过的读数：
- **Rapier 的「最深」从 tick 50 到 600 恒为 0.025**（穿透是**静态**的、不衰减），
  我们的是 0.013–0.017 且持续波动 ⇒ 两条路线的取舍不同（它接受静态穿透换安静）。
- **Rapier 残余 0.046 ≈ 1.1 × `g·sub_dt`**（= 9.81/240 = 0.041）；我们的典型蠕动
  0.073 ≈ 1.8 × 同量 ⇒ 残余与"每子步重力增量"同阶是个可用线索（见 D）。

### C. 证伪：「残余 = 去穿透偏置速度（`erp_inv_dt·pen`）」

**动机**：早前探针记 `erp·depth/dt ≈ 0.24 m/s`（1 cm 穿透 @120 Hz），与塔的 |v|max
0.22 同量；而我们是**单趟**解算（偏置混在 rhs，`lib.rs:1039` 的
`bias = (erp_inv_dt*pen).min(max_corr)`），**最终速度里带着**这个去穿透目标速度，
Rapier 则有"末趟无偏置"（`remove_bias_from_rhs`）。看似完美解释。

**判别实验（零代码：`max_corrective_velocity` 就是那个钳位）**。注意：**只往下扫才有
信息**——交接档记的 0.5/3.0/10.0 全在偏置速度之上，三档逐位相同（从未生效）。

tower25 @300 tick（`tower25 300 16 0.01 4 <maxcorr> 30 4`）：

| maxcorr | \|v\|max | KE | 最深 | 判定 |
|---|---|---|---|---|
| 3.0 / 0.5 / 0.2 | 0.305 | 905 | 0.017 | **三档逐位相同**（钳位未生效） |
| 0.1 | 0.254 | 837 | 0.014 | 变了，**无改善** |
| 0.05 | 0.272 | 958 | **0.049** | 穿透涨 3×，残余不降 |
| 0.02 | **1.076** | 2007 | 0.087 | 塔被搞垮 |

⇒ **证伪**：把去穿透偏置钳到 0.02 也没让残余趋近 0.02，反而先毁掉去穿透。残余不是
这个偏置速度。

### D. 正向发现：残余由**子步分辨率**决定，不是扫掠数

tower25，同配方只改 `substeps`（iters 16 / inner 4 / skin 0.01 / freq 30）：

| substeps | \|v\|max @100 | @200 | @400 | KE @200 | 最深 | 阈值下（可睡体） |
|---|---|---|---|---|---|---|
| 4（现行） | 0.213 | 0.268 | ~0.29 | 857 | 0.013–0.017 | 294 |
| **8** | **0.132** | 0.166 | 0.162 | **282** | **0.009** | **1112** |
| **16** | **0.090** | 0.117 | — | **101** | 0.007–0.008 | — |

**等扫掠数对照**（都 512 扫掠 = 基线 256 的两倍，只换放大哪条轴；@400 tick）：

| 配置 | \|v\|max | KE | 阈值下（可睡体） | 判定 |
|---|---|---|---|---|
| 基线：iters16 × sub4（256 扫掠） | ~0.29 | ~900 | 294 | — |
| 加倍**迭代**：iters32 × sub4（512） | 0.232 | 533 | 709 | 有效 |
| 加倍**子步**：iters16 × sub8（512） | **0.162** | **301** | **1112** | **更有效（1.57×）** |

⇒ **两条轴都有效，子步轴的杠杆更大**（同工作量下多约 1.6× 的可睡体）。机理读数：
残余的主项是"每子步扰动的余量"（重力增量 `g·sub_dt` + 重新线性化的间断），随
`sub_dt` 缩小；增大迭代只能压低"同一线性化内的残差"，压不到这一项。
这与 DESIGN §7 缺陷①（"子步每遍 ≈ inner 的 4 倍价值"）同向，但那条讲的是 inner，
本表是 iters——两者都改时别混淆。

**可睡体数随分辨率的完整走向**（这是本轮最有用的一个数）：256 扫掠 **294** →
512（迭代轴）**709** → 512（子步轴）**1112** → 1024（子步轴）**2080/2500（83%）**。
⇒ 塔不是"整体沸腾"，是"**少数尾部体**卡住全岛"（岛级原子入睡）。

**判定与落地取舍**：
- 立即可用的**保真档**：金样配方加 `substeps=8`（或 16）⇒ T5 速度列与可睡体数显著
  改善（成本 2×/4× 求解时间；塔场景从 206 → ~400/800 ms/tick）。
  **默认档不换**（8B 侧 4 子步已实测 607 vs 330 ms，8 子步会到 ~2.5–3×，不可接受）。
- 外推：|v|max 按 4/8/16 子步为 0.29/0.16/0.09（每倍 ≈1.6×/1.47×，递减）⇒ 要到
  0.04 阈值需再翻 2–3 档（~64–128 子步、16–32× 成本）⇒ **不是可行解**，只是把缺口
  量化成了"分辨率 vs 工作量"的兑换率。

### E. 结论（本轮对"为什么塔不睡"的回答）

睡眠缺口 = **真实残余蠕动的差距**（点速度：我们 ~0.31 / Rapier ~0.05，**6×**），
不是判据差（A）、不是去穿透偏置速度（C）：
- 残余对偏置钳制不敏感（C），对**子步分辨率**敏感（D）；
- 本轮又排除三项（阈值对齐、偏置钳位、岛内子块入睡的价值），并把
  「Rapier 每子步只要 2 遍（1 带偏置 + 1 无偏置）⇒ ~g·sub_dt 余量」与
  「我们 64 遍 ⇒ 6× 大的余量」并列摆出 ⇒ 与 §6/§7 的"稳定性/工作量比"缺口是**同一件事
  的两面**（性能面 9–10×、保真面 6×）。

**下一轮入口（按价值）**：1 已做并落档（见下节 F）；其余未试。

### F. Rapier 式「带偏置趟 → 位置积分 → 无偏置趟」落地与实测（2026-09-18 续）

**实现**（`PhysConfig::stabilization_iterations`，**默认 0 = 关闭 ⇒ 三哈希逐位不变**）：
- `ImpulseSolver::solve_unbiased` 复用 solve 全链（分岛/warm/并行/建约束），但：
  `max_corr = 0`（法向只留 speculative ＝ Rapier `rhs_wo_bias = max(dist,0)·inv_dt`）、
  迭代数取 `stabilization_iterations`、`normal_inner = 1`、`shock = 0`、
  **跳过休眠判定**（同子步已判过；重复判定会让 `sleep_timer` 双倍累积 ⇒ 入睡提前）。
- `World::substep` 在**位置积分之后**调用（顺序＝Rapier：带偏置趟 → 积分 → 无偏置趟）：
  去穿透仍由带偏置趟的速度积分兑现，无偏置趟只把修正速度从**最终速度**里移除。
- **切向漂移回拉保留**：对齐 Rapier——它的切向 `rhs_wo_bias = solver_contact.tangent_velocity`
  （材料点漂移率），即在无偏置趟仍生效。第一版把它一并归零，**125 体场景 |ω| 0.13→0.45 退化**；
  保留后回到 0.21（该场景的承重件正是漂移回拉，M1 第十一段已证）。
- 关节不重复求解（本件只动接触通道）；restitution 在无偏置趟仍会重算（e=0 场景无影响，记录在案）。

**tower25 单变量扫描**（300 tick；16 迭代 × 内层 4 × 4 子步）：

| stab | \|v\|max | KE | 最深 | 阈值下（可睡体） |
|---|---|---|---|---|
| 0（基线） | 0.305 | 905 | 0.017 | 294 |
| 1 | 0.252 | 627 | 0.011 | 241 |
| 2 | 0.186 | 598 | 0.009 | 347 |
| 4 | 0.192 | **538** | 0.011 | **412** |

**与子步叠加（增益可乘）**：`sub8 + stab2` ⇒ |v|max **0.119**、KE **174**、
阈值下 **1411**（对照：基线 294、sub8 单独 1112）⇒ 两件互补。
（下表与「叠加/不能替代」两段的首批读数取自**首版实现**＝漂移一并归零；
最终实现（漂移保留）在 tower25 上两者差 ≤2%——`stab2 @300` 首版 0.186/598、
最终 0.195/605；差异只在 125 体场景显著，见下。）

**不能替代偏置趟的迭代数（重要负结果）**：`iters=4/inner=1 + stab2`（6 扫掠）⇒ **崩**
（|v|max 2.31、KE 11321）；`iters=8/inner=1 + stab4`（12 扫掠）⇒ 退步（0.253/1036/194）。
⇒ 「少扫掠 + 末趟」**不能**换工作：本仓的"稳定性/工作量比"缺口不因这一件改变。

**金样三场景（600 tick / 16 / 0.01 / 内层 4 / 4 子步；stab=2 = 最终实现）**：

| 场景 | 基线 | stab=2 | 判定 |
|---|---|---|---|
| col45 | 45/45 入睡、KE 0 | **45/45、KE 0** | 中性 ✓（未踩历史退档坑） |
| pile5 | 1735/2000、KE ~10 | **1960/2000、KE 1.6**、最深 0.005 | **大改善**（+225 体，T5 速度列 0.088→0.054） |
| tower25 | 0/2500、KE 905、最深 0.017 | 0/2500、**KE 650**、最深 **0.009** | 改善但不入睡（|v| 0.305→0.191 @300） |

**T1 主靶 125 体（`diag_min 5 5 0.52 16 0.5 300 box 8 0.52 0.2 0 4 0.02 <stab>`）**：

| | \|v\| | \|ω\| | 末态 KE | 单 tick dE 峰值 |
|---|---|---|---|---|
| stab=0 | 0.078 | 0.132 | **1.6** | 4.0 |
| stab=2 | 0.116 | 0.212 | **18.1** | 3.5 |

⇒ **该场景退化**（末态动能 11×）。

**判定（与 `DRIFT_BIAS_SCALE = 0` 同类处置）**：本件是**场景依赖的取舍**，不是普适改善——
堆（pile5）大改善、塔（tower25）中等改善、col45 中性、**留缝角点（125 体）退化**。
⇒ **默认关闭**（默认档与三哈希逐位不变），按需开启（`stabilization_iterations ≥ 2`
配 `substeps=8` 是当前最佳保真档）；本件同时是 DESIGN §5/§9/§10 之后**第一个在
保真方向上被实测为真的结构件**，可作为后续结构重做的既有拼图。

**单行变体「末趟改刚性 cfm」（Rapier 的 `cfm_factor = 1.0`）——已试，不采纳**：
`force_rigid_cfm`（末趟整趟刚性）实测——

| 场景 | 软 cfm（保留） | 刚性 cfm | 判定 |
|---|---|---|---|
| tower25 @300 | \|v\| 0.195、KE 605 | 0.264、KE 685 | 略差 |
| pile5 @600 | 1935/2000 入睡、KE 2.0 | 1930/2000、KE 2.3 | 持平 |
| 125 体 | \|v\| 0.116、\|ω\| 0.212、KE 18.1、单 tick dE 峰值 3.5 | \|v\| 0.122、**\|ω\| 0.081**、KE 18.1、**dE 1.7** | 角向与 dE 更好，KE 退化不变 |

⇒ 刚性档只在 125 体场景的角向/dE 上更好，堆/塔持平或略差，**KE 退化与 cfm 无关**
⇒ 不采纳（保住"软接触限制迭代增益"的金样定标结论），代码已回退（`git diff` 空）。
**125 体 KE 退化的真因仍未定位**——这是本件下一个该查的点（候选：清理趟让
"偏移后的速度"参与摩擦目标，或让 speculative 额度被消费两次）。

### G. 「摩擦不看求解器造出来的速度」（TECH-SURVEY 的 A1）——两种读法都实测否证

**动机（外部双源）**：Unity DOTS 的启发式（*friction is solved using input velocities
from before the solver — eliminates intermediate velocities causing unwanted friction
impulses*）与 Rapier `friction_in_bias_pass` 的注释（*friction reacting to bias
velocities **pumps their coherent lean mode until they topple***）是同一原理的两种实现；
本仓塔的症状（整柱共振、塔顶 |ω| 0.5）与它逐字对得上，且本仓解算源码里早就写着
"摩擦模式被泵成蠕动 = 塔蠕动的根因"（`solve_constraint` 摩擦段注释，引自金样源码）。
⇒ 在 TECH-SURVEY §6 里被排为第一位。

**读法一（Rapier 结构）：带偏置趟只解法向，摩擦挪到无偏置末趟**（`friction_in_bias_pass: false`）

| 配置（tower25 @300） | \|v\|max | KE | 可睡体 |
|---|---|---|---|
| stab=2、摩擦在带偏置趟（对照） | 0.195 | 605 | 342 |
| stab=2、摩擦只在小偏置趟 | **5.622** | **154 037** | 0 |
| stab=1、同上 | 12.299 | 2 397 086 | 9 |
| stab=4、同上 | 7.884 | 292 323 | 0 |

⇒ **灾难性**：塔身完好（y 带 12.13–12.21）但动能 ×255–4000。
**机理**：本仓带偏置趟有 16 迭代 × 内层 4 = **64 遍**扫掠，位置又是由带偏置趟的速度
积分的 —— 摩擦缺席的窗口太长，法向冲量经有效质量矩阵串扰到切向的速度无人回收。
Rapier 能这么做是因为它的带偏置趟**只有 1 遍**⇒ 又一件"整组语义不可单件搬"
（与 §5 趟间积分、§9 检测上提、§10 流形复用同族）。

**读法二（Unity 结构）：摩擦留在带偏置趟，输入速度改用"本点法向冲量施加前"**
（`friction_pre_impulse_velocity: true`）

| 场景 | pre=0 | pre=1 |
|---|---|---|
| tower25 stab=2 | 0.195 / KE 605 / 最深 0.011 / 可睡体 342 | 0.280 / 595 / **0.009** / 310 |
| tower25 stab=0 | 0.305 / 905 / 0.017 / 294 | 0.199 / 898 / **0.014** / 261 |
| 125 体 stab=0 | \|v\| 0.078、\|ω\| 0.132、**KE 1.6** | \|v\| 0.238、\|ω\| 0.151、**KE 22.0（×14）** |
| 125 体 stab=2 | \|v\| 0.116、\|ω\| 0.212、KE 18.1、dE 3.5 | \|v\| 0.082、\|ω\| 0.152、KE 13.2、dE 5.9 |

⇒ 塔上是 **wash**（|v| 在 ±30% 噪声内、KE 持平、穿透略好、可睡体略差）；
**125 体在默认档退化 ×14** ⇒ 否决。

**判定**：两种读法都**回退**（源码与 `a4103d0` 逐位一致、三哈希不变），读数留档；
A1 从 `TECH-SURVEY.md` §6 的第一位移入 §7 护栏。
**残留未试**：Rapier 之所以成立可能靠"warm 启动的累加摩擦冲量每子步重施加 +
带偏置趟只有 1 遍"的**组合**——真要走这条线得整组换挡（1 带偏置 + 1 无偏置），
不是单件可拼。

**顺带修掉一个自己引入的小回归**：`a4103d0` 把金样 harness 的 `dump` 参数挤到了
第 10 位，而 `INDEX.md` 里 `render_compare.py` 的配方按第 9 位传 dump ⇒ 会静默不产出
转储。现已恢复 `dump` 在第 9 位（`-` ＝ 不转储，给后面的实验旋钮留位），
`stabilization` 排到第 10 位。

### H. A2（岛内染色 + 大岛切批）——**先算账，账算下来不值得做**（2026-09-18 续）

**第一步：算"预期收益从哪来"**（本仓纪律；`arena_bench` 相位 + 求解细分，同机同窗口）：

| 相位 / 求解细分 | pyramid | wall |
|---|---|---|
| 宽相 | 3% | 4% |
| 窄相 | 21% | 24% |
| 求解 | **76%** | **72%** |
| └ 约束构建（占帧） | 35%（**27%**） | 32%（**23%**） |
| └ 热启动预施加 | 3% | 3% |
| └ **迭代扫掠（占帧）** | 61%（**46%**） | 65%（**47%**） |

⇒ 岛内**可并行面** = 扫掠 46% + 构建 27% ≈ **73%** ⇒ 8 线程理想 **2.76×**
（现实扣掉屏障/负载不均衡约 2–2.3×）。**只对"大岛"场景有效**（8B 是 11267 个小岛，
已经并行，A2 对它 0 收益）。

**第二步：廉价判别——只重排、不并行，隔离"序"这一个变量**（贪心染色：同色不共享动体，
同 Box2D `b2ConstraintGraph`）。**扫掠数与其它一切都不变**：

| 读数 | 现行"约束索引序" | 染色序 |
|---|---|---|
| pyramid 末态动能 Σv² | 1.1064 | **2.5349（×2.3）** |
| pyramid p50 | 1.205 ms | 1.266 ms |
| pyramid 热启动预施加 | 25.8 µs | **87.4 µs（×3.4）** |
| 125 体 末态 KE | 1.6 | **13.1（×8）** |

⇒ **同样扫掠数下收敛退化**：染色序把载荷沿竖直链的传播变成"每色扫掠只传一层"；
另加一项隐性代价——按色分组打散了约束序的**空间局部性**（相邻约束不再相邻 ⇒ 热启动
的 `group_apply` 变成散点访存，+61 µs ≈ +5% 帧）。

**第三步（决定性障碍）：3D 装箱的色数天然细碎**（临时诊断打印）：

| 岛 | 约束数 | 色数 |
|---|---|---|
| pyramid | 590 | **20** |
| 125 体（5×5×5 留缝） | 325 → 380 | **25** |

原因：Box2D 文档里的"5050 体金字塔 **8 色**"是 **2D** 的（2D 盒最多 6 邻居）；
3D 盒有角/棱接触，内部盒邻居数最多 **26** ⇒ 色数上界 ≈27。每色只剩 15–30 条约束，
8 线程下每线程 2–4 条 ⇒ 屏障与调度开销吃掉并行收益（且本仓 `ScopedPool` 是**按相位
spawn** 的，做不了微秒级屏障——真要做还得先有常驻任务系统）。

**判定**：A2 **不做**（账面 2.76× × 收敛退化 × 色数三重否决）。A2 从 `TECH-SURVEY.md`
§6 移入 §7 护栏；§9 增补"**外部配方还要核维度**"（2D 染色配方不可搬 3D）。
**残留（若日后仍想吃大岛并行）**：需要**保序**的并行分解——Jolt `LargeIslandSplitter`
的分批不要求"同批互不共享体"，只要求批内可并行 + 批间有序；要用它得先读其源码确认
确定性来源，并配一个能做微秒级屏障的常驻任务系统。

**探针已全部回退**（源码与 `10db544` 逐位一致、三哈希不变；不留诊断打印与死开关）。

### I. A3（冷热分离：逆质量 / 世界逆惯量按组紧凑）——**落地：位不变 + 9/9 轮同向 −4.3%**

**动机与形状**（Box2D `b2BodySim`/`b2BodyState` 的冷热分离）：本仓对照后发现**热扫掠路径
只剩两处全局散点读**——`group_apply` 每点每扫掠调用 2 次，各读一次 `bodies.inv_mass[i]`
（4B）与 `inv_i_world[i]`（**36B Mat3**）；后者是**全局数组**（36B × 全体数：8B 档 = 7.2 MB），
而求解环按随机全局索引访问它。

**与既有否证的区别（关键）**：本仓此前的"体侧缓存"否证（§7-6/P4）是**把 16B 四元数读换成
36B 矩阵读**（读得更多 ⇒ +11%）；S24 是点自身字段重排（结论：瓶颈在 bodies 侧）。
A3 的形状相反：**把同样的值搬到按组局部索引的紧凑数组**（读得更近更少），且是纯搬运。

**实现**：`group_iw: Vec<Vec<Mat3>>` + `group_im: Vec<Vec<f32>>`（组 → 组内局部索引）；
gather 时与速度一起推送；两者以切片进入各组的求解闭包；`group_apply` 改读 `iw[q]`/`im[q]`，
**顺带不再需要 `bodies` 参数**——`solve_constraint` 签名里的 `bodies` 也随之可删
（编译器警告确认：整条热扫掠路径已不再触达全局 `BodySet`）；删除全局 `inv_i_world` 字段。

**判定**：

| 判据 | 读数 |
|---|---|
| **位一致**（核心主张：纯搬运） | 三哈希**逐位不变**（`0x6219d186…` / `0x63e5eb35…` / `0xd8601988…`）+ 43 套件 |
| **时序**（交错 A/B：只读 worktree + 独立 target，9 轮交替） | pyramid：旧均 **1.104 ms** / 新均 **1.056 ms** ⇒ **−4.3%，9/9 轮同向**（符号检验 p ≈ 0.2%） |
| 8B 解算相（3 轮交替） | 旧均 49.49 ms / 新均 49.52 ms ⇒ **0%（±4% 噪声内，无收益）** |
| 内存 | 去掉全局 36B × n 数组（8B 档 **7.2 MB**） |

⇒ **落地**。对 8B 无收益（该场景组内体本来就连续、工作集不紧张），对金字塔档 −4.3%。
**附带的结构收益**：求解热路径与全局 `BodySet` 解耦——这是后续任何"岛内并行/便宜子步"
方向的前置之一。

### J. 接触点裁量（A5 / DESIGN 阶段 2）——**前提被否证：点数是承重的，不是冗余**

**动机（来自本仓自己的注释）**：解算器里写着"4 点面接触是冗余约束 + 强转动耦合，
单向 GS 16 次迭代后残留可观（重堆实测微抖永不入睡）" ⇒ 看起来**冗余点在制造残余**；
而 DESIGN 阶段 2（接触聚类裁点）只算过工作量收益（点数 −30~40%）。本探针**同时量
残余与工作量**：把窄相 `select_contacts` 的截断上限从 4 做成旋钮扫 3/2
（选点规则不变＝按深度序取前 N 个非重复点）。

| 读数 | cap=4（现行） | cap=3 | cap=2 |
|---|---|---|---|
| pyramid 接触点（点/流形） | 2280（3.93） | **1657（2.90）** | 860（2.00） |
| pyramid p50 | 1.03–1.07 ms | **0.863 ms（−19%）** | **0.641 ms（−38%）** |
| pyramid 扫掠 | 488.8 µs | 372.8 µs | — |
| pyramid 末态动能 Σv² | 1.1064 | **1.9936（+80%）** | **188.7（×170）** |
| pyramid 堆顶 y | 19.405 | 19.374 | 19.142 |
| 125 体 末态 KE | **1.6** | **38.3（×24）** | **658（×411）** |
| 125 体 最深穿透 | 0.007 | 0.027（×3.9） | 0.081 |
| 125 体 单 tick dE 峰值 | 4.0 | 23.6 | 1691（有体已落到 y=0.662＝堆在垮） |

⇒ 两条结论：
1. **"冗余点制造残余"被否证**：点越少残余越大（单调），说明那 4 个点是**承重件**
   （承载分布 + 抗转动），不是可裁的冗余；
2. **A5 的收益/代价比很差**：−19% 工作量换 125 体 KE ×24、穿透 ×3.9（cap=3）；
   cap=2 直接推向垮塌。**A5 从 TECH-SURVEY §6 移入 §7 护栏**。

**边界（诚实说明）**：本探针用的是"取最深 N 点"，而 Rapier 式聚类是**保极值点 + 保空间
铺展**的选点——两者不等价。所以严格地说：*"少点 = 更差"已证*，*"更聪明的少点"未证*；
若日后仍想做 A5，必须先给出一套比"按深度取前 N"更聪明的选点规则，并过金样三场景
（本探针已给出代价量级：质量对点数极敏感）。

**探针已回退**（源码与 `6bf917d` 逐位一致、三哈希不变）。

### K. **流形特征稳定性实测**——§9/§10 的归因过强，失败机理需重测（2026-09-20）

**为什么查这个**：§9（P1：检测上提、后续子步复用流形 ⇒ 塔崩 |v| 44.4）与 §10（复用约束
＋ prepare，全变体崩）把失败归因于"**本引擎流形是裁剪产物 ⇒ 材料点配对跨帧漂移**"，
并据此宣告"廉价子步不成立"。但读窄相源码发现：盒-盒裁剪**本来就是特征稳定的**——
裁剪交点用 `feat_intersect(入射棱对, 参考侧平面)`（"同三元组重现同 ID"），入射面顶点用
`feature = side_bit | 顶点序号`，设计注释明写"跨帧稳定 ⇒ warm 缓存可续接"。⇒ **该归因
缺实测支撑**，而它守着本仓最值钱的结构杠杆（检测每步一次 = 文档算过 −27% 帧）。

**做法（landed 诊断，零行为影响）**：在 `build_constraint` 的 warm 匹配三个分支上加
atomic 计数（精确特征 / 近邻回退 / 未匹配），`warm_match_stats_take()` 读并清零，
`arena_bench` 打印。**三哈希逐位不变**（只计数、不改行为）。

**读数（pyramid，按运行长度看累计 + 增量窗口）**：

| 运行/窗口 | 精确特征命中率 |
|---|---|
| pyramid `--steps 200`（含沉降） | 79.1% |
| pyramid `--steps 600` | 81.4% |
| pyramid `--steps 3000` | 84.1% |
| **pyramid 增量窗口 200→600（沉降后）** | **83.9%** |
| **pyramid 增量窗口 600→3000（稳态）** | **85.1%** |
| wall（默认步数） | 76.0%（回退 4.4%、未匹配 19.6%） |
| **tower25 @300 tick / stab=2** | **69.6%（回退 18.7%、未匹配 11.7%）** |

⇒ **结论是"场景相关"，不是"归因错误"**（本节的初稿只看了 pyramid，措辞下重了，此处按塔的
读数修正）：

1. **平面大堆（pyramid）稳态 85.1% 精确命中** ⇒ 在那种场景上，§9/§10 的"裁剪产物 ⇒
   配对漂移"归因确实过强（把 15% 未命中当成了不可靠的全部）；
2. **塔只有 69.6% 精确，非精确匹配达 30%**（其中 **18.7% 走的是回退分支**＝"特征未命中
   但 1×skin 内有候选"）⇒ 在塔上**该归因成立**，而塔正是 §9/§10 失败的那个场景。
3. 回退分支占比在塔里异常高（18.7% vs pyramid 3.0%）说明：塔的 `feature` 变了但**同一个
   材料点还在附近**——形态上像"参考面/裁剪序变化导致特征 ID 变化"，而不是材料点真的换了。

**新问题（下一轮入口，比"直接重开 P1"更小更该先做）**：塔那 30% 为什么失配？细分手段：
按门分类计数（法向 0.95 门 / 距离门 `match_dist` / 深度跳变门 `FB_DEPTH_JUMP` / 无上一帧
流形），并加"参考面是否翻转"计数（`ref_base` 变化 ⇒ 侧平面索引 k 变 ⇒ `feat_intersect`
的输入三元组变 ⇒ ID 变）。**定位后再判**：若失配主要来自参考面翻转，那"检测每步一次"
的可行性取决于**参考面选择的跨帧稳定性**（可加迟滞/稳定选择），而不是流形语义本身。

#### K.1 成因拆解（同轮追加）：**78% 是"面选择变化"，但它解释不了 §9 的崩**

做法：特征 ID 是位域（bit31 = 入射侧 A/B、bit30 = 裁剪交点 vs 顶点、其余 = 几何哈希），
把"回退命中"的那对特征异或后按位域拆开 ⇒ 直接读出变了哪一种（`feature_kind` /
`feature_hash_part`，诊断、三哈希不变）。

| 场景（占该场景的**回退命中**） | 侧别翻转 | 裁剪路变 | **哈希变** | 特征相同 |
|---|---|---|---|---|
| tower25 | 21.5% | 22.7% | **55.9%** | 0.0% |
| pyramid（--steps 600） | 37.6% | 14.9% | **47.2%** | 0.4% |

⇒ 塔的失配里 **78%（= 侧别 21.5% + 哈希 55.9%）是"接触仍在、但面选择变了"**
（参考面在 A/B 间跳，或盒内参考面索引/入射面变）；22.7% 是"裁剪交点 ↔ 入射面顶点"互换。
"特征相同"≈0 说明这些回退**从不因顺序/距离门误入**（都是真的换了编码）。

**⚠️ 这条读数的边界（推翻本节的初读）**：以上都是**跨帧** churn（每帧重检测时面选择会变）。
而 §9 的 P1 在**一个 tick 内复用同一张流形表** ⇒ 特征 ID 逐子步完全相同 ⇒ warm 匹配是
**精确命中**、暖启动完整。⇒ **特征 churn 解释不了 §9 的崩溃**，本节初稿提出的
"参考面翻转导致复用不稳"**被削弱**；反而加强了候选机理①（**开局自由落体的检测冻结**：
塔前 ~100 tick 在落体，一 tick 下坠 2–5 cm，后 3 个子步用接触前流形 ⇒ 深穿透无阻挡）。

**仍然有信息量的那一半**：在**常规路径**（每子步重检测）上，塔每个子步有 30% 的点拿不到
精确暖启动（18.7% 走收紧的回退、11.7% 完全没匹配）。收紧回退是历史上**故意**的
（放宽到 4×skin 会错配锚点 ⇒ 暖冲量过驱动 ⇒ 堆永不入睡），所以这里没有"放宽就好"的余地；
**剩下待查的是那 11.7% 完全未匹配的拒因**（三个回退门的哪一个 + "无上一帧流形"），
这才是"塔的暖启动为什么比平面堆差"的最后一环。

**原提案（保留，K.1 之后成为唯一候选）**：§9 的 P1 若重开，配"运动自适应检测节奏"
（准静态每 tick 一次、`max|v|·dt > skin` 时加测）——动机是塔开局自由落体（2 cm 缝，
一 tick 下坠 2–5 cm）时"子步 0 检测、后续复用"会让后 3 个子步用**接触前**的流形
⇒ 深穿透无阻挡 ⇒ 巨额修正。**这条仍未验证**（K.1 已把"参考面翻转"那个竞争候选削弱：
tick 内复用流形时特征 ID 不变）。

### L. §9 的 P1 重测（检测每步一次）——崩可复现，但**崩因不是 warm 匹配**；运动自适应给出权衡曲线

**动机（先查档，再动手）**：`git show 00a1ebf --stat` 显示那次 P1 实验**只改了两份文档**
（`DESIGN-staged-solver.md` +40、`EXPERIMENTS.md` +1）——**实现从未入档**（工作区做完即回退），
所以「塔崩 |v| 44.4 ⇒ 廉价子步不成立」这条结论**事后无法复核**；而本会话已两次修正
§9/§10 的归因（末节 K / K.1），且其后落地了三件相关能力：`build_constraint` 每子步从
**当前位姿**重算深度/锚点、warm 表改稠密槽位、A3 按组紧凑（热路径与 `BodySet` 解耦）。
⇒ 用一个开关（`detect_once_per_tick`，默认关）重做。

**重测（tower25，300 tick，substeps=4；k 开 = 非首子步跳过宽相+窄相、复用本 tick 流形表）**：

| 配置 | \|v\|max | KE | y 带 | 最深 | warm 精确率 |
|---|---|---|---|---|---|
| 基线（每子步检测） | 0.305 | 905 | [0.25,12.15] | 0.017 | 69.6% |
| **检测每步一次（无自适应）** | **14.619** | **299 134** | [0.24,12.18] | 0.140 | **92.1%** |
| 同上 + stab=2 | 13.755 | 568 250 | [−0.04,12.20] | 0.117 | **96.6%** |

⇒ ① **崩可复现**（与 §9 记录的量级一致）；② **但 warm 精确率反而升到 92.1%（96.6% @stab=2）**
——与 K.1 的预测一致（tick 内复用流形 ⇒ 特征 ID 逐子步相同 ⇒ 精确命中）⇒ **"配对/暖启动"
被彻底排除为崩因**。剩下的只能来自**接触集/几何在运动中冻结**：盒子以 ~0.6 m/s 逼近时
一整个 tick 不被阻挡，接触只能在下个 tick 首次出现 ⇒ 平滑接触变成硬碰撞（沉降期 KE ×300）。

**运动自适应版**（复用仅在准静态：`max|v|·dt ≤ 0.5·skin`）：

| 判据 | 基线 | 运动自适应 |
|---|---|---|
| tower25 KE（stab=0 / stab=2） | 905 / 605 | 1928（×2.1）/ 1046（×1.7） |
| tower25 y 带 | [0.25,12.15] | [0.25,12.15] ✓ **不崩** |
| tower25 可睡体（stab=2） | 342 | 237（−31%） |
| 125 体 末态 KE | 1.6 | **0.5（好 3 倍）** |
| pyramid 宽相 + 窄相 | 295 µs | **149 µs** |
| pyramid 求解 | 918 µs | 899 µs（≈持平） |
| pyramid p50 | ~1.03–1.07 ms | **1.049 ms**（检测类 −146 µs ⇒ 约 −14% 帧） |
| pyramid 末态动能 Σv² | 1.1064 | 1.3537（+22%） |

⇒ **权衡档，不是默认档**：拿到「检测减半」的 −14% 帧，代价是塔的 KE ×1.7–2.1、可睡体 −31%。
与 `stabilization_iterations` 同类处置：**默认关、按需开、消融留档**。
**哈希世代**：开启即换代（门槛 `0xb8d4bb4e0c71a1a5c54d6b2fdc2e4bfd`、压力
`0x94983ea167e4ef261646a24e943c8c4f`；确定性 `0xd8601988…` **未变**——该示例场景未进入
复用路径，原因未核）。关闭时与基线三哈希**逐位一致**。

**三条诚实结论**：
1. §9 的"崩"可复现，但它的**归因**（陈旧配对 / 暖启动）**被实测排除**——本仓文档里
   "陈旧流形 + 位姿推进 = 吃错几何"的说法需要改成"**接触集在运动中冻结**"；
2. 复用的真实代价落在**慢动/沉降期**：`0.5·skin` 阈值仍放行了塔的蠕动（|v|≈0.2–0.3）⇒
   复用照常发生。要同时拿 perf 与塔的保真，需要一个更细的自适应判据（按**每对**的接近
   速度，或按接触的 speculative 余量），或先解决"陈旧接触集"的语义（＝§9/§10 想做的 P2 方向）；
3. **方法论**：否证实验的**实现不入档**会让结论无法复核——本会话两次修正 §9/§10 的归因
   都撞上这一点。建议今后否证实验至少把补丁留成 `git stash`/分支或写进 EXPERIMENTS 的
   附录（一行 `git diff` 即可）。

### M. 「检测每步一次」的崩因坐实＝**检测视野不足**（skin 扫描，零代码判别）

**假设（承接 L）**：复用流形时真正缺的是"**逼近中的接触**"——盒子以 v 逼近，`v·dt_tick`
那段距离必须在本 tick 内被拦住，但窄相只在 `sep ≤ skin` 时才产流形。
**判别（纯配置、零代码）**：加大 skin ⇒ 视野覆盖 `v·dt`（0.6 m/s ⇒ 10 mm @60 Hz）。

| tower25 @300，检测每步一次 | \|v\|max | KE | y 带 | 最深 |
|---|---|---|---|---|
| skin=0.01（原） | 14.619 | 299 134 | [0.24,12.18] | 0.140 |
| skin=0.02 | 1.959 | **2 386（÷125）** | [0.25,12.15] ✓ | 0.016 |
| skin=0.04 | 0.598 | 1 992 | [0.25,12.18] ✓ | 0.018 |
| skin=0.08 | **0.321** | 1 271 | [0.25,12.19] ✓ | 0.025 |

⇒ **假设成立**：加大视野即可让"检测每步一次"在塔上**不崩**；`skin=0.08` 时 |v|max 0.321
与基线 0.305 同量级（KE 仍 1.4×，即复用仍留有代价）。
⇒ **2026-09-14 的"塔崩 ⇒ 检测冻结不可行"应改写**为「**检测视野与检测节奏不匹配**」：
视野 10 mm = `v·dt` 10 mm（0.6 m/s 逼近）⇒ 逼近段完全没被覆盖。
⇒ 顺带解释了历史上一处表面矛盾（"skin 0.02 在**每子步检测**下更差"）：**视野的最优值
取决于检测节奏**——每子步检测时 `dt_detect` 小、小视野最优；每 tick 检测时视野必须覆盖
`v·dt_tick`。

**下一步设计（未实现，机制已由本表背书）**：视野不该是常数，应为
`skin + max(0, v_接近)·dt_detect`（**速度充气视野**）：
- 每子步检测 ⇒ `dt_detect` 小 ⇒ 视野自动小 ⇒ 保持基线的塔最优（skin 0.01）；
- 每 tick 检测 ⇒ 视野自动放大 ⇒ 不漏逼近接触；
- **不需要额外机制**：本仓求解器已有 spec 项（`sep·inv_dt`）负责"允许多快闭合"，
  视野放大后的接触会被平滑拦停（这正是 skin=0.02/0.04/0.08 三档不崩且越大约稳的原因）。
实现面：`narrow.collide` 需多传 `dt`（`bodies` 已在手，相对速度可读）；判据
`d <= skin` → `d <= skin + max(0, −v_rel·n)·dt`。判定 = 塔三场景 + 125 体 + 三哈希换代 +
arena 长跑 `--steps 3000`（默认档唯一有效的门）。

### N. 速度充气视野落地：把「检测每步一次」的崩**修掉了**（skin 保持 0.01）

**实现**（`DefaultNarrowPhase::set_predict_dt`，由 `World` 每子步设为**检测间隔**）：
窄相接受判据从 `sep ≤ skin` 放宽为 `sep ≤ skin + max(0, v_接近)·predict_dt`
（`v_接近` 取质心相对速度在法向的投影；角项忽略，贴面接触上是一阶小量）。
两处 SAT 预筛（盒对 + 多面体对）与 `clip` 的逐点过滤共用本对的充气量（scratch 字段）。
- **复用流形时** `predict_dt = dt_tick`（视野覆盖到下一次检测）；
- **每子步检测时恒传 0** ⇒ 现行行为**逐位不变**（三哈希已验证）；
- CCD 逐采样显式清零（它在时间维上已走路径，不叠加预测）。

**判定（tower25 @300，skin 保持 0.01）**：

| 配置 | \|v\|max | KE | y 带 |
|---|---|---|---|
| 基线（每子步检测） | 0.305 | 905 | [0.25,12.15] |
| 检测每步一次（**无**充气） | 14.619 | **299 134** | [0.24,12.18] |
| **检测每步一次 + 充气视野** | **0.279** | **1 078** | [0.25,12.14] ✓ |
| 同上 + stab=2 | 0.379 | 1 023 | [0.25,12.18] ✓ |

⇒ **崩被修掉**：同是"检测每步一次"，KE 从 30 万降到 **1 078**（÷280）。且比"整体加大 skin"
更有效（skin=0.02/0.04/0.08 三档的 KE 是 2 386/1 992/1 271）——符合设计意图：
**只在真正逼近处放大视野**，而不是全局抬高接受门限（后者会改稳态行为）。

**其它场景（同开关开启）**：

| 判据 | 基线 | 检测每步一次 + 充气 |
|---|---|---|
| pyramid p50 | 1.03–1.07 ms | **0.982 ms** |
| pyramid 宽相 + 窄相 | 295 µs | **155 µs** |
| pyramid 求解 | 918 µs | 850 µs（早退更早触发：匹配更稳 ⇒ 收敛更快） |
| pyramid 末态动能 Σv² | 1.1064 | 1.3537（+22%） |
| 125 体 末态 KE / \|ω\| | 1.6 / 0.132 | **0.5（好 3 倍）/ 0.052** |

**判定**：机制成立、代价从"崩"降到"塔 KE ×1.2–1.7"，**但仍不是默认档**（塔是 T1 验收
主靶，KE 仍高 20–70%）⇒ 与 `stabilization_iterations` 同类处置：**默认关、按需开、留档**。
`detect_once_per_tick` 保持 `false`（关闭态三哈希逐位一致）。
**意义**：DESIGN §9/§10 的"廉价子步不可行"至此**被三层修正**——① 归因（配对/暖启动）被
实测排除（末节 L）；② 机理是检测视野不足（末节 M）；③ **修复落地后崩消失**（本节）。
⇒ 「检测每步一次」从"否证"变回"**有代价的可行档**"，剩下的差距是塔那 20–70% 的 KE——
候选收窄到"陈旧接触集"的最后一环（spec 额度每子步重发 / 接触点的世界位置陈旧）。

### O. 复用模式「放宽回退匹配半径」——否证（余价不来自未匹配点的冻结几何）

**假设（承接 N 的余价）**：复用流形时那 3–8% 未匹配点的接触几何被**冻结在世界点上**
（`clip` 产出的世界点逐子步被重新锚定、不随体移动）⇒ 力臂/深度在错位置求值，是塔
KE ×1.2–1.7 的来源。而"回退半径收紧到 1×skin"是为**跨帧重检测**调的（防面选择 churn
造成错配，K.1），tick 内复用无此 churn（K.1 实测精确 92–96.6%）⇒ 放宽应当安全且有效。

**做法**：`ImpulseSolver::set_loose_warm(reuse)`（只在复用模式生效；关闭时逐位不变），
回退接受半径 `match_dist·0.25` → `match_dist`（单变量）。

| 配置（复用 + 充气视野） | 回退收紧（现行） | **回退放宽** |
|---|---|---|
| tower25 stab=0 KE | 1 078 | **1 451（更差）** |
| tower25 stab=0 \|v\|max | 0.279 | 0.376 |
| tower25 stab=2 KE | 1 023 | 1 193 |
| pyramid p50 | **0.982 ms** | 1.101 ms |
| pyramid 末态动能 Σv² | 1.3537 | **1.0220（比基线 1.1064 还好）** |
| 125 体 KE | 0.5 | 0.5 |

⇒ **假设不成立**：放宽回退使塔更差（KE +35%）而金字塔的时序也变差（放宽 ⇒ 更多锚点被继承
⇒ 早退更晚触发？或匹配分支更贵）——净收益为负 ⇒ **不采纳，代码已回退**（工作区与
`20919d7` 逐位一致）。
⇒ 复用余价的来源**仍未定位**；剩下的候选：① spec 额度在复用下的发放语义（本仓 `depth`
每子步从锚点重算 ⇒ 理论上自洽，未实测）；② 冻结的**点集**本身（少点/多点）在迭代中的
累积效应；③ 复用与无偏置末趟（stab）的交互（stab=2 时余价从 ×1.2 升到 ×1.7）。

### P. 两个档的交叉矩阵（tower25 @300，iters=16 / inner=4 / skin=0.01）——两个预期都被推翻

| 检测每步一次 | substeps | stab | \|v\|max | KE | 可睡体 | **ms/tick** |
|---|---|---|---|---|---|---|
| 关 | 4 | 0 | 0.305 | 905 | 294 | **104.5** |
| 关 | 4 | 2 | 0.195 | 605 | 342 | 118.4 |
| 关 | 8 | 0 | 0.157 | 282 | 1112 | 219.5 |
| **关** | **8** | **2** | **0.121** | **178.7** | **1286（51%）** | 225.5 |
| 开 | 4 | 0 | 0.279 | 1078 | 259 | 104.8 |
| 开 | 4 | 2 | 0.379 | 1023 | 238 | 114.9 |
| 开 | 8 | 0 | 0.287 | 696 | 511 | 221.2 |
| 开 | 8 | 2 | 0.408 | 1375 | 456 | 218.0 |

**结论 1（推翻"detect-once 可转默认档"的预期）**：在塔上它**任何子步数都不省时**——
`sub4: 104.8 vs 104.5`、`sub8: 221.2 vs 219.5 ms`（检测省下的被**求解相的增长**吃掉：
匹配更稳 ⇒ hmm，或约束集不同 ⇒ 早退更晚）。**它的 −14% 只出现在 pyramid 那种低子步数
（默认档 = 2 子步）的场景**（前一节的 0.982 ms）。⇒ 它是**基准场景的赢、不是塔/保真档的赢**。
**结论 2（两档互斥）**：开 detect-once 后保真在每个组合都变差，且**在保真档上最差**
（sub8+stab2：KE 178.7 → **1375**、可睡体 1286 → 456）⇒ 想拿塔的保真就**必须关掉它**。
**结论 3（保真档定标，可直接用）**：`substeps=8 + stab=2`（检测关）= **KE 178.7、可睡体
1286/2500（51%）、|v|max 0.121、成本 2.16×**（vs 基线 104.5 ms/tick）；子步轴贡献
最有效（+110% 成本换 KE ÷3.2），stab 只 +13% 成本却再降 KE ÷1.6 ⇒ **两者都开才划算**。

### Q. 两个指定任务同时收口：复用余价「不重要」、A6「前提不成立」（2026-09-20 续）

#### Q.1 复用余价的机理——两个候选都否证，且它本来就不影响决策

新落地解算记账（`solve_accounting_take`：点总数 / 分离点数 / `spec` 和 / `bias` 和；
零行为影响）。tower25 @300（substeps=4、stab=0）：

| 记账 | 检测关（基线） | 检测开（复用+充气） |
|---|---|---|
| 点总数 | 1529 万 | 1534 万（+0.3%） |
| 分离点占比（吃 spec） | 24.9% | 25.3% |
| **spec 和** | 5 805 069 | 5 853 291（+0.8%） |
| **bias 和** | 65 500 | 61 694（**−5.8%**） |
| warm 精确率 | 69.6% | **76.1%** |
| 末态 KE | 905 | 1 078（+19%） |

⇒ ① **"复用让去穿透/闭合额度被重复发放"否证**（三个量都在 ±1% 内，bias 反而更低）；
② "复用让暖启动变差"此前已否证（精确率反而更高）；③ 末节 P 已证**复用根本不省塔的时间**
（221.2 vs 219.5 ms）⇒ **这 +19% KE 既不来自配额也不来自匹配，且不影响任何决策**
⇒ **任务关闭**（余价成因仍未定，但已确认不值得再追）。

#### Q.2 A6 参考面稳定性——**参考面几乎从不翻转**，前提不成立

新落地参考面代理（`warm_normal_flip_take`：匹配点里"接触法向变了"的占比；法向变 ⇒ 多半
换了参考面，法向不变 ⇒ 同面内换了裁剪侧平面/入射棱对）。

tower25 @300 的回退命中构成：

| | 检测关（基线） | 检测开（复用） |
|---|---|---|
| 侧别翻转（A/B 角色） | 21.5% | 21.1% |
| 裁剪路变（裁剪点 ↔ 顶点） | 22.7% | 22.4% |
| 哈希变（同面、换裁剪侧平面/棱对） | 55.9% | 56.5% |
| **接触法向变化 ⇒ 换参考面** | **0.1%** | **0.3%** |

⇒ **参考面在两种模式下都几乎从不翻转（0.1–0.3%）**。churn 是**编码层**的：同一对面、
同一几何，只是「谁当入射侧 / 用哪个裁剪侧平面 / 交点是裁剪产物还是顶点」的编码变了。
⇒ **A6 的原设计（给参考面选择加迟滞）修不了什么 ⇒ 关闭**。若仍要动，方向是**规范化编码**
（按固定规则定角色与侧平面序），但**收益不明**：现有回退（半径刻意收紧到 1×skin）本就是
"锚点错配"的保守路径，把 fallback 转成 exact 等于放宽接受半径，而历史上"放宽接受"实测伤
pile 家族（M1 第十一段）。
⇒ 判定：**A6 关闭**（前提不成立 + 收益不明）。

**方法论**：Q.1/Q.2 两条都是上一轮交接档推荐的入口（A6 那条我甚至写成"已收窄为参考面
稳定性"）——**量了才发现两条都站不住**。这是本会话第四次"预期被自己的实测推翻"。

### R. 胶囊体浏览器穿地：机制定位到"静止时向下漂移"（2026-09-20 续）

**发现路径（工具优先）**：接线后自检 `shape-capsule` 报穿地（15/4/0 → 15/3/1）⇒ 回退接线
⇒ **新增桥的原生驱动** `physarena/wasm-bridge/examples/capsule_probe.rs`（同一份 `add_body`/
`vxl_step` 代码，本机可跑）⇒ **桥在原生下复现** ⇒ 与引擎侧测试（`capsule_drop.rs`，正常）
对照后二分到**唯一差异：桥调 `vxl_body_material`，引擎测试没调**。

**二分（原生驱动，`MU` / `E` 环境变量）**：

| 材质 | 末态 y | 判定 |
|---|---|---|
| 不设材质（`NO_MATERIAL=1`） | **0.695** ✓ | 正常（= half_height + radius） |
| μ=0.7、**e=0** | **0.695** ✓ | 正常 |
| μ=0.5、**e=0.05** | **−12.739** ✗ | 穿地 |
| μ=0.0、**e=0.05** | **−12.350** ✗ | 穿地 |

⇒ **触发条件是恢复系数 `e > 0`**（与摩擦无关）。

**机制（轨迹读数）**：e>0 时胶囊**先正确落定在 y≈0.69**，然后**以越来越快的速度下沉**
（t=75→165: 0.573 → 0.059 → −0.281 → −1.233 → −2.799 → −4.977 → −7.769，**增量逐次变大**
= 正反馈）。而 e=0 时它在 **t=90 就入睡了**（`sleeping=1`）——**睡眠把它冻结在 0.695，
掩盖了下沉**。⇒ 真实缺陷是：**静止的胶囊获得一个"向下、随穿透加深而变大"的漂移**
（即某个偏置的方向反了），e>0 只是让残余抖动使它**保持清醒**从而暴露出来。

**为什么窄相单测没抓到**：两个单测只验**窄相输出**（法线 a→b、点数、深度符号 ✓ 都对），
而缺陷只在**过求解器之后**显现 ⇒ 需要"窄相 + 求解 + 积分"级的判据——`capsule_drop.rs`
（引擎侧）与 `capsule_probe.rs`（桥侧）正是这个层级，但前者用了默认材质（e=0）⇒ 也漏了。
**教训**：`e>0` 是**暴露器**（保持清醒），不是病因；单测的材质要跟真实场景一致。

**下一步（入口已就绪）**：用 `capsule_probe.rs`（一条命令、确定性）调试——
① 先确认**偏置方向**：打印该接触的 `depth`/法线/`rhs`（加临时探针）；
② 怀疑面：胶囊 arm 里 `depth`/`point` 与求解器约定的接缝（窄相自测过的那几项**不足以**
证明求解器看到的约定一致）；③ 修好后 `capsule_drop.rs` 的材质要改成 μ=0.7/e=0.05
（与 arena 探针一致）——否则回归门留着同一个盲区。

#### R.1 根因锁定：**EPA 跑在"光滑支撑"上不适定**；修法＝圆角内核化（2026-09-20 续）

**判据（把接触细节打出来）**：引擎级重现（`capsule_drop.rs`，μ=0.7/e=0.05）逐 tick 打印
法线与**带符号**深度，落地瞬间的读数：

| tick | y | vy | 流形 | 法线 | 深度 max |
|---|---|---|---|---|---|
| 57 | 0.6977 | +0.477 | 1 | `[0, 1, 0]` ✓ | 0.0063 ✓（正确落定） |
| 58–60 | 0.70 | ≈0 | 1 | `[0,1,0]` ✓ | −0.001…−0.007 ✓（贴面，正常） |
| **64** | 0.7148 | +0.065 | 1 | **`[0.707, 0, 0.707]`** ✗ | **42.6712** ✗ |
| **66 / 69** | 0.710 / 0.699 | −0.26 / −0.12 | 1 | **`[0.707, 0, 0.707]`** ✗ | **42.57 / 42.41** ✗ |

⇒ ① **落定本身是对的**（t=57 深度 0.0063、法线向上 ✓）；② 病态出现在**贴着接触的临界
时刻**：法线退化成 **45° 对角**（= 大盒的**角点方向**）、深度虚高到 **42 m**（≈ 30·√2）。

**为什么是 EPA（两个独立证据）**：
1. 42 这个量级不是任何深度，而是**支撑平面在角点上的投影**（大盒半长 30 ⇒ 对角投影 30√2）；
2. 我把该路径改成"用 EPA 的深度"后，**窄相单测反而挂了一个** ⇒ 临界接触一旦交给 EPA
   就不可靠；两条实验性改动（见证点平面、EPA 深度）**已回退**（窄相 2 单测恢复全绿）。

**根因（原理）**：`gjk::epa` 的多面体展开假定**支撑体有多面体面片**；而我的
`CapsuleSupport` 是**光滑曲面**（支撑点随方向连续滑动、没有平面片）⇒ 对这类支撑 EPA
**不适定**（面法线/终止判据都退化）。**这是"圆角形状直接喂 EPA"的经典陷阱。**

**修法（已定方向，未实现）＝圆角内核化（rounded-shape core decomposition）**：
让 GJK 跑**线段内核**（不含量半径），得到"线段 ↔ 对方"的**距离与见证点**（良态、纯多面体）；
再把半径**解析加回**：接触判据 `d_seg < radius + skin`、压入量 `radius − d_seg`、
逐端点深度用**见证点平面**（`n·p_other`，注意**不能**用对方的支撑平面——大盒 + 微倾法线
会给出数十米的偏移，这正是本节的 42）。⇒ EPA 只在"线段**进入**对方"时才会被调用（罕见且
良态）。**归一化约定仍是 a→b，且必须用两体中心定号**（穿透时见证点会换序）。

**当前状态**：窄相 2 单测全绿（回退后）；`capsule_drop.rs` 仍是 **ignored 重现**
（失败模式：落定正确但随后正反馈下沉）；浏览器接线**未恢复**（arena 保持 15/4/0）。
本节与 R 合起来构成下一轮"一条命令开工"的完整入口：修 `capsule_reach`（内核化）→ 解禁
`capsule_drop.rs` → 重接线 → 自检应 16/3/0。

## R.2 内核化**两次实现都失败**——并牵出更根本的 GJK 缺陷（2026-09-20）

按 R.1 的方向动手，两次尝试都留下**硬读数**，且都指向同一个根因：

**(a) 零半径线段芯**（新增 `gjk::SegmentSupport`，不含量半径）——用例
`capsule_vs_box_floor_vertical`（盒地板顶面 y=0；胶囊下端点在 y=0.29、真压入 1 cm）实测：

    [DBG core-sep] d=7.077  p_a=(0, 0.29, 0)  p_b=(5, 0, 5)  n=(-0.7065, +0.0410, -0.7065)

真值应为 `d=0.29 / p_b=(0, 0, 0)`（顶面）。返回的 `p_b` 是**盒角**，`d` 正是到该角点的距离
（‖(5,0,5) − (0,0.29,0)‖ = 7.077 ✓ 自洽）⇒ 法线成了 45° 对角。

**(b) 微量半径芯**（`radius = 1e-3` 的 `CapsuleSupport`，本意是避开退化）——**同样失败**，
读数只在小数末位变化（`0.706517` vs `0.70651287`）⇒ 证明问题**不在芯的退化**。

**根因（本节真正的发现，范围超出胶囊）**：本仓 `gjk::gjk` 在**对方支撑出现"面平局"**时
（盒顶面对 `dir = ±Y` 的支撑：四个角点并列）会把支撑点解到**同一个角点**，随后的迭代
"无进展 ⇒ 提前退出"，返回**角点见证 + 虚高距离**。圆角胶囊平时看着正常，是因为光滑支撑恒有
**唯一**支撑点，把平局掩盖了；一旦换成任何"点状/线段状"支撑（零半径与微量半径都一样），
平局立刻显形 ⇒ **"收成芯再跑 GJK"这条修法整体否证**（两次实现、两条读数）。

**由此定下的下一步（方向已换，锚点已找到）**：**不要在相交支里用 GJK/EPA 定法线**，改走本仓
**已有的解析最近点**——`sphere_convex_ab`（`lib.rs:1157`）用
`closest_point_on_poly(&self.poly_b, center)` 配 `inside` / `max_plane_d_of` 处理内部情形，
**没有 GJK、没有 EPA、没有平局**。胶囊接触按其分解：对中心线取最近点（端点起步，再
`p ← 段上最近点(q)` / `q ← poly 上最近点(p)` 迭代 2–4 次，每步只复用 `closest_point_on_poly`），
半径解析加回；逐端点深度沿用现有 `radius − n·(s − p_other)`——该式只用到 `n·p_other`＝**平面
常数**，不依赖见证点的切向位置（这正是 (a)(b) 里 `p_b` 取到角点却仍能算对深度的原因）。
顺手可加一道廉栅：EPA 荒谬深度闸（`!depth.is_finite() || depth > 1e3·radius ⇒ None`），
宁可当帧无接触，也别灌入 42 m 压入量把刚体打穿地面。

**状态**：改动**已全部回退**，窄相 **30/30 绿**；`capsule_drop.rs` 仍 ignored；arena 15/4/0。
R.1 的"EPA 不适定"结论**继续成立**（42 m 假深度 + 45° 假法线），但 R.1 给出的修法**已否证**。

> R.1 的"下一步＝内核化 GJK"**已由本节否证**：开工请走 R.2 定的**解析最近点**路线。

## R.3 解析最近点落地：胶囊穿地缺陷关闭，arena 15/4/0 → 16/3/0（2026-09-20）

按 R.2 的路线实现并全链验证（本条是**结论**，实现细节留在代码注释里）：

**改动两处**
1. 新增 `capsule_axis_reach`（`lib.rs`，紧邻 `sphere_convex_ab`）：中心线取最近点——迭代
   `q ← closest_point_on_poly(p)`、`p ← 段上最近点(q)`（≤4 轮；点到**凸**集的距离沿线段是
   凸函数 ⇒ 驻点即全局最近点对），再把采样点交给球的解析路径拿对方**表面点**。
2. 两条胶囊臂：对方是**多面体**（`poly_for` 命中）走上面的解析路；否则（胶囊/球/高度场）
   退回原 GJK 路。压入量公式两路统一改为**见证点平面** `plane = n·p_other`。

**为什么"支撑平面"必须换掉**（R.1 里那个 42 的真正出口）：`n·support(对方, ∓n)` 对**光滑**体
（球/胶囊）恰好等于表面点投影 ⇒ 精确；对**多面体**取的是**最远点**投影——大盒配微倾法线就是
±30 量级的偏移。而 `n·p_other`（真表面点）对两类都精确 ⇒ 一条公式通吃。

**判据读数（全部可复现）**
- 窄相 `cargo test --release -p vxl-phys-narrow`：**30/30**（含 `capsule_vs_box_floor_vertical`
  与 `capsule_flat_gives_two_points` 两单测）；
- `capsule_drop.rs` **解除 ignore** 转常开回归门：静置段 `y=0.7000` **恒定**（= 半高 0.4 +
  半径 0.3）、法线精确 `(0,1,0)`、`vy=0`；接触建立于 t=57（深度 6.3 mm）、6 帧内收敛；
- 浏览器 arena 自检：`vxl-phys 15/4/0 → 16/3/0`（**降级 4→3**：胶囊探针不再走
  `capsule→凸包` 回退）；9 引擎矩阵**失败项 0**。

**接线**（都在 `physarena` 仓）：`src/engines/vxl.ts` 能力表加 `capsule` + 桥声明
`vxl_add_capsule` + 分发 case + 其体积式（质量可比性）；桥 `vxl_add_capsule` 本就是导出 ✓。

**方法论留档**（三轮踩坑的共同教训）：**"光滑外形直接喂 EPA"与"退化支撑喂 GJK"是同一枚
硬币的两面**——EPA 要面片、GJK 要非退化；胶囊的线段芯两边都不满足。稳妥解法是**别让窄相
走这两条泛型路**，改用形状自身的解析量（球/胶囊各有最近的解析例程）。

## 2026-09-21 · 提供者口径 / 复用 / T4 三刀（**六条否证** + 一条口径自纠）

同日叙事见 `SESSION-2026-09-21-CONTRACT-AND-GATES.md`；结论表在 `OPEN-PROBLEMS.md` P5/P6/T4。

| # | 假设 | 方法/结果 | 裁决 | 依据 |
|---|---|---|---|---|
| C1 | 盒在三角网上"沉降 0.28 m"是窄相缺陷 | 带接触复核：接触点 y 1.57–1.77、深度 −0.006…−0.0006（**在带内、正确**）⇒ 是拿 `h(体中心)` 当参考（斜面上不成立） | **证伪**（口径错误） | P5 ⑧ |
| C2 | 起伏网盒体**悬空 0.094 m** 是真悬空 | Rust 侧逐字同场景轨迹：300 tick 落在**下坡翻滚**里（间隙 ±0.5 m 摆、\|v\| 1–4 m/s），t≈225 才安静；换 **900 tick** 窗口同一 arena 探针**直接通过** | **证伪**（瞬态，窗口太短） | P5 ⑰ |
| C3 | 球"埋在地表下 1.09 m" = 幽灵支撑自锁 | 独立仪器（探针**自己暴力枚举全部三角形**算 `clearance = d_min − r`）：粗网 12 例全 **−0.009…−0.012**（贴着）、平网 **−0.0000** ⇒ 球只是**滚走了 17 m**，旧口径拿**投放点**的 `h` 比 | **证伪**（参考点错位；⑪/⑫/⑭ 作废） | P5 ⑱ |
| C4 | T4 成本在 warm 索引的哈希查找 | 零依赖手写 splitmix 快哈希（只改表布局、从不遍历 ⇒ 行为逐位不变，哈希核过）：约束构建均值 **161009 → 166621 µs**（+3.5%，±10% 噪声内） | **回退**（无收益） | T4 第一刀 |
| C5 | "每体旋转矩阵缓存"能省构建成本 | **估算即否证**（未写码）：稠密场景 2×571=1142 次 `from_quat` vs 缓存后 210 次 ⇒ 省 ~930×15–30 ns = **1.5–3%**（噪声内）；稀疏场景（0.33 流形/体）反而多算 | **不做** | T4 第二刀 |
| C6 | `detect_once_per_tick`（tick 内复用流形）值得重开 | 一次性 A/B（翻 true 量完即翻回）：**双输**——默认档 Σv² **+32%**、金样 pile5 入睡 **1994/2000** 且**更慢**（5.93→**7.67 ms/tick**，活跃 600/600 vs 373/600）、T4 扩展比 2.38→2.36（无变化） | **保持 false**（不是取舍，是双输） | `vxl-phys/src/lib.rs` 该函数注 |

**口径自纠（非实验，但同样留痕）**：我曾把"碎片雨 95 ns/流形"与"金字塔 ~1558 ns/流形"并列
暗示差 16×——核对后两者**口径不同**（前者 = `detail[0]` **仅约束构建**且是并发下的**累计和**；
后者 = **整个求解相位** ÷ 流形数）⇒ **不可比，已撤回**。可信的只有"构建占解算 97%、迭代 3%"
（同源同口径；另有 `IslandDiag` 的**窗口均值 ns/流形、ns/点**作为唯一规范指标）。

**方法论（本会话三次栽在同一件事上）**：凡"体在错误高度/位置"的读数，**必须**用**与位置无关**
的判据复核——`clearance = d_min − r`、**全身采样**最小间隙（**角点判据在曲面上不成立**）、
**接触集自身**的 depth、**离场轨迹**（越过边界的上一 tick 速度）；且报"不复现"之前先核
**命令行逐字一致**（漏传 `--steps` 曾让我把 P5 误判成"不可复现"，实测差 45×）。
**另一条**：`probe::start/us` 自身 ≈25 ns/次 ⇒ 调用密度 >1000 次/步的区段**不能**加计时探针
（每流形预算 95 ns，仪器会主导测量），只能上零开销计数器或做**不改动力学**的端到端 A/B。
