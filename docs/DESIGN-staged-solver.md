# 设计：求解器结构性重做（staged 语义 / 聚类 / SoA-SIMD / 染色）

> 状态：**阶段 1 已实测否证**（见 §5）；阶段 3/4 未开工。入口 = OPEN-PROBLEMS P3
> （10× 缺口）+ P1（睡眠侧）。数据来源：M1-PLAN §11（口径 10×）、§12（成本拆解）、
> §7（SIMD 上界）、ADR 0002/0006。
> 纪律：每阶段**独立可验证、可回退**；判定沿用「门禁链 + 四哈希 + 金样三场景 +
> 125 复现 + m0_gates 末态全睡」。

## 0. 为什么（当前事实，全部实测）

| 事实 | 数值 | 来源 |
|---|---|---|
| SPEC §3 最低通过 | 10 万动态+10 万静态、简单碰撞、**30 FPS（整 tick ≤33.3ms）** | SPEC §3 |
| 8B 实测（本机 8 线程） | 总均 **330ms** ⇒ 差 **9.9×**；iters=4 仍 178ms | M1-PLAN §11 |
| 解算占比 | **89%**（岛组 259ms + warm 表 33.5ms + 建岛 2.9ms） | §12 |
| 岛组内 | **迭代环 95%**（245.9ms/组）、建约束 5%、收集 0.3% | §12 |
| 降迭代 | **不可行**：iters=8 塔 KE 32.8 万 J、iters=4 塔崩 | §11 |
| SIMD 岛批量 | 已证伪（−3%）；同岛 4-lane 上界 ≤10%；SoA 单岛 3/4 利用率 | §7-7/§7-8 |
| 睡眠侧 | 匹配线已关闭（全拒也不睡）⇒ 缺的是**求解器形态** | §9、第十二/十三段 |

⇒ 结论：**只剩结构重做**。目标不是「再挤 10%」，而是「换个成本结构」：
用 staged 语义把「需要的迭代次数」从 16 降下来（同时解睡眠），
用聚类把「点数」降下来，再用 SoA+SIMD 压每点成本。

## 1. Rapier 语义对照（逐项核过 vendored 0.35.3；引自第十二/十三段结论）

| 件 | Rapier | 本引擎现状 |
|---|---|---|
| biased/unbiased 双趟 | 每迭代：先带偏置解、再不带偏置解 | ❌ 单趟（bias 混在 rhs） |
| 趟间位置积分 | 迭代间按 `dt/iters` 积分位置 | ❌ **位置在解算中不变**（§12 实证） |
| live 分离刷新 | 迭代中重算分离 | ❌ rhs 每帧烘焙一次 |
| 颜色分组 | 约束图染色 ⇒ 同色并行 | ❌ 仅岛级并行（README 列的 M1 项） |
| 聚类 | 接触点聚类裁点 | ❌ 平均 2.52 点/流形全保留 |
| 软接触 erp/cfm | 同 | ✅ 已逐式对齐（T1 计划修正） |

## 2. 分阶段计划（每阶段独立判定；不过即回退，停在上一个可用状态）

**阶段 0：解算域引入「位置可写」**（不改行为）
- 内容：岛组解算增加位置 scratch（gather/scatter 与速度同构），积分量恒 0，
  跑通管线 + 四哈希逐位不变（纯搬运）。
- 判定：四哈希不变 + 41 测试绿。**这是把「趟间位置积分」变可能的最小前置**。

**阶段 1：趟间位置积分（单一变量）**
- 内容：主迭代循环按 `dt/iters` 在每迭代末积分位置，并刷新各点 bias
  （用锚点分离 `depth0 + sep·n` 实时重算；warm 收集顺序不变）。
- 目标：**iters 16→4 且质量不降**（塔完整 + 125 复现 |v| 衰减 + m0_gates 末态全睡）。
- 判定：金样三场景（tower 完整性/Δpos、col45/pile5 入睡）+ 125 复现 dE + m0_gates
  末态 + T4 逐位一致；iters=8/4 的 8B 实测（目标：解算 ≤100ms）。
- 回退判据：任一场景退化即回退（这是行为变化类，允许换哈希但须显式记录）。

**阶段 2：接触聚类裁点**
- 内容：对「同一对面上的多点」做聚类（Rapier clustering 同思路）⇒ 点数降。
- 目标：8B 迭代成本 −30~40%（点数 2.52 → ~1.6/流形）。
- 判定：与阶段 1 同表 + 摩擦/转动行为抽查（单点接触的扭矩近似误差要金样把关）。
- **⚠️ 2026-09-18 前提否证（见 `EXPERIMENTS.md` 末节 J）**：本阶段的**前提**是"多点里有
  可裁的冗余"。实测（截断上限 4→3→2）：点数是**承重件**——4→3 使 pyramid 工作量 −19%
  但末态动能 +80%、125 体 KE ×24、最深穿透 ×3.9；4→2 直接推向垮塌（125 体 KE ×411、
  有体落到 y=0.66）。⇒ **本阶段关闭**，除非能给出"保极值点 + 保空间铺展"的**更聪明选点**
  （本仓实测是"按深度取前 N"，与 Rapier 聚类不等价），并给出该规则下的代价/收益。

**阶段 3：SoA + SIMD 全链**（前两步收敛后才做）
- 内容：约束数据 SoA 化（每点 ~15 个 f32 按字段并连续数组）→ 岛批量/岛内 4-lane。
- 依据：§7-4/§7-6（先布局后 SIMD；AoS 取数会放大访存）。
- 目标：迭代环再 −2×（按 §7 的 138ns/点·迭代靶子）。

**阶段 4：约束图染色**（README 列的 M1 项；大岛场景）
- 内容：岛内约束图染色 ⇒ 同色约束体互不共享 ⇒ 同色内可并行（新行为类）。
- 目标：`m1_pile`（万级单栈）与塔类大岛场景的墙钟（8B 小岛场景无收益，勿指望）。
- 判定：大岛场景实测 + 三哈希（允许按行为变化更新）+ 金样。

## 3. 风险与已知坑（先记，防重踩）

1. **确定性**：阶段 1–4 全是行为变化类 ⇒ 三/四哈希预期会变，须逐项显式记录
   （ADR 0004 流程）；阶段 0 必须逐位不变（否则说明搬运不纯）。
2. **塔的配方约束**：iters/soft 参数与塔稳定强耦合（iters=8 已崩）——阶段 1
   若不能把「iters=4 + 位置积分」做到塔稳定，则性能收益拿不到。
3. **warm 表 33.5ms 是内存延迟**（S21）⇒ 阶段 3 改布局时勿忘这条路径
   （`build_constraint` 查找同吃）；数据结构（排序合并/稳定槽位）另立小项。
4. **岛级并行已 5×**（ADR 0006）：阶段 4 只对大岛有意义；8B 的 11267 小岛
   场景收益≈0，验收别只看 8B。
5. **工作量**：阶段 0–1 ≈ 1–2 个会话；阶段 2 ≈ 1–2；阶段 3 ≈ 2–3；阶段 4 ≈ 1–2。
   **建议顺序 0→1 先行**（它们是睡眠侧的解药，且能直接缩短 8B 的迭代成本）。

## 4. 与验收的关系（诚实版）

- 8B 30 FPS（33.3ms）即便四阶段全成，也有结构下限：解算下限 ≈ 固定(2.9+33.5+建约束)
  ≈ 50ms + 迭代（4 迭代 × 12.75 × 1/4 聚类 × 1/2 SIMD ≈ 25ms）⇒ 仍 ~75ms
  **≳ 预算 2.2×**。⇒ **30 FPS 目标在「10 万体密堆」读法下即使全做完也难达成**
  ⇒ 口径问题（P3 两条路）必须先行解决，本设计是「无论口径如何都值得做」的部分。
- 若口径落在「稀疏接触场景」：阶段 1（睡眠）与阶段 4（大岛）优先，阶段 2/3 可缓。

## 5. 阶段 0/1 实测记录（2026-09-14 第六段）：阶段 0 ✅ 逐位通过；**阶段 1 否证**

**阶段 0（位置/旋转 scratch 搬运）✅**：岛组解算增加 pos/rot 的 gather/scatter 与
`group_pos/group_rot` scratch（与速度同构），积分量恒 0 ⇒ `m0_gates` 哈希
`0xbdf0cee6…` 与 stress `0x2cc8c488…` **逐位不变** ⇒ 搬运纯。**改动已回退**
（不留死代码；配方见本条）。

**阶段 1（逐迭代位置积分 + bias 刷新）❌ 否证**：实现 = 主迭代每步后
`pos += lv·h`（h = dt/iters）+ 旋转积分 + 从新几何刷新各点 rhs/cfm（弹性项烘焙）。
实测（三档迭代数、逐位对照）：

| 判定项 | 非 staged（配方） | staged iters=16 | staged iters=8 | staged iters=4 |
|---|---|---|---|---|
| 125 复现 dE 峰值 | **18.2** | 34.4 | 77.5 | 176.2 |
| tower25 \|v\| / KE / \|Δpos\| | **0.236 / 819 / 0.059** | 18.2 / 661 785 / **3.10** | 18.9 / 1.18M / 15.6 | 32.8 / 164 791 / 15.9 |
| 塔身 y 带 | [0.25,12.14] ✓ | [0.24,12.14] | [−13.44,12.08] ❌ | [−48.94,7.27] ❌ |

**变量隔离（两个判别实验）**：
1. `iters=1`（只发生一次积分=总量≈非 staged）⇒ 125 dE **1377.5** ⇒ 不是「瞬态速度
   被积分」的累积问题（迭代越少越差，单调）；
2. **只推进位置、不刷新 rhs**（隔离 rhs 刷新）⇒ 125 dE 41.2、塔 \|Δpos\| **13.1m 崩**
   ⇒ **「位置推进时机」本身**即为病因，不是刷新之过。

**机制（据上判定）**：本引擎的软接触形态把 erp 去穿透与 speculative 都做成
**每 tick 目标速度**（`erp_inv_dt·pen` / `gap·inv_dt`）——位置一次性推进时它们恰
被兑现一次；逐迭代推进会让位置在这些目标速度**仍处瞬态**时就开始走
（Σ_k v_k·h ≠ v_终·dt，且在 warm-start 瞬态段偏大）。Rapier 的 staged 之所以成立，
是因为它的偏置走**独立位置通道**（unbiased 速度趟 + 位置趟），而本引擎**两次实测
否决过位置修正通道**（分裂冲量位移写回退役；PBD 式切向位置投影塔弹射）——
所以「直接搬 Rapier 的趟间积分」在本架构上不成立。

**对计划的修正（重要）**：① 阶段 1 关闭——除非做**完整双趟**（unbiased 速度 +
独立位置趟 + live 几何），而那要重新启用被否决两次的位置通道，**不建议**；
② 睡眠侧与 10× 缺口的剩余路径应转向**不需要改偏置通道**的部分：
阶段 2 接触聚类裁点（点数 −30~40%，行为变化但不动偏置语义）与阶段 3
SoA+SIMD（先布局后向量化），阶段 4 染色（大岛场景）；③ 若仍要睡眠侧，
优先级应回到「验收判据口径」（SPEC §3 的稳定性判据是「休眠体重复唤醒 <1/s/体」，
不是「万级全睡」——本引擎的嗡振若**不触发反复唤醒**，在口径上可能已合规，
需按该判据实测一次）。

## 6. 综合结论（2026-09-14 第八段末）：9× 缺口是 solver 设计的**稳定性/工作量比**问题

**场景澄清（重要）**：`m1_scale` 的 8B 场景**本就是 SPEC §3 的「简单碰撞」读法**——
10 万动态盒以 1.0 间距摊铺落在 10 万静态瓦片地板上（0.2 间隙 > skin ⇒ 无侧接触链，
每体只有地板接触）。所以「稀疏读法能过」这个假设**不成立**：两种读法下都是 9–10×。

**拆到每体每 tick**：330ms / 10 万体 = **3.3µs/体·tick**（好的引擎 ≈0.3µs 量级）。
解算侧：≈2.9µs/流形 = 16 iters × 4 inner × ≈2.5 点 = **每接触对每 tick ≈160 次点求解**。
对照 Rapier：4 迭代 × 4 子步 = 16 次子求解、每次 1–2 点 ⇒ **我们的「稳定性/工作量比」
约差 4×**（用 4× 的扫掠量换到同等稳定；内层扫掠 inner=4 与 16 外层迭代都是
「配方」，两者降不了——S23/S11 实测）。

**结论**：9× = 4×（稳定性/工作量比，需 Rapier 式整组 staged + 聚类重构）
× ≈2×（工作单元数与每单元成本：聚类/裁点 ≈1.25×、SIMD ≈1.1–2×，且后者
受本引擎「解算访存五路全否」约束——§15）。
⇒ **本引擎在当前设计形态下无法达到 SPEC §3 的 8B 档（30 FPS）**；要跨过去必须
做**求解器换取架构**（这正是本设计文档 §1 的整组 staged 语义），属多会话级重构，
且其关键件「位置通道」在本引擎历史上被两次否决（§5）⇒ **需产品裁决**：
(a) 启动求解器重构（大，且要重开位置通道）；(b) 重新校准 M1 的性能预期（例如
按「已通过档位不劣化 >10%」的持续回归口径 + 相位级预算记账）；
(c) 换验收场景读法（如「简单碰撞」= 更小规模或静态为主）。

**本轮已穷尽的局部杠杆（全部有实测记录，勿重走）**：迭代数（S11）、inner 扫掠
（S23）、SIMD 岛批量（ADR 0006）、staged 位置积分（S22）、点布局（S24）、
体侧缓存（§7-6）、扁平化（§7-5）、快速哈希（S12/S21）、预取（W8）、边距
（W9，已落地 +12% 均值）、树形（W3/W4）、失效策略（W1）。
已落地净收益（累计）：边距 K=6（broad 均 −12%、树均 −33%）、深度跳变门（125 峰值
148.9→18.2、pile5 入睡 +505）、相对 §5 时点整体 −28%（环境校正后）。

## 7. 架构诊断（2026-09-14 第九段）：三个结构缺陷，两个有实锤

### 缺陷 ①：稳定靠「同一线性化重复扫掠」（inner），不靠「重新线性化」（子步）

**实锤（塔 25 层 600 tick，同配方只换排列；扫掠数 = 子步 × 迭代 × 内层）**：

| 排列 | 总扫掠/tick | tower \|v\| / KE / 塔身 | 判定 |
|---|---|---|---|
| 1 × 16 × 4（现行配方） | 256 | 0.236 / 819 / [0.25,12.14] | ✓ 基准 |
| **4 × 16 × 1** | **64** | 0.256 / 1124 / [0.24,12.13] | ✓ **同等质量、1/4 工作量** |
| **8 × 8 × 1** | **64** | **0.222** / 647 / [0.24,12.14] | ✓ 更好 |
| 8 × 16 × 1 | 128 | 0.201 / 334 / [0.24,12.16] | ✓ 显著更好 |
| 4 × 8 × 1 | 32 | 14.5 / 崩 | ❌ 悬崖在 64 附近 |

⇒ **子步的每遍 ≈ inner 的 4 倍价值**（子步=新几何+位置推进+重烘焙；inner=同一线性化
再扫、增益递减）。**这就是 §6「稳定性/工作量比差 4×」的机理。**

### 缺陷 ②：子步重复跑碰撞检测 ⇒ 便宜子步用不起来

**实锤（8B 同窗口）**：(1,16,4) 总均 **329.8ms** → (4,16,1) 总均 **607.2ms（慢
1.84×）**：窄相 11→29–45ms、宽相/其它同步 ×4 —— **碰撞检测/管线被跑 4 遍，
盖过了解算 −1/4 的收益**。
根因：`World::substep()` 每子步完整重跑 字段→速度积分→宽相→窄相→解算→位置积分→CCD；
而 SPEC §2.5 定案的 TGS-Soft（Box2D v3 同族）是**窄相一 tick 一次、接触集跨子步存活**，
子步只做「按当前位置重烘焙锚点/深度/偏置 + 少量迭代 + 位置积分」。

### 缺陷 ③：几何/偏置一次烘焙到 tick 末（阶段 1 失败的真正原因，修正 §5 归因）

§5 把阶段 1 的失败归因为「位置推进时机」——**不完整**。真实机制：`build_constraint`
把**力臂 `ra/rb`**、质量项、偏置一次性烘焙（tick 开头），阶段 1 却让位置/旋转在迭代间
移动 ⇒ **冲量施加用冻结力臂、几何已变** ⇒ 不一致 ⇒ 注能（塔 KE 66 万）。只刷新 `rhs`
不够，**力臂/质量必须随几何一起刷新**。⇒ §5 的「位置通道不可行」结论**作废**：
不可行的是「冻结几何 + 移动位置」的组合，不是 stage 结构本身。

## 8. 修复计划（每步独立判定；P1→P3 是本设计的主线）

| 步 | 内容 | 预期 | 判定 |
|---|---|---|---|
| **P1** | **碰撞检测上提到 tick 级**：宽相/窄相移出子步循环，接触集跨子步存活 | 子步成本从「全管线 4×」降到「重烘焙+迭代」 | 行为应逐位不变（仅重排）→ 四哈希 |
| **P2** | **子步内重烘焙几何**：每子步从锚点重算 `ra/rb`/深度/偏置（质量项可先沿用）| 缺陷③修复 ⇒ 阶段 1 的注能消失 | 125 复现 + 塔 + 金样 + 四哈希 |
| **P3** | **结构换挡**：转 `substeps=4 × iters=4 × inner=1`（或按场景定档，§4.13 LOD 同理） | 塔在 64 扫掠下保持（已证）；8B 解算 290→~70–120ms | 全验收 + 金样容差表 |
| P4 | 岛内并行/染色（SPEC §2.6；大岛场景 m1_pile） | 大岛串行 → 并行 | m1_pile 墙钟 + T4 式扩展 + 哈希 |
| P5 | 聚类裁点（可选，工作单元 −25%） | 解算 −20% | 同 P3 |

**期望修订（诚实）**：P1–P3 是本次诊断里**唯一有 4× 级实锤的杠杆**，8B 预计
330ms → **~110-150ms**（≈2.2-3×）；要再往 33.3ms 靠，需 P4/P5/SIMD 叠加并逐项复测
（SIMD 受「解算访存五路全否」约束）。**不建议**在 P1–P3 之前再试任何局部旋钮
（本轮已穷尽 12 项，全部有实测记录）。

## 9. P1 实测记录（2026-09-14 第十段）：单独不成立，必须与 P2 同批

**实现**（已回退）：`World::step` 改为「子步 0 检测（宽相 + 窄相）、后续子步复用
流形表」——`substeps=1` 时**逐位不变**（`m0_gates` 哈希 `0xbdf0cee6…` 与 41 组测试
均不变 ⇒ 重排纯）。

**配方（substeps=4）下的实测**：

| 场景 | 现状（每子步检测） | P1（tick 级一次检测） |
|---|---|---|
| col45 | 35/45 入睡 | **45/45 全睡**（|v| 0.000）✅ |
| pile5 | 1700/2000 | **1970/2000** ✅ |
| tower25 | \|v\| 0.236 ✓ | **\|v\| 44.4 / KE 74 万 / y 带 [−96.4, 12.2]** ❌ **崩** |

⇒ **P1 单独不成立**：对简单平堆是净正向（甚至更好），对塔是灾难——复用「检测时刻的
流形世界点」而位姿已推进 ⇒ 塔的接触churn（每 tick 大量换面）吃到陈旧点。
⇒ **陈旧流形必须由 P2 的 prepare 路径按当前位姿重推导**（这正是 Box2D
`b2PrepareContacts` 做的事：流形的**局部**点固定、世界锚点/分离每子步从当前变换重算）。

**成本侧（8B 同窗口，P1 + 4 子步 × 16 迭代 × 1 内层 = 64 扫掠）**：
总均 **628ms**（vs 现状 (1,16,4) 同样 64 扫掠的 329.8ms）——**无收益**。
根因：**每子步的固定成本被付 4 遍**：warm 表重建 ≈33.5ms、建约束 ≈12ms、建岛 ≈3ms、
gather/scatter/休眠各一次 ⇒ 每子步 ~50ms 固定成本，盖过扫掠的节省
（子步型 64 扫掠的纯扫掠成本确实更低：晚段 tick 168ms vs 现状 ~290ms，但固定成本
4× 把它吃光还剩缺口）。

**⇒ P2 的规格（下一会话的直接入口）**：
1. **cbuf 跨子步存活**：子步 k>0 不再走 warm-map 匹配，直接复用上一子步的
   `ContactConstraint` 列表（`pn/pt1/pt2/la/lb/depth0` 已在点内）⇒ 消灭每子步的
   33.5ms warm 表 + 12ms 建约束（这是成本的真正大头）；
2. **prepare（每子步）**：从当前位姿 + 局部锚点重算 `ra/rb`、有效深度、偏置/cfm、
   质量项（质量项可先沿用、后量）；流形的「特征存在性」由 check（点是否仍有效）
   保证，避免陈旧点注能；
3. 之后再试结构换挡（P3）：`4 子步 × 4 迭代 × 1 内层`（16 扫掠，塔的质量需重新验，
   参考：Box2D 默认 4 子步 × 1-2 迭代）。
**判定**：塔/三场景 + 125 复现 + 四哈希 + m0_gates 全睡（行为变化类）。
**期望（诚实）**：P1+P2+P3 后 8B ≈ **150-200ms**（≈1.7-2.2×），**不足以单独跨 9×**；
P4 岛内并行对 8B（小岛）无用，P5 聚类 ~20%。⇒ 9× 的其余部分需要 SIMD/更深的
算术形态改造，或按 §4.13 LOD 对验收场景定档（需产品裁决）。

## 10. P2 实测（2026-09-14 第十一段）：**证伪**——「复用约束 + prepare」在本引擎里必崩

**已实现的 P2**（全部已回退）：`solve()` 增子步号；首子步 build（含 warm 匹配），
后续子步**复用约束列表**并跑 `prepare_constraint`（从 `la/lb` + 当前位姿重算
ra/rb/深度/质量/rhs/cfm + `active` 失效门）；warm 回写与休眠只在末子步；
World 侧「仅首子步检测」。`substeps=1` 逐位不变（哈希/测试同值）✓。

**实测（tower25 600 tick、配方 substeps=4；pre-P2 基准 = |v| 0.236 塔身完整）**：

| 变体 | prepare 内容 | tower 结果 |
|---|---|---|
| pre-P2（现状） | 每子步**重检测 + 重匹配 + 全量 build** | **0.236 / 塔身完整** ✓ |
| P1（仅复用流形） | —（无 prepare） | 44.4 / y 带 [−96.4,12.2] ❌ |
| P1 + 失效门（V3） | 仅 `active` 门 | 8.0 / 300 tick 倒到 4.48 ❌ |
| V2 | 门 + ra/rb + 深度 + rhs/cfm（不刷质量） | 8.1 / 倒到 4.70 ❌ |
| full-P2 | 门 + 全部（含质量） | 31.6 / 立刻被弹飞（tick50 y 带到 14.36）❌ |

**结论（重要）**：**「每子步重新检测 + 重新匹配」不是纯开销，是承重件**——
把流形/约束跨子步复用后，任何 prepare 变体都撑不住塔（且 prepare 越"全"越糟）。
⇒ **「廉价子步」（P1+P2 路线）在本引擎不成立**；子步型排列的 4× 质量/工作量优势
（§7 缺陷①）**无法在不稳定化的前提下兑现**。

> **⚠️ 2026-09-20 更正：上面这条"不成立"的**归因**已改为"场景相关"，结论降级为"待复核"。**
> 本节把失败归因于"本引擎流形是**裁剪产物** ⇒ 材料点配对随裁剪漂移"。新落地的 warm 匹配
> 分支诊断（`EXPERIMENTS.md` 末节 K）实测：
> - 平面大堆（pyramid）**稳态精确特征命中 85.1%**（回退仅 3.0%）⇒ 该归因**在平面堆上不成立**；
> - **塔（tower25）只有 69.6% 精确，非精确匹配达 30%**（**回退分支 18.7%**、未匹配 11.7%）
>   ⇒ 该归因**在塔上成立**，而塔正是本路线失败的那个场景。
>
> 由此得到两个候选机理，**K.1 的成因拆解已把 ② 削弱**：① **开局自由落体的检测冻结**
> （P1 是"子步 0 检测、后续复用"，塔开局 2 cm 缝下坠 2–5 cm/tick ⇒ 后 3 个子步用接触前
> 流形 ⇒ 深穿透无阻挡 ⇒ 巨额修正）——**当前唯一候选**；② ~~塔的参考面翻转~~：实测塔的
> 回退命中里 78% 确是"面选择变了"（侧别 21.5% + 哈希 55.9%），但那是**跨帧** churn，
> 而 tick 内复用同一张流形表时特征 ID 不变 ⇒ 解释不了本路线的崩。
>
> **重开条件（比直接重开 P1 更小更该先做）**：先按门分类计数定位塔那 30% 的失配来源
> （法向 0.95 门 / 距离门 / 深度跳变门 / 无上一帧流形 / 参考面翻转）。若主因是参考面翻转，
> 可行性取决于**参考面选择的跨帧稳定性**（可加迟滞），与流形语义本身无关。判据仍为
> 塔三场景 + 三哈希换代 + arena 长跑 `--steps 3000`。

**为什么（据证据的推断）**：本引擎接触的「材料点配对」由**窄相每帧重新配对**
决定（裁剪点 = 每次碰撞检测的几何产物），而 `depth0 + sep·n` 的回收约定只在
「锚点仍是当前配对」时成立；复用流形后配对陈旧 ⇒ 有效深度/力臂/质量与真实几何
失配，被 erp/spec 目标放大（full-P2 的质量随几何刷新反而让失配更"自洽"地注入）。
⇒ 结构性差异：Box2D 的流形是**特征稳定**的（局部点不随裁剪漂移），本引擎的流形是
**裁剪产物**——这是两套求解器语义的接缝，不是能靠 prepare 补上的实现细节。

**这条线关闭后的剩余判断（诚实）**：9× 缺口里「4× 稳定性/工作量比」目前**没有
可行的兑现路径**（要么改窄相为特征稳定的流形——大改且影响所有接触语义；要么接受
现状）。可做的仍是与子步无关的项：P4 岛内并行/染色（大岛场景）、P5 聚类、
SIMD/布局（上界受限）。**建议**：把取向从「追 30 FPS」改为「按 SPEC §3 的
持续回归口径（已通过档位不劣化 >10%）+ 相位级记账」交付，把 30 FPS 作为
「需要窄相/求解器语义重构」的立项输入。

**⇒ 下一步（据本节证据修正，按顺序）**：
1. **warm 缓存的槽位化**（真瓶颈，且与子步无关）：每子步/每 tick 的 warm 表
   「重建 + 剪枝」实测 **33.5ms**（220k 次随机桶访问、内存延迟主导，S21 已证换哈希
   无效）。流形序在 **tick 内与相邻 tick 间**高度稳定 ⇒ 用「**按流形位置对齐的槽位表**
   （`slots[i].key == 当前流形键` ⇒ 原位写入，零哈希）+ 键→槽的表仅供错位回退」
   可把该成本砍到接近 0，并**同时**让「每子步重检测 + 重匹配」的子步结构变得便宜
   （窄相 11ms/次 × 4 可接受；求解器固定成本里的 warm 表 33.5ms/次 才是杀手）。
   判定：逐位不变（同键同值）+ 8B 计时 + 金样三场景。
2. 若 1 成功，再走「P3 结构换挡」（4 子步 × 4-16 迭代 × 1 内层），且**保留每子步
   重检测**（本节已证它是承重件）——此时子步的增量成本 ≈ 窄相 11ms + 少量固定项。
3. P4 岛内并行/染色（大岛场景）与 P5 聚类仍可独立推进。

## 11. warm 缓存攻坚第一步（2026-09-14 第十二段）：分拆 + 印章法**无收益** ⇒ 真因锁定

**分拆实测**（8B 同窗口，临时计时器，已删）：`warm 合并 + 剪枝` 33.5ms 里——
- **合并插入**（220k 次 `HashMap::insert`）= **13.14ms**
- **剪枝**（构建 195k 的 `cur` + 排序 + 每条 `binary_search`）= **20.67ms**

**印章法尝试（已实现并实测，行为逐位一致，但无收益 ⇒ 已回退）**：把剪枝换成
「本 solve 调用是否刷新过」的印章（`WarmManifold.seen: u32`）单遍 `retain`，
**证明等价**（有流形且≥1 体清醒 ⇒ 必属清醒岛 ⇒ 必被求解盖章；双睡的条目两条规则
都保）——`m0_gates` 哈希逐位不变 ✓✔（等价性得到实测背书）。但 **8B 总均
331.8 vs 基线 329.8（无收益）** ⇒ 剪枝的成本**不在 binary_search**，而在
**遍历 HashMap 桶数组本身**（≈195k 条 × 随机/半随机访存 + `awake` 两次读）。

**⇒ 真因锁定（下一步的正确靶子）**：warm 表的成本是**访存**——无论是 insert、
retain 还是 get，都在走散列桶。唯一有效的修法是**稠密槽位表**：
`slots: Vec<WarmManifold>`（连续内存）+ `index: HashMap<键, u32>`（只在「新键分配」
与「查不到时」使用）。设计要点：
1. `build_constraint` 查找时**同时解析槽号**并把槽号带进约束（求解期只读槽位）；
2. 回写（合并）改为**按槽号原位写入** ⇒ 合并的 13ms 归零；
3. 剪枝改为**对稠密槽位单遍扫描**（连续内存）+ 印章标记，空洞用「空闲表 + 高水位线
   周期压实」处理 ⇒ 剪枝的 20ms 也应落到 ~3-5ms；
4. 预期总收益 **~25ms/tick（≈7.5%）**，且**逐位不变**（同键同值，仅数据结构变化）。
5. 风险：空洞管理与压实要保证「同键同值」⇒ 判定 = 四哈希 + 金样三场景 + 8B 计时。
