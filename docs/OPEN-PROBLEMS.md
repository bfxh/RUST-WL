# 未解问题台账（OPEN-PROBLEMS.md）

> M1 剩余的全部开口问题。每条：**现状 → 已排除（链 EXPERIMENTS.md）→ 候选入口 →
> 验收判据（三边并列，缺一不可）**。解决即移入 M1-PLAN 落地段（+adr 如构成决策）。
> **M2 启动条件：本表 P1/P2/P4 全部 ✅（P3 核口径后有结论）**
> ——纪律：任一任务不过即停在上一个可用状态。

## P1 — T1 求解稳定性：峰值已达标；睡眠侧 = 整组 staged 语义（大改）

**现状（2026-09-14 第三段后）**：
- **匹配线已关闭**（决定性证据 S19）：全拒档跑 600 tick 仍 125/125 不睡、|v| 0.05–0.28
  持续振荡 ⇒ 「125 体末态入睡」**不是匹配侧任何门能达到的**；与第十二/十三段
  「本征残差」结论互证。
- 落地两件：回退半径 1×skin（ADR 0003）+ 深度跳变门/分离门（ADR 0007）——
  125 体 dE 峰值 148.9→**18.2 ✅**、pile5 入睡 1195→**1700/2000**、tower25
  |v| 0.283→**0.236**、`m0_gates` 全睡保持、T4 4.80× 逐位一致；代价 col45 位置
  0.0065→0.0105 m（T5 收紧口径内）。
- **睡眠侧剩余缺口**：万级入睡（`m1_pile` awake→0 = 10000 不睡、dE 沸腾；tower25
  0/2500 入睡）需要 **Rapier 式整组 staged 语义**（biased/unbiased 双趟 + 趟间
  位置积分 + live 分离刷新 + 颜色分组 + 聚类）。已核实本引擎解算中**位置不变**
  ⇒「趟间位置积分」结构上缺失。

**睡眠缺口的第一手读数（2026-09-20，金样 `tower25/pile5` 600 tick 末态 + 既有"阈值分解"块）**：

| 场景 | 末态入睡 | 仅线性超阈 | 仅角速超阈 | 双超 | **阈值下**（共） |
|---|---|---|---|---|---|
| tower25 | 0/2500 | 448 | 326 | 1405 | **321**（2500） |
| pile5 | 1735/2000 | 24 | 3 | 8 | **1965**（2000） |

⇒ 三条结论（其中一条**否掉本档此前的一个隐含假设**）：
1. **塔不是"少数体拖累全岛"**：87%（2179/2500）的体**自己就超阈**（|v| 达 0.224 = 阈值 0.04 的
   5.6 倍；|ω| 达 0.501 = 阈值 0.05 的 10 倍）⇒「岛内原子入睡」不是塔的瓶颈，
   真正的瓶颈是**全局残留微动高于阈值**。（pile5 相反：仅 ~35 体超阈 ⇒ 能睡到 1735，
   其差的 230 才是原子规则 + `sleep_time` 窗口的代价。）
2. 残差**集中在上层**（|v| 前列 y 6.2–12.1、|ω| 前列 y 9.1–12.1，塔高 12）——自由面"甩鞭"特征。
3. **目标可重构 + 杠杆—代价曲线已量（2026-09-20，金样 `tower25` 600 tick，vxl 16 迭代）**：

   | substeps | stab | 末态入睡 | \|v\|max | KE(J) | **阈值下**（共 2500） | ms/活跃tick |
   |---|---|---|---|---|---|---|
   | 4 | 0 | 0/2500 | 0.224 | 822.0 | **321** | 105 |
   | 8 | 0 | 0/2500 | 0.234 | 336.0 | **1020** | 221 |
   | 4 | 2 | 0/2500 | 0.191 | 650.3 | **287** | 121 |
   | 8 | 2 | 0/2500 | **0.144** | **204.7** | **1323** | 237 |

   ⇒ ① **子步是主杠杆**（阈值下 321→1020，×3.2；代价 ms/活跃tick ×2.1）；
   ② `stab` 单独**几乎无效**（4,2 反而 287），只在子步档里叠加（8,2 再 ×1.3）——与既有
   "非加性"观察一致；③ **最好的旋钮组合仍差 3.6 倍**：塔要睡需要 |v|max < 0.04，而 (8,2) 只到
   **0.144**，且塔是**一个岛**（1177 体超阈 ⇒ 原子规则下全岛不睡）⇒ **现有旋钮到不了**。
   ⇒ **结论**：本条"需要整组 staged 语义"的判据**成立且已量化**（目标：|v|max 再降 3.6× 或
   睡眠规则允许**部分入睡**——后者是行为改变，须过四哈希 + 金样 + 稳定性判据）。
   对照：Rapier 同场景 2500/2500 入睡、|v|→0.000、8.07 ms/活跃tick ⇒ 残差不是"物理必然"，
   是**架构差异**。

4. **标度律与外推（2026-09-20，塔 + `stab=2`，300–600 tick）**：

   | substeps | \|v\|max | KE(J) | 阈值下（/2500） | ms/活跃tick |
   |---|---|---|---|---|
   | 4 | 0.191 | 650.3 | 287 | 121 |
   | 8 | 0.144 | 204.7 | 1323 | 237 |
   | **16** | **0.073** | **77.1** | **2034** | **462** |

   ⇒ ① **每次子步翻倍 ⇒ |v|max 约减半**（0.144→0.073 = ×0.51），代价**线性**（ms/活跃tick
   每翻倍 ×2）；② 外推：要 <0.04（入睡所需）还需再翻一倍 ⇒ **substeps=32 ≈ 9× 默认代价**
   ≈ 920 ms/活跃tick（Rapier 同场景 8.5 ms ⇒ 差 ~108×）⇒ **旋钮路线不可行**，判据从"感觉"变成
   算术；
   4. **角向线索**：高保真下阻塞项**从线性换成角速度**——substeps=16 时「仅角速超阈 354」远多于
   「仅线性超阈 59」（8 子步时是 543 vs 334、双超 505）⇒ 若要走"压残差"路线，该盯的是
   **角速度侧**的机制（摩擦锥/角向扫掠），而不是继续加子步。这一条与 A4 的"放大角惯量"否证
   不同：那是**改物理常数**，这是**查角向机制**。

   **新配方下的角向画像（2026-09-20，`16/1/16`，塔 600 tick）**：角速前 8 的 |ω| = **0.136–0.203**
   （阈值 0.05 的 **2.7–4.1×**，非临界），而它们的 |v| 仅 0.010–0.053（**多数在线性阈值之下**）
   ⇒ 这些体是**原地打转**（自转不位移）；位置在 **y≈1.2–4.2 的下/中层**（其中三个挤在同一层
   4.202）+ 一个在顶层 12.1
   ⇒ **修正**：旧配方下"残差集中在上层"说的是**线性**残差；新配方下**角向残差住在下/中层**，
   两种残差位置不同。
   **持续性判别（2026-09-20，同配方 `16/1/16` 的两个时点）**：

   | tick | 仅线性超阈 | **仅角速超阈** | 双超 | 阈值下 |
   |---|---|---|---|---|
   | 300 | 290 | **235** | 62 | 1913 |
   | 600 | 191 | **261** | 31 | 2017 |

   ⇒ 线性残差在**衰减**（290→191），而角向残差**不衰减、略增**（235→261）⇒ **角向残差是稳态
   现象而非瞬态**。
   **μ 剂量-响应（2026-09-20，同配方 300 tick，`VXL_MU` 覆盖金样摩擦）**：

   | μ | 仅线性超阈 | **仅角速超阈** | 双超 | 阈值下 | \|v\|max | KE(J) |
   |---|---|---|---|---|---|---|
   | 0.25 | 213 | **113** | 25 | **2149** | 0.103 | 113 |
   | 0.5（基线） | 290 | **235** | 62 | 1913 | 0.093 | 126 |
   | 1.5 | 239 | **754** | 217 | 1290 | 0.175 | 169 |
   | 5.0 | 0 | 2 | 2498 | 0 | 14.0 | **3 142 534** ✗✗ |

   ⇒ ① **角向残差随 μ 单调增大**（113→235→754）——与"加摩擦就粘住"的直觉**相反** ⇒ 角向残差是
   **摩擦冲量驱动**的（切向冲量的力臂在净注入转矩），**"提高 μ"不是解**（已用剂量-响应否掉）；
   ② **μ=5.0 发散**（KE 314 万 J、y 带 12.2→8.9、全超阈）⇒ 顺带暴露**高摩擦下不稳**这一
   独立的鲁棒性缺口（μ=5 不物理，但引擎通常应优雅退化）。
   **下一步（代码实验）**：查**切向冲量的施加**——`group_apply` 的角向部分与切向冲量的
   作用点/顺序（净转矩 ∝ μ 说明符号上没有正确对消）；另需给 μ=5 的发散单独定位。

   **首个代码审读结论（2026-09-20，零成本读码，未改代码）**：摩擦公式本身正确——
   `solve_constraint` 里是**速度驱动**（`vt1/vt2` 朝零相对切向速度解 2×2）+ **锥夹紧**
   `max_f = p.friction · p.pn`，与 Rapier 同族；子步内陈旧也已排除（新配方 `inner=1`，
   且每子步重新检测 ⇒ 切向基每子步刷新）。
   **主嫌：暖启动的表示与基不匹配**——`WarmPoint` 存的是**标量** `pn/pt1/pt2`，它们是
   **当时切向基下的分量**；下一子步基已随法线轻微转动，标量却被当作**新基下的冲量**继续用
   ⇒ 每子步投错一点点 ⇒ 净转矩累积，**量级正比于冲量（∝ μ）且稳态不衰减** ✓✓
   与三条实测（μ 单调↑、300→600 不降、inner=1 已排除子步内陈旧）**全部吻合**。
   **两条候选修法**（须过四哈希 + 金样 + 稳定性 + arena）：① 暖启动改存**世界系冲量向量**
   （复用重投影到新基，零额外状态）；② 或在法线转动超过阈值时**放弃该点暖启动**（保守但简单）。
   判据：新配方下塔的「仅角速超阈」应显著下降（当前 235 @300 tick / 261 @600），且
   pile/col45 不回退、μ=5 的发散另行定位。

   ✅ **假设已实测验证（2026-09-20，修法①的 12 行实验版）**：在暖启动复用点把 `pt1/pt2`
   由**存储时基**还原成**世界系切向冲量**再投到当前基（`t1o·pt1 + t2o·pt2` → `.dot(t1/.t2)`）：

   | 场景 | 指标 | 基线 | 加重投影 | 判定 |
   |---|---|---|---|---|
   | tower25（300 tick） | 仅角速超阈 | 235 | **164（−30%）** | ✅ 假设成立 |
   | tower25 | 超阈总数（线+角+双） | 587 | **432** | ✅ |
   | col45（600 tick） | 入睡 / 超阈 | 45/45 / 0 | **45/45 / 0** | ✅ 无变化 |
   | pile5（600 tick） | 入睡 / KE / 超阈 | 1980 / 0.1 / 0 | **1940 / 0.3 / 5** | ⚠️ 小幅退（−2%，容差内） |

   ⇒ **主嫌确认**（跨子步暖启动的基不匹配确有净转矩注入）；但**不是全部**（降 30% 而非归零）。
   **pile5 那 −2% 的定性（2026-09-20，补测轨迹）**：带改动时 pile5 的入睡数在 **450 tick 后停在
   1940 不动**（50:1155 → 100:1685 → 300:1885 → 450:1935 → **500–600:1940 平台**），而基线到
   600 tick 为 1980 ⇒ **是永久平台、不是"晚睡一会"** ⇒ 这是一次**真取舍**（塔 −30% 角向 vs
   pile5 −40 体入睡），不是免费收益。
   ❌ **"阈值门控能否两全"已实测否证（2026-09-20，门控 = 接触切向相对速度 |v_t| > 5 cm/s）**：

   | 场景 | 指标 | 基线 | 无门控重投影 | **带门控重投影** |
   |---|---|---|---|---|
   | tower25(300) | 仅角速超阈 | 235 | **164** | **248**（收益被挡掉） |
   | pile5(600) | 入睡 | 1980 | 1940 | **1925**（更差） |

   ⇒ 门控**既没保住塔的收益、又让 pile5 更差** ⇒ **"免费收益"不成立，取舍是固有的**。
   **剩下的只是一个价值判断（需人拍板）**：无门控重投影下 —— 塔「仅角速超阈」−30%（235→164）、
   超阈总数 587→432  ⟷  pile5 入睡 −40 体（1980→1940，永久平台）。选哪边，或都不选。
   **⚠️ 上面的 −30% 是 300 tick 单点读数；600 tick 单点是 −16%，窗口均值下只剩 −2.4%（不显著）。**
   **裁决：不落地（2026-09-20，窗口均值复测定案）**——见下方"重投影的最终裁决"。三条理由：
   ① 角向收益在窗口均值下**消失**（单点读数落在 ±18% 波动带内）；② 账面混合且对阻塞问题
   （塔入睡）无影响（0/2500 不变）；③ 按本仓"无实测净收益即回退"纪律。留作**已量化的
   正确性偏差**（机制本身成立、效应量不成立），与角向机制一并处理；同时先修**陈旧的
   `m0_gates` 判据**（见下）。
   **落地（若仍选接受）**：换代哈希流程（四哈希 + 冻结读数按 ADR 0004 更新）+ 金样三场景 +
   arena 19/0/0；改动位置（暖启动复用点，约 12 行）与全部读数在本条内可复核。
   **代码已回退**（只留读数与下面的补丁）。μ=5 的发散见下方重定性。

   **补丁留档（可复现的最小形态；落在 `WarmPoint` 复用处、`pts[npts] = PointConstraint{…}` 之前）**：

   ```rust
   let warm_pt = warm_pt.map(|w| {
       let (t1o, t2o) = tangents(warmm.map_or(m.normal, |wm| wm.normal));
       let tw = t1o * w.pt1 + t2o * w.pt2;
       WarmPoint { pt1: tw.dot(t1), pt2: tw.dot(t2), ..w }
   });
   ```
   实测旋钮版还带 `WARM_REPROJECT: u8`（0 = 基线 / 1 = 仅切向 / 2 = 全向量），
   用 `sed`/Edit 切换档位复现全部读数；**该旋钮是实验件、不入默认**。

   **补充判定（2026-09-20，重投影 × μ）**：
   - **μ=5 发散与暖启动机制无关**（实测排除）：加装重投影后仍发散，且略差（KE 314 万 → **368 万**）
     ⇒ 两条缺口**真正独立**，μ=5 需单独定位（不再挂在"暖启动"这条上）。
   - **重投影的收益随 μ 变化**：μ=0.5 时塔角向 −30%（235→**164**）；μ=1.5 时 754→782（**中性偏负**）
     ⇒ 该修法只在中低摩擦下有效，别期待它顺便解决高摩擦不稳。

   **⚠️ 上一条"两条缺口真正独立"的**范围修正**（2026-09-20 复测，同一会话 A/B）**：
   该结论测的是"切向重投影**不治** μ=5"（成立），但被写成了"μ=5 **与暖启动机制无关**"
   （不成立）。新证据：μ=5 的失败**随重线性化次数增长**，与"逐子步注入"同族，见下。

   **600 tick 耐久性复测（决定性：把决定的判据从 300 tick 挪到验收时程）**：

   | 指标（`16/1/16`，600 tick） | 基线 | 切向重投影 | 判定 |
   |---|---|---|---|
   | tower 仅角速超阈 | 261 | **220** | ✅ −16% |
   | tower 仅线性超阈 | 191 | 175 | ✅ −8% |
   | tower 双超 | 31 | **48** | ❌ +55% |
   | tower 超阈总数 | 483 | 443 | ✅ −8% |
   | tower 阈值下 / KE(J) | 2017 / 103.5 | 2057 / **106.9** | ⚖️ / ❌ |
   | tower \|v\|max | 0.091 | **0.097** | ❌ |
   | pile5 入睡 / 超阈 / KE | 1980 / 0 / 0.1 | **1940 / 5 / 0.3** | ❌ |
   | col45 入睡·超阈 / Δpos / 活跃 tick | 45·45 / 0.0118 / 456 | 45·45 / **0.0069** / **126** | = / ✅ / ✅ |

   ⇒ **① 收益衰减**：塔角向 −30%@300 → **−16%@600**，且带修法时角向残差**仍在增长**
   （164→220）⇒ 重投影买的是**固定偏移**的消除，**不是**增长机制的消除（"300 tick 判据"
   会把收益读高，这是判据与节奏耦合的又一例）。
   ② **账面是混合的**，没有主导项：角向/线性/Δpos/静定速度变好，pile5 入睡、塔 KE、
   `|v|max`、双超数变差；**对"塔能否入睡"这个阻塞问题没有影响**（0/2500 不变）。
   ③ **`m0_gates` 判据本身是陈旧的**：`M0-GATES.md` 记「末态活跃 = 0（全睡）」，而当前基线
   （哈希 `0x6219d186…` 与档内逐位一致）实测 **51**；切向重投影给 **27**、睡醒翻转 2→1
   （代价 max_depth 0.2561→0.3860 瞬态、deep_ticks 79→87）。哈希换代
   `0x6219d186…`→`0xaa93ed05…`（门槛）/ `0x63e5eb35…`→`0xfa72ab78…`（压力），
   `twin_match` 仍 true、arena 平稳、PASS ⇒ P1 验收里「m0_gates 末态活跃保持 0」
   这条**其"0"不是当前基线**，须按实测重写。

   **全向量重投影（法向也按烘焙法线还原）= 机制判别，不是候选修法（2026-09-20，塔 300 tick）**：

   | 档 | 仅线性超阈 | 仅角速超阈 | 双超 | 阈值下 |
   |---|---|---|---|---|
   | 基线 | 290 | 235 | 62 | 1913 |
   | 切向重投影 | 245 | **164** | 23 | 2068 |
   | **全向量重投影** | **155** | **259** | 44 | 2042 |

   ⇒ **法向失配主要驱动线性残差、切向失配主要驱动角向残差**——两类残差成因不同；
   且两者**不能同时**用这一手法修（全向量把切向的角向收益吃掉）。这条是归因产物，
   落地价值低（`pn` 重投影还改了摩擦锥尺度）。

   **μ=5 的重新定性（2026-09-20；新小复现 + 配方分配判别）**：
   - **新小复现（亚秒级，取代塔 2500 体）**：`VXL_MU=5` + **col45**（45 体单列）在
     `16/1/16` 下**整体垮塌**（KE **6752**、y 带 2.25→**0.80**、Δpos **5.83 m**）；
     μ=2.0 稳定（45/45 睡、Δpos 0.0094）⇒ 阈值在 **2.0–2.5** 之间（2.5/3/4 均垮）。
   - **不是 dt 极限（方向相反）**：substeps **64 更糟**（y 带 → **−121.98**、KE 347 219）；
     substeps **4 反而撑住**（y 带 [0.25, 2.26]）⇒ 失败**随重线性化次数增长**。
   - **固定总扫掠 256、只改分配（μ=5 col45）**：`16/4/4` **30/45 睡**（KE 0.4）·
     **`16/2/8` 45/45 全睡（KE 0.0）** · `16/1/16` **垮塌**（KE 6752）
     ⇒ **新参考配方 `16/1/16` 在高摩擦下不稳，而同为 256 扫掠的 `16/2/8` 完好**。
   - 两条重投影变体都不治它（切向：塔 KE 3.14M→3.68M；全向量：col45 KE 5386 仍垮）。
   ⇒ **结论**：切向重投影不治 μ=5（实测成立）；但 μ=5 的失败**与重投影次数同向增长**，
   属"逐子步注入"的失控区，**不是** dt 极限、也**不是**已排除的独立机制。
   ⇒ **派生条目（重要，直接关系配方升默认）**：参考配方的**适用域要标 μ 上限**
   （16/1/16 从 μ≥2.5 起垮；16/2/8 不垮）——见「下一轮入口」新增第 3 条。

   ⇒ **修正二（2026-09-20，时程复测）：塔处于「极限环」，而决定量在波动带内。**
   补测 1200 tick（基线，逐 50 tick 采超阈分解）后，上面"300→600 不降反增 ⇒ 稳态增长"
   **和**当时读到的"1200 tick 降到 181 ⇒ 衰减"**都是单点读数**——真实形态是：

   | 量（tick 150–1200，22 点） | 均值 ± 1σ | 极差 | 形态 |
   |---|---|---|---|
   | KE(J) | **102.5 ± 9.2**（±9%） | 89.3–125.9 | tick ~150 起**硬平台**，1000 tick 不动 |
   | 仅角速超阈 | **222.5 ± 40.8**（±18%） | 151–291 | 周期 ~250–300 tick 的振荡 |

   ⇒ ① **是极限环、不是衰减也不是增长**：KE 自 tick ~150 起稳在 ~100 J ⇒ **注入与耗散
   长期平衡**（"有持续注入"成立），但**有界**（无失控）；
   ② 窗口均值：前半段（150–650）241.5 → 后半段（700–1200）203.5，差异 **t≈2.2**（勉强显著），
   即只有很弱的慢衰减倾向，**不足以支撑"增长"叙事**；
   ③ **教训（已落到仪器）**：末态只打一次的分解会把波动读成趋势——两人（档与人）都栽在这上面。

   **重投影的最终裁决（窗口均值，tick 600–1200 × 13 点，同会话 A/B）**：

   | 窗口均值 | 基线 | 带重投影 | 差 | 显著性 |
   |---|---|---|---|---|
   | 仅角速超阈 | 210.8 | 205.8 | **−2.4%** | t≈0.3（不显著） |
   | 仅线性超阈 | 168.4 | 151.2 | −10% | t≈1.9（边缘） |
   | 双超 | 35.1 | 33.5 | −4.6% | — |
   | 超阈总数 | 414.3 | 390.5 | −5.8% | — |
   | KE(J) | 98.75 | 94.7 | −4.1% | — |

   ⇒ **角向收益在窗口均值下消失**（−30%@300、−16%@600 都是**单点**读数，落在 ±18% 波动带内，
   无法与"振荡相位不同"区分）⇒ **裁决：不落地**（此前的"混合账面"结论方向正确，但依据升级为
   "无可靠效应"）。
   ⇒ **判据修正**：单点判据（曾拟"601 tick 角速超阈不增"）在 ±18% 波动上**无效**；
   一律用**窗口均值 + 波动带**（例：600–1200 均值）。
   ⇒ **仪器已落地**：金样每 50 tick 随行打印 `超阈 线/角/双`（`over_threshold`，与末态分解
   **同源**，两处不会再漂开）。注：`gold-sample` 有**既有** clippy 红灯
   （`build_vxl` 的 `too_many_arguments`，第 66 行附近），**与本次改动无关**（改动 hunk 全在 196 行后）。

   **子步标度重测（窗口均值，塔 tick 600–1200，inner=1）——两类残差收敛行为不同，角向有地板**：

   | substeps | KE(J) | 仅线性 | 仅角速 | 双超 | 超阈总数 | 每翻倍增益 |
   |---|---|---|---|---|---|---|
   | 8 | 315.2 | 561.3 | 468.0 | 509.7 | **1539** | — |
   | 16 | 98.75 | 168.4 | 210.8 | 35.1 | **414** | ×3.7 |
   | 32 | 43.85 | 18.2 | 116.7 | 18.8 | **154** | ×2.7（**减速**） |

   ⇒ ① **线性残差随子步加速崩塌**（561→168→18，比值 3.3× → 9.3×）；
   ② **角向残差反而减速**（468→211→117，比值 2.2× → 1.8×）⇒ **趋向与子步无关的地板**
   ⇒ 角向部分**不能靠旋钮消除**、必须改机制——这是"旋钮路线不可行"的更锐版本
   （此前把两类混在一起说，会让人以为全部残差都只是 dt 问题）；
   ③ 总超阈体数的**增益比在减速**（3.7→2.7）⇒ 趋于非零地板；配合成本（sub32 ≈ 2× sub16
   ⇒ 外推到 ×256 子步 ≈ 2 s/活跃tick）确认旋钮路线不可行。**本表用窗口均值重算过，不是单点外推。**
   ④ ⚠️ **`16/1/4` 会发散**（KE **44 万 J** @1200、y 带到 **−358**）⇒ **`inner=1` 需要足够的子步**
   ——此前"inner=1 伤稳定性的风险被否掉"**只在 substeps=16 测过**（范围修正）。与 μ=5 的
   "`16/1/16` 垮、`16/2/8` 完好"合起来看：**子步的稳定区随 μ 移动（μ=0.5 需 ≥8；μ=5 在 16 已破）
   ⇒ `substeps=8` 是跨 μ 的稳健点**，与第 1 条 `16/2/8` 的推荐**互相印证**。

   ✅ **残差的来源已定位到「切向漂移回拉」——它同时是角向地板的成因、也是位置的守护者（2026-09-20，单变量判别）**：
   旋钮 = `DRIFT_BIAS_SCALE`（1.0 → **0.0**），塔 `16/1/16`：

   | 塔 2000 tick | 基线 | **drift=0** |
   |---|---|---|
   | 仅角速超阈 | 202 | **0** |
   | 仅线性超阈 | 155 | 107 |
   | 双超 | 49 | **0** |
   | 超阈总数 | 406 | **107（−74%）** |
   | KE(J) | ~105 | ~41（−61%） |
   | **末态 Δpos** | **0.0794** | 0.1440（**+81%**） |
   | ms/tick | 154.2 | **115.7（−25%）** |

   三场景（drift=0）：col45 600 45/45 睡、Δpos **0.0049**（基线 0.0118，更准）·
   pile5 600 **2000/2000 睡**（基线 1980）、Δpos **0.0052**（基线 0.0157，更准）·
   塔 1200–2000 **有体入睡**（200–663，基线 0）、y 带完整、最深 **0.006**（基线 0.008–0.009）。

   ⇒ ① **角向残差的成因就是它**：关掉后角向超阈 202 → **0**（不是减少，是**消失**）⇒ 上一块
   "角向趋向与子步无关的地板、必须改机制"**方向对、措辞要修**——那块地板就是这一项，
   而它是**可关的**（不是 dt 问题：子步动不了它，是因为它的量级不随 dt 缩）。
   ② **但它是真取舍**：关掉后塔的**位置精度退 81%**（0.0794→0.1440）——回拉的本职正是
   抑制长期蠕动；col45/pile5 反而更准 ⇒ 蠕动问题主要在**高塔**。
   ③ **与"整组 staged 语义"缺口接上了（重要）**：本仓缺"趟间位置积分"，现在是用**速度级**
   回拉（drift 目标速度）**代偿位置修正** ⇒ **代偿的代价就是它注切向速度（进而注自转）**
   ⇒ **"角向地板"与"staged 语义缺失"是同一件事的两面**。
   ④ **下一步（比整组重构窄得多）**：把这条修正从"速度目标"改成**扭矩中性**形态——
   位置级修正（直接位移，即 Rapier 那趟的等价物）或施加时**不产生净转矩**（沿两体连线/质心等效）。
   判据：**在 Δpos ≤ 0.0794（2000 tick）不劣的前提下**，角向超阈窗口均值下降 >2×SEM。
   ⑤ **旧否证不复现**："关闭时塔在 t≈1400 升级为弹射"在当前代码状态下**不成立**
   （drift=0 跑到 1950 tick 仍稳定、y 带完整）——那条记录的是**当时**的实现
   （之后落地了回退半径 1×skin、深度跳变门/分离门、速度充气视野等）。**又一次"否证=时间戳"。**
   ⑥ 若要把 `DRIFT_BIAS_SCALE` 的改动**升默认**：按 ADR 0004 换代哈希 + 金样三场景 +
   稳定性判据 + arena 19/0/0 + `m0_gates`（本轮未跑门，**默认档未动**）。

   ❌ **中间档与限速都试过、都否证（2026-09-20，同一 2000 tick 口径）**：

   | 变体 | 超阈总数 | 仅角速 | Δpos(2000) | 说明 |
   |---|---|---|---|---|
   | `scale=1.0`（基线） | 406 | 202 | **0.0794** | 位置最准 |
   | `scale=0.5` | 300 | 179 | 0.0973 | 只买到 26% 残差改善、丢掉 23% 位置精度 |
   | `scale=1.0` + **目标限速 0.05 m/s** | 424 | 183 | **0.1605** | 角向几乎不动、位置更差 |
   | `scale=0.0` | **107** | **0** | 0.1440 | 残差最小、位置最差 |

   ⇒ ① **没有可用的中间档**：响应是**阈值型**而非比例型（0.5 与 1.0 的角向几乎同档）；
   ② ⚠️ **"目标速度无上限 ⇒ 摩擦锥常驻饱和"这一假设被自己的实验否证**：把目标限速到
   0.05 m/s（远低于锥内可交付量）后角向 202→**183**（几乎不动），而 Δpos 反而更差
   （0.0794→**0.1605**）——若真因是锥饱和，限速应当显著见效；
   ③ **修正后的机理（问题不在大小、在性质）**：回拉要求接触点有一个**非零的相对切向
   **滑动速度**（`trhs`），而这个被"命令"出来的滑动在接触力臂上会**部分实现为绕接触点的
   自转**（0.05 m/s 的滑动配 0.25 m 力臂 ⇒ ω≈**0.2 rad/s = 角阈值的 4×**）
   ⇒ **任何非零 scale 都会维持它**；`scale=0` 之所以干净，是因为它把接触变成**纯静摩擦
   （零相对切向速度）**，不是因为命令变小了。
   ④ ⇒ **这条修正必须从"速度级"改成"位置级"**（＝ Rapier 那趟"趟间位置积分"的等价物）。
   **限速/缩放都是死路（两条已实测否证）**；位置级修正**不做功 ⇒ 不注速度**。

   ✅ **长跑复核（2026-09-21，`16/1/64`，塔 **2400 tick，已跑完**）**：
   末态 **仅线性 0 / 仅角速 3 / 双超 0、阈值下 **2497/2500****、`max|Δpos|` **0.0659**
   （**优于**基线 0.0794）、KE **4.7 J**、y 带完整 [0.24,12.15]、最深 0.006；
   **入睡数单调爬升**：1500→275、2000→325、2100→425、2200→500、2300→525、2350→525、2400→**550**。
   ⇒ ① 塔在**收敛入睡**（2400 tick 时 550/2500 且仍在升），不是停在"部分入睡"的平台；
   ② **`16/1/64` 未见发散**——`inner=1` 的发散在**低子步侧**（见 `16/1/4`），高子步侧 2400 tick
   y 带与穿透均正常 ⇒ 该档的稳定性风险**初步解除**（仍须过正式长跑门）；
   ③ ⇒ **P1 的剩余确认为"标定量级"**：找全睡所需的 tick/子步档 → 过落地门（长跑稳定性 + 四哈希 + 金样 + arena）。
   **勘误**：本条曾记为"跑到 2250 被中断、无末态行"——**那是我误判**（后台任务实际跑完了 2400 tick、
   末态行如上）；已按真实读数改写。

   ✅ **剩余阻塞已换形态（2026-09-21，从已有长跑数据读出，未新跑）**：`16/1/64` 的长跑里
   **tick 2150 时超阈 = 0（线 0 角 0 双 0）而塔仍未全睡**（入睡 450→500→525→550 慢慢爬），
   超阈数在 **0–6 之间闪**。
   ⇒ 结合本仓**岛级原子入睡**规则（整岛成员须**连续**低于阈值满 `sleep_time`）：
   现在卡住的**不是残余幅值**（已降到 3–6 体、且能瞬时归零），而是**闪烁**——少数体反复
   跨过角阈值 ⇒ 大岛的 `sleep_time` 窗口**永远被打断** ⇒ 岛上其余 ~1900 体陪着不睡。
   ⇒ **P1 剩余的两条路（性质不同，须区分）**：
   ① **把残余压到"舒适低于阈值"**（不再闪烁）：继续加子步/换档 —— 属标定，成本是代价；
   ② **改睡眠规则允许"岛内子块入睡"**（Rapier 允许，本仓原子）—— 属**行为改变**，
   须过四哈希 + 金样 + 稳定性判据（台账早已把它列为替代路线，现在它成了**主路之一**）。
   ⇒ 换句话说：**"塔不入睡"这条缺口，其残余侧已经基本解决；剩下的是"残余的尾巴"或"睡眠规则"**。

   **【外部方案评估】四条"终极方案"逐条对账（2026-09-21，依本会话实测）**：
   外部建议（XPBD/warm start · 迁 Bullet3/Jolt · 分布式子空间多世界 · 定点数+ECS）
   **共同前提与本仓实测不符**，逐条：

   | 建议 | 前提 | 本仓实测对账 | 结论 |
   |---|---|---|---|
   | 换 XPBD、**必须开 Warm Starting** | 强耦合不收敛 | **暖启动早就开着**（`WarmPoint` 缓存 + 跨子步复用 + 特征匹配 + 近邻回退）；且本会话实测暖启动**不是元凶**（重投影窗口均值 −2.4%、不显著） | 已满足，无事可做 |
   | 残余＝**浮点精度**反复横跳 ⇒ 定点数 | 1e-7 级抖 | 残余是 **~100 J 的宏观确定性极限环**（KE 102.5±9.2 J、1000 tick 不动），且已定位为**语义**错误（把真实滑移当数值漂移回拉、∝ μ 注转矩） | 前提不成立；定点数不会改善，而粘着门已把它从 202 降到 3 |
   | **岛太大** ⇒ 拆成多个物理世界 | 全岛同生共死 | **唯一命中真问题的一条**——但命中的是**睡眠规则**而非"要拆世界"：tick 2150 超阈已归零而塔仍未睡 ⇒ 卡在**闪烁打断 `sleep_time` 窗口** + **岛级原子入睡** | 方向对、方案过大：正解是**允许岛内子块入睡**（Rapier 先例），不是分布式多世界 |
   | 迁 Bullet3/Jolt / 重写底座 | 本仓解算不行 | 缺口只剩 **3 体**；迁移会丢掉**差异化资产**：跨平台哈希门、确定性域、T4 岛并行 4.4×、A9 形状保真 arena 19/0/0；且本仓已有纪律——外部引擎的"整组语义"**不可单件搬**（已四次验证） | 不建议 |

   ⚠️ **分布式子空间 + 幽灵刚体 + 异步冲量传递**会**直接违反本仓的确定性承诺**（ADR 0004 跨平台
   哈希门 + "岛间无耦合"已证明）——竞态与时空不一致正是本仓用确定性换掉的东西 ⇒ **不可采纳**。
   ✅ **要外部验证就用现成的仪器**：PhysArena 自检（19 探针 × 9 引擎，**本机可跑**）已经把本仓与
   Jolt/Bullet/Havok/PhysX/Rapier 同场对比 —— 比迁移便宜四个数量级。
   ⇒ **建议只做两件小的**：① 把残余尾巴压到舒适低于阈值（标定）；② 睡眠规则允许**岛内子块入睡**
   （＝上表第三条的**正确且小得多**的实现）。

   5. **残差买的是"刷新率"不是"扫掠数"（2026-09-20，塔 300 tick，**总扫掠数固定 256** = `iters 16 × inner × substeps`）**：

   | 配置 | \|v\|max | KE(J) | 仅线性超阈 | 仅角速超阈 | 双超 | 阈值下 |
   |---|---|---|---|---|---|---|
   | A：`inner=4 × substeps=4`（重复为主） | 0.305 | 905.4 | 510 | 269 | 1427 | **294** |
   | B：`inner=1 × substeps=16`（重线性化为主） | **0.093** | **125.9** | 290 | 235 | **62** | **1913** |

   ⇒ 同样 16 次扫掠，B 的 KE 低 **7.2×**、"阈值下"多 **6.5×** ⇒ **残差由"重新线性化的次数"决定，
   与"同一线性化扫几遍"关系很小**。
   **与 `DESIGN-staged-solver` §7 的关系（重要区分）**：§7 的结论「稳定性靠 inner 而非子步」测的是
   **线性稳定性**（塔不崩、|v| 带完整）；本条测的是**残差幅值**。两者不矛盾——**inner 买"不崩"、
   重线性化买"残差小"**。
   6. **刷新单价的适用范围 + 改用决定量（2026-09-20，塔 300 tick，`inner=1`）**：

   | 子步 | inner | 每迭代扫掠（子步×inner） | 总扫掠（×iters 16） | \|v\|max | 阈值下 | **超阈体数** | ms/活跃tick |
   |---|---|---|---|---|---|---|---|
   | 4 | 4 | 16 | 256 | 0.305 | 294 | 2206 | 105 |
   | 16 | 1 | 16 | 256 | 0.093 | 1913 | 587 | 131 |
   | 32 | 1 | 32 | 512 | **0.568** | **2223** | **277** | 294 |

   ⇒ ① **"刷新单价是瓶颈"（第 5 条）对 10 万体成立、对塔不成立**：塔从 16→32 子步只 +163 ms
   （≈5 ms/次刷新）——旧结论的适用范围要标清；
   ② **高效前沿在"多子步 + 少内层"**：(16,1) 比 (4,4) 只贵 25%，**超阈体数少 3.8×**
   ⇒ 睡眠目标下这是更好的工作点，**默认档配方值得按此重排**（仍需过四哈希 + 金样 + 稳定性判据）；
   ③ ⚠️ **`|v|max` 是脆弱的离群指标**：32 子步时反而飙到 0.568（少数体瞬时跑飞），而**超阈体数**
   才是入睡的决定量（2223 为该轮最佳）⇒ 金样表的 `|v|max` 列要这么读，后续以超阈体数为准；
   ④ **外推（按决定量）**：超阈体数每翻倍子步约 ×0.5（2206→587→277）⇒ 压到 0 还需 ~8 次翻倍
   （≥2 s/活跃tick）⇒ **"旋钮路线不可行"成立**，且这次用的是决定量而非离群值。
   ⑦ **候选配方三场景验证已过（2026-09-20）**：`iters/inner/substeps = 16/1/16` 下
   col45 **45/45 全睡**（与现行一致）、pile5 **1980/2000**（较现行 1735 **多 245 体**、
   KE 9.9→**0.1**）、tower25 超阈体数 2206→**587**；"inner=1 伤堆叠稳定性"的风险**实测否掉**；
   代价 **+25%**（105→131 ms/活跃tick）⇒ **不是取舍，是残差与入睡两列同时变好**。
   建议升为参考配方（须在此配方下**重建基线**，旧读数按历史保留；对照表与判据在 `RECIPES.md`
   金样段）。

**已排除清单（详见 EXPERIMENTS.md S1–S19）**：两条硬限定 = 距离类判别式全部失效、
全局开关不可能（需求梯度 tower > m0_gates ≫ col45/pile5）；本轮新增：恒真式判据
（S14）、切向锥饱和门（S15）、未承载不回拉复测不复现（S17）、**全拒仍不睡 ⇒ 匹配线
整体关闭（S19）**。

**下一轮入口（2026-09-20 二次重排；旧 1/2 已由 `SESSION-2026-09-20.md` §3 收口）**：
0. **本会话交接档 = `SESSION-2026-09-20.md`**（13 个 commit 的产出/否证/归因修正 + 门禁状态；
   另：本条"重投影取舍"已有 **600 tick 耐久性 + 全向量判别**读数，动手前先读上面的复测块
   ——**不要再用 300 tick 的 −30% 当决定依据**）。
1. **【新，最高优先】参考配方的**高摩擦适用域**：`16/1/16` 在 **μ≥2.5 起垮**（col45 亚秒级
   复现：KE 6752、y 带 2.25→0.80），而同为 256 扫掠的 **`16/2/8` 完好**（45/45 睡、KE 0）。
   **`16/2/8` 在 μ=0.5 的读数已补（2026-09-20，`RECIPES.md` 金样段）**：pile5 **2000/2000
   满睡**（优于 16/1/16 的 1980）、塔超阈 1476（差于 483、好于旧配方 2206）⇒ 它是
   **Pareto 更优的参考候选**（塔残差与重线性化次数单调：2206→1476→483）。
   **两条配套新读数（2026-09-20）**：① ⚠️ **`16/1/4` 会发散**（KE 44 万 J ⇒ `inner=1`
   需要足够子步）；② 子步的稳定区**随 μ 移动**（μ=0.5 需 ≥8、μ=5 在 16 已破）
   ⇒ **`substeps=8` 是跨 μ 稳健点**，与 `16/2/8` 互为印证。
   配方刚升参考 ⇒ **升默认前必须**：① 定一档并标 μ 上限；② 把 μ 档加进 `RECIPES.md`
   的金样判据（现行判据只测 μ=0.5，**测不出这个洞**）。
2. **【本轮最高优先】落地「只对粘着接触回拉」**（已验证的候选修法，见上方 ✅ 块；**不是探索，是收尾**）：
   改动约 6 行、全场景同时变好（pile5 满睡、塔角向 −54%、位置精度反而提高）。
   **要做的**：① 跑 arena 自检 19/0/0（本环境跑不了 ⇒ 需能跑的机器/会话）；
   ② 按 ADR 0004 换代四哈希并**显式记新旧值 + 原因**（门槛已知 `0x6219d186…` → `0x6a932b44…`）；
   ③ 更新 `default_tier_stability` 四个冻结值——当前实测堆顶 y **2.7285047**（冻结 2.7223，+6.2 mm），
   需判定该漂移可接受；④ 记入代价（塔 +3.6%）与 `m0_gates` 瞬态深度变深（0.2561→0.6587）。
   **之后再看**：这个门是否会顺带改变"角向地板"的结论（角向从 202 降到 93 但仍非 0）
   ⇒ 剩余角向可能与**粘着接触**的回拉有关（下一步把门收紧到"更严格粘着"或查剩余注入）。
   ✅ **【已落地 2026-09-21】「只对粘着接触回拉」——全场景同时变好，不是取舍（单变量判别）**：
   改动＝给 `drift_eff` 加一个门（约 6 行）：滑动接触的锚点分离是**真实材料滑移**，回拉等于
   抹掉真实滑动、且 ∝ μ 地注转矩；只有**粘着**接触的分离才是数值错位。
   粘着/滑动用**上一子步的暖启动冲量**判定（**零额外状态**）：`|pt| ≥ 0.99·μ·pn ⇒ 滑动 ⇒ 不回拉`。

   | 场景 | 指标 | 基线 | **只粘着回拉** |
   |---|---|---|---|
   | col45 600 | 入睡 / Δpos | 45/45 / 0.0118 | **45/45 / 0.0048** |
   | pile5 600 | 入睡 / Δpos | 1980/2000 / 0.0157 | **2000/2000 / 0.0059** |
   | tower 600 | 超阈总数 / Δpos | 483 / 0.0863 | **247（−49%） / 0.0751** |
   | tower 2000 | 线·角·双 ＝总数 / Δpos | 155·202·49＝406 / 0.0794 | **147·93·29＝269（−34%，角 −54%） / 0.0818** |
   | `m0_gates` | 末态活跃 | 51 | **4** |
   | `m0_gates` | **瞬态**最大深度 | 0.2561 | 0.6587（**变差**） |
   | 代价 | ms/活跃tick（塔 600） | 135.6 | 140.5（+3.6%） |

   ⇒ **本线第一个"不是取舍"的改动**：残差与位置精度**同时**变好（pile5 满睡、col45/pile5 更准、
   塔角向 −54%）；唯一混合项是 `m0_gates` 的**瞬态**最大深度变深（末态深穿透仍 0）。
   机理自洽：滑动接触的回拉力矩在净注入转矩（∝ μ）——关掉它角向残差立刻减半；真实滑移不再被
   "抹掉" ⇒ 位置精度反而提高。这也印证了诊断 (b)：**速度级回拉对"真实滑移"的接触是有害的**。
   **门禁现状（已实测）**：`cargo test --release` **只有冻结读数门失败**
   （`default_tier_stays_stable_on_long_run`：堆顶 y **2.7285047 vs 冻结 2.7223（+6.2 mm）**），
   其余 25 项 + **金丝雀**全过；`m0_gates` PASS、双跑一致、arena 平稳，**门槛哈希换代**
   `0x6219d186…` → **`0x6a932b44…`**（压力/确定性哈希未测）。
   **未做**：~~arena 自检 19/0/0~~ → **后续已跑通并通过，见下**（原判断"本环境跑不了"是错的）。
   ⇒ **状态：已落地（2026-09-21）**。ADR 0004 换代完成并已记录（门槛/压力/确定性三哈希与默认档
   四个冻结值已更新，见 `RECIPES.md` 的「2026-09-21 换代」段）；五项门禁链全绿；
   金样三场景 + arena 19/0/0 + `m0_gates` PASS。
   **仍待办（不影响落地）**：T4 扩展比须在空闲机复测——**基线自己也只读 2.29×**（本改动 2.70×），
   属环境而非本改动；其正确性判据（串行/并行哈希逐位一致）已通过。
   **门阈值扫描（单变量，塔 600 tick）——0.99 在每个轴上最好，且回答了"剩余角向从哪来"**：

   | 阈值 | 仅角速超阈 | 仅线性超阈 | 双超 | 总数 | Δpos |
   |---|---|---|---|---|---|
   | 1.00（＝基线，门形同虚设） | 261 | 191 | 31 | 483 | 0.0863 |
   | **0.99（候选修法）** | **122** | **111** | **14** | **247** | **0.0751** |
   | 0.90（更紧） | 137 | 158 | 20 | 315 | 0.0830 |

   ⇒ ① **收紧门不是继续降残差的方向**（0.90 全面变差）；
   ② **剩余角向来自"锥内接触"而非近锥接触**——既然"少回拉一些接触"会让残差变差，
   剩下的角向就是**回拉合法作用域内部**的注入；
   ③ ⇒ 下一步**不是调门，而是换施加方式**：对**粘着接触**改用**位置级**修正（速度级**命令**
   才是自转来源——位置级单独测时角向曾是 **1** vs 基线 261）。这与被否证的"无条件传送"版
   **不同**：门保证只对**数值漂移**（且是锥内接触）动手，避开了"抹掉真实滑移"那条死因；
   仍需解决"按体限幅"（先前诊断为净平移）。

   **位置级 × 速度级的四种组合（塔 600 tick，同口径）——"并存"否证、位置级可换残差但丢位置**：

   | 变体 | 仅角速超阈 | 仅线性超阈 | 总数 | Δpos | 稳定性 |
   |---|---|---|---|---|---|
   | 基线（速度级、无门） | 261 | 191 | 483 | 0.0863 | 稳 |
   | **速度级 + 粘着门（0.99）** | 122 | 111 | **247** | **0.0751** | 稳 |
   | 位置级（只修小分离 ≤2 mm + 按体限幅 0.5 mm/子步），速度级关 | **0** | 132 | **134** | 0.1183 | 稳 |
   | 速度级 + 位置级（**并存**） | — | — | — | **5.0562** | **发散** |

   ⇒ ① **"先与速度级并存"已否证**：两条同时开发散——它们互相打架（速度级命令接触滑动，
   位置级又把体拉回来）；
   ② **位置级单独**（小分离门 + 按体限幅）能把**角向清到 0**、总数 **134**（远优于速度级门的 247），
   且**按体限幅确实消除了先前那种净平移发散**；**但位置精度退到 0.1183**（基线 0.0863）
   ——限幅把修正的**权限**压得太小、跟不上蠕动；
   ③ ⇒ **当前最优是"速度级 + 粘着门"**（残差与位置**同时**优于基线）；
   位置级是"残差更小 / 位置更差"的另一个 Pareto 点：要它同时保住位置，需**放开权限**
   （更大的 `SEP`/`MAX`）并逐档重测——但权限放大会回到先前"净平移/发散"的风险区。

   **❌ 位置级路线的权限窗口不存在（2026-09-21，同一变体上扫 `SEP`/`MAX`）**：

   | 分离门 `SEP` / 每体上限 `MAX` | 稳定性 | 仅角速 | 总数 | Δpos |
   |---|---|---|---|---|
   | 2 mm / 0.5 mm | 稳 | **0** | **134** | 0.1183 |
   | 4 mm / 1 mm | **发散** | 42 | 2371 | **19.56** |

   ⇒ **稳定边缘离发散只有 2×**，而在稳定边缘上位置精度已比基线差 37%（0.1183 vs 0.0863）
   ⇒ **位置级路线（本形态）没有可用工作点**：保住位置所需的权限落在稳定窗外。
   ⇒ **位置级探索到此关闭**（四种组合 + 权限扫描均已实测）；**"速度级 + 粘着门"是唯一
   两项都优于基线的变体**，落地清单见上。

   **换代哈希读数已备齐（2026-09-21，候选修法下实测；ADR 0004 落地记录可直接引用）**：

   | 哈希 | 基线（现冻结） | **候选修法下** |
   |---|---|---|
   | 门槛（`m0_gates` 600） | `0x6219d1866b20c002d806d3d699a487ff` | **`0x6a932b44622d9bb5b06cd2d6005c9c64`** |
   | 压力（`m0_gates` 120） | `0x63e5eb35b71b8c84ade4a053aeecb900` | **`0x417be20a8e49c9b0436987415ac9961a`** |
   | 确定性（`determinism`） | `0xd8601988ad7989ffb58ba8b956c2f8db` | **`0x7acbdfac46b03aaaebc04ca0c30bfc4f`** |
   | T4 碎片雨（`m1_islands`） | 串行/并行一致 | `0xb27cd6ffab97b4a67c23fb7db8d217d3`，**串行/并行逐位一致 ✅** |

   门槛场景健康：末态活跃 **51 → 4**、瞬态 79 → 89 tick、**最大深度 0.2561 → 0.6587（变差）**、
   睡醒翻转 2 → 3；PASS、双跑对拍一致、arena 平稳。
   ⚠️ **T4 扩展比读数 2.70×（判据 ≥3×，档内记录 4.39×）判 FAIL**——但那是**计时量**
   （解算 9.7 ms / 3.6 ms），按本仓协议（"性能类峰值须同机同状态同窗口复测"、环境波动 ±28%）
   **须在空闲机复测后再归因**；其**正确性判据（串行/并行哈希逐位一致）通过**。
   ⇒ **落地仍缺**：① ~~arena 自检~~ **已通过（19/0/0，逐探针与基线相同）**；
   ② `default_tier_stability` 四个冻结值更新——堆顶 y **2.7285047 vs 2.7223（+6.2 mm）** 待你判定；
   ③ T4 扩展比在空闲机复测（**基线同样读 2.29×**，属环境）。

   ✅ **arena 自检已跑通（2026-09-21；此前"本环境跑不了"的判断是错的，特此更正）**：
   管线在 `physarena` 侧、**node 驱动**、本机可跑：`npm run build:vxl`（从本仓构建 wasm 桥）
   → `npm run build` → `npx vite preview --port 4173` → `ARENA_TAG=<tag> node scripts/arena-drive.mjs selftest`。

   | 运行 | vxl-phys 结果 | 备注 |
   |---|---|---|
   | 基线（引擎 HEAD） | **19 pass / 0 degraded / 0 fail** | 与档内基线一致 |
   | **候选修法** | **19 pass / 0 degraded / 0 fail** | **19 项探针的 verdict 与基线逐项相同**（仅 `bootMs` 计时不同） |

   **正向对照（证明测到的是新构建）**：wasm 产物哈希随引擎源码变化 —
   候选修法 `e020f38e…` vs 基线 `d8dcebd3…` ⇒ 该自检确实跑的是新引擎，不是缓存包。
   ⇒ **"行为改变落地四条件"里的 arena 一项已通过**。

   ⚠️ **T4 的 FAIL 已归因（同日，基线对照）**：**基线本身也读 2.29×**（候选修法 2.70×），
   二者都低于判据 ≥3×、也都低于档内记录的 4.39× ⇒ **是环境/机器状态，不是本改动**；
   其**正确性判据（串行/并行哈希逐位一致）通过**。⇒ 该判据须在空闲机复测（基线同需）。

   ✅ **默认档质量对照（2026-09-21）——回答了"冻结值 +6.2 mm 能不能接受"**：
   `arena_bench pyramid --steps 3000`（默认档 `iterations=3 inner=1 substeps=2`，211 体）：

   | 指标 | 基线 | 候选修法 | 判定 |
   |---|---|---|---|
   | 最大穿透 | −0.0199 m | **−0.0199 m** | 相同 |
   | 堆顶 y | 19.426 | 19.420 | **−6 mm**（＝冻结门抓到的那 +6.2 mm） |
   | **末态动能 Σv²** | 1.4718 | **1.0517** | **−29%**（确定性量 ⇒ 抖动更小） |
   | 末态清醒 | 210/210 | 210/210 | 相同 |
   | p50 | 1.337 ms | **1.149 ms** | −14%（**计时量**，须同窗口复测） |

   ⇒ **+6.2 mm 是良性平衡点位移、不是退化**：它伴随**残差动能 −29%**、穿透不变、
   清醒数不变（p50 那一项是计时量，按协议须同窗口复测，不作结论）。
   ⇒ `default_tier_stability` 的四个冻结值可按 ADR 0004 **照实更新并记换代理由**
   （值得记的不只是"值变了"，还有"**换来的质量与代价**"）。

   ✅ **【拐点】落地后塔**开始入睡**——"必须整组 staged 语义"的前提被搬掉了（2026-09-21，落地后复测）**：
   落地粘着门后重跑子步标度（塔 1200 tick）：`substeps=32` 末态 **线 0 / 角 22 / 双 0、阈值下
   2478/2500**（修复前同口径窗口均值：角 **116.7**、总 **154**）；`substeps=64` 末态
   **线 0 / 角 8 / 双 0、阈值下 2492/2500**，且**入睡数持续增长**：tick 1000→**175**、
   1050→250、1100→275、1150→**300**、1200→**300**；Δpos 0.0669（sub32）/ 0.0777（sub64），
   均**优于**基线 0.0794。
   ⇒ **关键定性变化**：修复前塔在**任何**子步数下都是 **0/2500** 入睡（一个都不睡）；现在
   **有体入睡且递增** ⇒ 之前"塔要睡需要整组 staged 语义（多会话大改）"的判断，其**前提**
   （残余地板来自"逐子步注入"、且非旋钮可消）**已被今天的修复搬掉**。
   ⇒ **P1 剩余量级重估**：不再是"架构大改"，而是**配方 + 标定**——找让塔全睡的
   `substeps` 档（并在睡后享受"睡眠岛跳过"的成本坍塌），再按落地流程过门。
   ⚠️ 成本仍是代价（sub32 183.5 ms/活跃tick、sub64 约 2×），但**入睡后活跃 tick 归零**
   ⇒ 稳态成本与"全程活跃"不是一回事，须按睡眠后的活跃 tick 口径复测。
   ⚠️ `16/1/64` 的稳定性未做长跑验收（`inner=1` 低子步曾发散、见上），须先过长跑门。

   **落地形态（已进默认行为，commit `e76b9ad`）**：`vxl-phys-solver` 新增
   `const DRIFT_STICK_RATIO: f32 = 0.99;`，`drift_eff` 改为只在**非滑动**时生效：
   ```rust
   let sliding = warm_pt.is_none_or(|w| {
       (w.pt1 * w.pt1 + w.pt2 * w.pt2).sqrt() >= DRIFT_STICK_RATIO * mu * w.pn.max(0.0)
   });
   let drift_eff = if !sliding && drift.length_squared() > DRIFT_DEADZONE * DRIFT_DEADZONE {
       drift * (DRIFT_BIAS_SCALE * sp.inv_dt)
   } else {
       Vec3::ZERO
   };
   ```
   - ❌ **"直接把切向分离平移掉"的首版实现否证（2026-09-21；代码已回退，形态与读数留档）**：
     实现＝主趟、**岛求解之前**（`solve_phase` 内、拿 `&mut BodySet`）、同流形多点取**均值**、
     逆质量配权、**只平移不自转**、只修切向、只对**回收锚点**（分离 ≤ `RECYCLE_DIST`，与复用判定同门）。

     | 强度 | 稳定性 | 仅角速超阈 | 仅线性超阈 | Δpos | KE(J) |
     |---|---|---|---|---|---|
     | 1.0 | **发散** | 15 | 262 | **15.88 m** | 2 079 285 |
     | 0.05 | 稳（y 带完整、最深 0.017） | **1** | 242 | **0.1276** | 116.7 |
     | 基线（速度级 = 1.0） | 稳 | 261（600 tick） | 191 | **0.0863** | 103.5 |

     （1.0 行为 2000 tick 末态；0.05 与基线为 600 tick 同口径。）

     ⇒ ① **角向消除在位置级下依然成立**（角向 **1** vs 基线 261）⇒ 再次确认"速度级**命令**
     一个非零滑动"才是自转的来源；② **但位置级没把蠕动控制买回来**：0.05 时 Δpos
     **0.1276 > 基线 0.0863**，而把强度提到能起作用（1.0）就**发散** ⇒ **该形态否证**。
     ③ **诊断（两条，都是结构性的）**：
        (a) **无界投影 + 逐流形累加**——一个体有 ~8 个接触 ⇒ 每子步被平移 ~8 次
        （1.0 时可达 ~8 cm/子步，而 sub_dt ≈ 1 ms）⇒ 求解器对剧烈几何变化的反应本身就是注能；
        (b) **投影分不清"数值漂移"与"真实滑移"**——滑动接触的锚点分离**就是**真实相对运动，
        把体**传送**回去等于抹掉真实滑移；Rapier 那套是**受接触软度/最大修正量约束**的位置修正，
        **不是无条件传送**。
     ⇒ **正确形态＝摩擦/软度受限的相互投影**：每子步每体总位移要有上限（与 `max_corr`/slop
     同量级）、按**摩擦锥可交付量**限幅，且**先与速度级并存**（不是替换）再看能否同时保住
     Δpos 与角向。
     ✅ 顺带：默认关的路径**实测逐位中性**（`m0_gates` 哈希 `0x6219d186…` 不变、末态活跃 51、
     PASS）——"对既有场景逐位中性必须实测"的又一次执行。
   - ✅ **放置点是关键变量（同日，单变量）**：**同一份修正、同一强度（1.0），只改放置位置**：

     | 放置 | 稳定性 | 仅角速超阈 | 仅线性超阈 | Δpos |
     |---|---|---|---|---|
     | 岛求解**之前** | **发散**（t600 KE 80 854） | — | — | — |
     | 岛求解**之后**（写回/剪枝后） | **稳** | **3** | 678 | 0.1148 |
     | 基线（速度级 1.0） | 稳 | 261 | 191 | 0.0863 |

     （均塔 600 tick、速度级关闭以隔离位置级。）
     ⇒ ① **"先推位置再解"是发散的直接原因**——等于把剧烈变化的几何喂给求解器，它对变化的
     反应本身就是注能；放到**求解之后**（冲量已施加）就稳了 ⇒ **位置级修正必须放在求解之后**
     （与 Rapier "位置积分在速度解之后"一致）。
     ② **逐流形累加的问题转成了线性残差**：角向被清掉（**3** vs 261）而线性反而变差
     （678 vs 191），Δpos 仍不如基线（0.1148 vs 0.0863）⇒ 与诊断 (a) 吻合：一个体 ~8 个接触
     ⇒ 每子步被推 ~8 次、方向不一致 ⇒ **净平移**。
     ③ **下一个单变量＝按体限幅**：先按体累加、再把每子步总位移钳到上限（与 `max_corr`/slop
     同量级），预期消掉那部分净平移。快筛用 600 tick，定案用 `substeps=32` + 窗口均值。
   - ✅ **旧否证已解锁（2026-09-21，读码核对）**：档里"阶段 1 / 位置通道不可行"
     （`DESIGN-staged-solver.md` §5）**记的是当时的结构**，而 §7 缺陷③ 已把真因**修正**为
     「**几何/偏置一次烘焙到 tick 末** + 迭代间移动位置 ⇒ 冲量用**冻结力臂** ⇒ 注能（塔 KE 66 万）」，
     并写明「**不可行的是那个组合，不是 stage 结构本身**」。**该前提在当前代码里已不成立**：
     `World::substep()` **每子步**调一次 `solver.solve()`，而 `build_constraint` 每次都从
     **当前位置**重算 `ra = pt_world − pos_a` / `rb = pt_world − pos_b` 与深度/偏置
     （`crates/vxl-phys-solver/src/lib.rs:1175`）⇒ **几何逐子步刷新**（＝ §8 的 P2 已实质落地）。
   - **落脚点**：`World::substep()` 中 `Integrator::integrate_positions`
     （`crates/vxl-phys/src/lib.rs:1052`）**之后**加一趟窄修正；下一子步 `solve()` 从新位置重算几何
     ⇒ 不再是"冻结力臂 + 移动位置"。**需要的接口**：修正量在解算侧产生（漂移在 solver 的 warm 槽里）、
     位置在 `vxl-phys` 侧积分 ⇒ 要么 solver 导出定长缓冲（每点位移量），要么把位置积分下移进 solver
     （后者改动更大、且与"位置在解算中不变"的现行不变量冲突 ⇒ 优先前者）。
   - **形态（先窄后宽）**：只修**切向**分离（法向由 bias/depth 负责）、且**只平移不自转**
     （平移是扭矩中性的最小动作；自转修正留待需要时再加）。位移按逆质量配权、比例系数要留旋钮，
     首测取"每子步修正剩余分离的一小部分"（避免一次性投影引起抖动）。
   - **判据**：**Δpos ≤ 0.0794（塔 2000 tick）不劣** 的前提下，角向超阈 13 点窗口均值
     下降 > **2×SEM（≈11×2）**；三场景 + `m0_gates` 不回退。
   - **测量设置**：查角向时在 **`substeps=32`** 上测（线性已死、角向被隔离）；**禁单点**。
   - **形态基线（别按旧说法做）**：塔是**有界极限环**——KE 稳在 **102.5 ± 9.2 J**、
     角向超阈 **222.5 ± 40.8**、周期 ~250–300 tick；不是"稳态增长"。
   - **已排除**：暖启动基不匹配（重投影窗口均值 −2.4%，t≈0.3 不显著）。
3. **T2 候选过滤的访存局部性**（性能项，靶子已量化：32ns/条候选 × 均 35 万 / 峰 115 万）
   ——与前两条不同层，可并行推进。
4. **【已收口】复用余价的最后一环**：见 `SESSION-2026-09-20.md` §3.1（两候选均否证：额度
   重复发放三量 ±1% 内、复用后精确率反而 69.6%→76.1%；且末节 P 已证复用在塔上不省时）。
5. **【已收口】A6 窄相"特征稳定流形"**：见 §3.2（接触法向变化仅 0.1–0.3% ⇒ 参考面几乎
   从不翻转，churn 在编码层：侧别 21% / 换裁剪侧平面 56% / 裁剪点↔顶点 22%）。
6. **接触聚类裁点**（DESIGN 阶段 2）：**已否证**（点数是承重件，EXPERIMENTS 末节 J）。

**【已关闭】旧入口 1「staged 语义单件实验（先只做趟间位置积分）」**：该件已于
2026-09-14 **否证**（`DESIGN-staged-solver.md` §5 阶段 1：125 dE 18.2→34.4/77.5/176.2、
塔 |v| 0.236→18.2、|Δpos| 3.1 m；两个隔离实验见该节）。本条入口写于"第三段后"，
未随第六段之后的否证更新——**不要重做**。§9（P1 检测上提，塔崩 44.4）、§10（P2 复用
约束 + prepare，全变体崩）、M1 第十一段（把回拉拆成末趟，更差）同样已关闭。

**验收（判据保留，附口径注）**：
- 125 体复现 **dE ≤20 ✅ / 末态入睡 ❌**（入睡项需 staged 语义，非匹配侧）；
- col45/pile5 保持或超过落地版（35/45、**1700/2000**）；
- tower25 塔身完整且不退化（**0.236 / 0.0590** ✓）；
- ~~**m0_gates 末态活跃保持 0** ✓~~ **判据陈旧，按实测重写（2026-09-20）**：当前基线（哈希
  `0x6219d186…` 逐位一致）实测 **末态活跃 51**、`M0-GATES.md` 里写的"0（全睡）"不是当前
  读数 ⇒ 验收口径改为「**不劣于 51，且睡醒翻转 ≤2**」；切向重投影实测 27 / 1（更好）。
- 门禁：四哈希 + 金样容差表。**注**：性能类峰值须同机同状态同窗口复测
  （§9②：环境波动 ±28%）。

## P2 — T2 宽相峰值：33.95 vs ≤15ms（树 ≤5ms）

**现状**：`fix_upwards` 提前退出**已落地**（7b36caa：拆 `fix_upwards_grow` 供生长
路径、全链版留给 insert/remove；树峰 12.17→7.34ms、哈希逐位不变）。本次同窗口
复测（§9）：宽相 均 17.0 / 峰 43.5、树 均 2.65 / 峰 8.80、查询 均 10.0 / 峰 33.0
——但机器状态较 §5 记录整体 +28%（候选数不变）⇒ **判定须同机同状态**；按无量纲
看：候选 均 35.1 万 / 峰 115 万（与 §5 同）。查询峰 = 缓存翻转帧全量重查（固有）。

**已排除**：树形两版（W3/W4）、失效策略（W1）、边距**收窄**（W2/W7——解释已由 W9
修正）、fix_upwards 提前退出（W5——**已落地**）、候选过滤硬件预取（W8）、
盒快照（破坏缓存完备性：双侧发射依赖实时盒，未实现）。

**已落地（W9）**：边距速度项 K=1.5→**6**（`(base + v·dt·6).min(0.5)`）——拆段计时
把成本钉在「逃逸重查」（8.08ms vs 过滤 1.22ms）后，放大速度项实测 broad 均
17.03→**14.7–15.1**、树均 2.65→**1.79**、m0_gates p50 0.54→**0.37**，**逐位中性**。

**候选入口**：
1. **核「沉降期」口径**：验收「任一 tick 宽相 ≤15ms」的缺口全在**坍塌相**
   （t≈46–62：候选 115 万、窄相 30、解算 650ms）——该相是否属「沉降期」需与
   V2 §11 原文核（此前记录各任务验收均为相位级）。
2. 坍塌相本身的刷新量分档（逃逸体型处理）——仅在上项确认口径后开工。

**验收**：8B 沉降期任一 tick 宽相 ≤15ms（树 ≤5ms）；**注**：须同机同状态同窗口
复测（环境波动 ±28%，见 §9②）。

## P3 — 8B 最低档：口径已落定（SPEC §3）＝整 tick 30 FPS ⇒ **真实缺口 10×**（milestone 级）

**口径（本仓 SPEC.md §3，照抄）**：「刚体规模 最低通过 = **10 万动态+10 万静态，
简单碰撞，CPU，30 FPS**」（参考硬件 i5-13490F/10C16T）；稳定性判据 = NaN/Inf=0、
深穿透（>skin×4）=0、**休眠体重复唤醒 <1 次/秒/体**；已通过档位随里程碑复测，
**劣化 >10% 阻断**。

**实测（本机 8 线程，8B 密堆场景 `m1_scale`，120 tick）**：总均 330ms / 峰 769ms
（iters=16 配方）⇒ 距 33.3ms **9.9×**；iters=4 也要 178ms（5.4×）。
分解：解算 = **87ms 固定 + 12.75ms/迭代**；**降迭代已关闭**（质量侧实测：
iters=8 塔 KE 32.8 万 J、iters=4 塔崩 max|Δpos| 16.5m ⇒ 16 是配方）。
旁证：SIMD 上界已量 ≤10%、岛批量路线已关闭、T2/T3 已到各自地板。

**架构诊断与修复计划（2026-09-14 第九段，详见 DESIGN-staged-solver §7/§8）**：
三个结构缺陷已定位并有两个实锤 —— ① 稳定靠 inner（同一线性化重复扫掠）而非子步
（重新线性化）：**同 64 扫掠，子步型排列质量 ≈ 现行 256 扫掠配方（塔 |v| 0.256 vs
0.236）**；② 子步重复跑碰撞检测 ⇒ 便宜子步用不起来（8B 4 子步总均 607 vs 330）；
③ 几何/偏置一次烘焙到 tick 末 ⇒ 阶段 1 注能（§5 归因修正：不是「位置通道不可行」）。
修复主线 P1 碰撞检测上提 → P2 子步内重烘焙几何 → P3 结构换挡
（预期 8B 330→~110-150ms，≈2.2-3×；再往下需 P4 岛内并行/P5 聚类/SIMD 叠加）。

**其余两条路（备用）**：
1. **结构性**：整组 staged 语义 + 接触聚类（裁点）+ SoA/SIMD 全链——同时是 T1
   睡眠侧的解药；属大改，须单独立项排期（含 README 列的「约束图染色」）。
2. **口径**：核「简单碰撞」场景的定义——本仓 `m1_scale` 是**10 万体密堆**
   （接触密度极高）；若验收场景是稀疏接触（碎片/散布层），同一 FPS 数字的
   含义完全不同。**V2 原文不在本机**（`VoxelForge-V3/docs/…` 外部工程）
   ⇒ 需用户提供 V2 §11 原文或裁决。

**注意**：此前文档的相位预算（宽相 ≤15 / 窄相 ≤25）是自定分配、加不出 33.3ms，
**不是验收**——不要再用它们当达标依据（M1-PLAN §11）。

## P4 — T3 窄相峰值：28.68 vs ≤25ms（最接近达标）

**现状**：均 11.05 ✓；SSE2 SAT + 体轴缓存已落地（逐位透明）；剩余 13% 触
「标量算术 + 访存」地板（减轴/改迭代序都属行为变化）。

**候选入口**：核口径（同 P3）；或 4 点裁剪专用路径（逐位透明要求下再挖）。

## T4 — 碎片雨并行扩展：**本机饱和在 ~2.5×，门的 ≥3× 在本机结构性不可达**（2026-09-21 实测，未改门）

**为什么查**：`m1_islands` 在本机读 1.67×（并行跑）～3.08×（离群），跨在门的 ≥3× 线上；
按"性能类门要机器级独占"的既定纪律先量清楚，**不急着改门**（用户口径：看看情况再加）。

**机器**：Intel Core Ultra 7 270K Plus，**8 P + 16 E / 24 逻辑核**、1 NUMA、36 MB L3。

**① 线程曲线**（4000 簇 / 120 tick / 解算相位比）：

| 线程 | 2 | 3 | 4 | 6 | 8 | 16 | 24 |
|---|---|---|---|---|---|---|---|
| 解算扩展 | 1.62× | 1.82× | 2.34× | 2.38× | **2.50×**（5 次均值） | 2.52× | 2.46× |

⇒ **4 线程后饱和，再加线程无收益（16/24 略降）**。

**② 岛数曲线**（8 线程）：

| 簇数 | 1000 | 2000 | 4000 | 8000 | 12000 |
|---|---|---|---|---|---|
| 解算扩展 | **0.97×** | 2.01× | 2.31–2.75×（5 次均值 2.50） | 2.33–2.85×（均值 2.61） | 2.52× |
| 求解 ms/tick | 2.54 | 3.30 | 6.10 | 11.74 | 16.02 |

⇒ ⚠️ **1000 簇的 0.97× 不是"没加速"，是没进并行分支**：求解器的并行门槛是
`manifolds.len() < 4096 ⇒ g_count = 1`（设计如此）。**加大工作量也抬不上去**（8000/12000 与 4000 同档）
⇒ 既不是"活不够"，也不是"固定 per-tick 开销"（该假设已被岛数扫描否证）。

**③ 平台 fork-join 开销**（新增到 `m1_islands` 的读数）：本机 **191.7 µs/次**（每次开 7 个 scoped
worker、空活）⇒ 每 tick ≈ 0.38 ms（substeps=2），相对 6–16 ms 的解算**不是限制项**
（求解器注释里"≈90 µs/个"的估计偏保守，本机约 27 µs/个）。

**④ 相位占比**：解算 **67–71%** 的 tick（其余：宽相 ~19–21%、窄相 ~7–9%、积分 ~3%）⇒
**门的分子/分母口径没问题**，并行区就是大头。

**结论**：本机上 `≥3×` 在该场景**结构性达不到**（饱和 ~2.5×，分布 2.31–2.85× 且跨线），
而记录值 4.39× 来自**另一台机器**（档里未记机器型号——这本身是个缺口）。
**⚠️ 不是回归**：串行/并行末态哈希逐位一致；且本会话改动都在提供者层/文档/测试，
未触碰求解器并行路径（`JobSystem::for_each_range` 全仓**无调用点**——求解器只借它读线程数、
自己用 `std::thread::scope` 分组，顺便记一笔：该注入接口目前是死接口）。

**"再加"的候选（按性价比排序，待立项）**：
1. **门口径**：记录每台机器的基线区间（如本机 2.3–2.9×）并按"相对本机基线的退化"判定，
   或把测量搬到**空载机 + 更高簇数**（本次已证簇数不能提升比值，故优先级低于 2/3）；
2. **找饱和真因**：① 饱和与线程数、岛数都无关 ⇒ 指向**负载不均/共享内存竞争/混合核调度**；
   下一步是给求解器的分组加**每组耗时探针**（看是不是某组长期收尾）或上采样 profiler；
3. **接口收编**：让求解器的并行循环走注入的 `JobSystem`（持久化线程池、可绑 P 核），
   顺带解决"死接口"与 spawn 成本可控性。

### T4 真因已定位（2026-09-21）：**访存带宽受限 ⇒ 并发时每线程吞吐掉一半**（候选 2 落地）

**仪器**：给求解器加了 `IslandDiag`（`vxl_phys_solver::IslandDiag`，每次 `solve_phase` 覆盖，
零行为改动）——把求解相位拆成 `gather`（串行）/`scope`（唯一并行段）/`scatter`（串行），
并给**每组**记墙钟与流形数；`m1_islands` 打出"岛并行拆分"行。

**读数**（12000 簇 × 60 tick / 最后 tick；`g` = 组数 = 线程数）：

| g | 每组流形 | 每组耗时 µs | **µs/流形（每线程吞吐）** | gather（串行） | scope（并行） | scatter | 扩展比 |
|---|---|---|---|---|---|---|---|
| 1（串行基准） | 4728 | — | **≈0.97** | — | — | — | 1.00× |
| 2 | 2364 | 2878 | **1.22**（×1.26） | 344 | 2855 | 106 | 1.56× |
| 4 | 1182 | 1763 | **1.49**（×1.54） | 377 | 1935 | 593 | 2.12× |
| 8 | 590 | 1443 | **2.45**（×2.53） | 375 | 1596 | 304 | 2.74× |

⇒ **并发度每上一档，每线程的单位工作量成本就涨一档**（1.26 → 1.54 → 2.53×）。
模型 `墙钟(g) = gather + (W/g)·slow(g)` 与实测吻合（W ≈ 4.6 ms/tick 的单线程解算工作）。

**三条便宜的假设被逐一排除（都有读数，不是推断）**：
① **负载不均**：每组流形 590/591/592、耗时离散度仅 **1.10×** ⇒ 均匀；
② **串行占比（Amdahl）**：gather+scatter ≈ 0.45–0.97 ms，占求解相位 ≤ 1/3 ⇒ 上限 ~5×，不是 2.5× 的成因；
③ **spawn/join 开销**：本机 185–200 µs/次、每 tick 约 0.4 ms ⇒ 不是限项；
④ **E 核拖尾**：若 8 线程摊在 P/E 混合核上，组耗时会**双峰**（E 核那半明显慢）——
   实测 8 组同速（1.10×）⇒ **排除**"慢核拖尾"，指向**均匀的每线程吞吐下降**。

**结论**：该场景的解算**受访存带宽限制**（每线程要读/写共享的体状态、流形、warm 槽表，
且有多层间接寻址），所以并发一起来就互相挤带宽 ⇒ 端到端上限 ~2.5–2.9×，
**与"引擎哪里写错了"无关，也与线程调度无关**。⇒ T4 的 ≥3× 在本机属于**被数据布局锁住**的指标。

### T4 降访存第一刀：**warm 索引换快哈希 —— 实测无收益，已回退**（2026-09-21）

**靶子先换对了**（新加的细分读数，12000 簇 × 60 tick × 8 线程，累计 µs）：
**约束构建 152106–167688（97%）｜热启动预施加 ~1500（1%）｜迭代扫掠 2883–4392（2–3%）**
⇒ 该场景**物理迭代几乎不花钱**（收敛早退生效），时间全在**每流形一次的约束构建**上。
把每流形的构建摊开：~95 ns/流形（≈430 cycles，约 2.5 点/流形）——其中
`warm_index.get(&(a,b,space))`（Rust 默认 **SipHash** + 随机桶访存）是首要嫌疑。

**A/B（零依赖：手写 splitmix 快哈希 `BuildHasherDefault`，只改表布局、从不遍历 ⇒ 行为应逐位不变）**：

| 运行 | 约束构建累计 µs（SipHash） | 约束构建累计 µs（快哈希） |
|---|---|---|
| 1 | 152106 | 148715 |
| 2 | 167688 | 179246 |
| 3 | 163233 | 171903 |
| **均值** | **161009** | **166621**（+3.5%，在 ±10% 噪声内） |

扩展比同步在噪声里（2.62/2.81/3.25 vs 2.96/3.01/3.52）。**结论：查哈希不是成本所在**，
已 `git checkout` 回退（不留无收益的改动）。行为中性已由哈希核验：`determinism`
`0x711be572…` 与 m0 压力哈希 `0x417be20a…` 逐位不变。

**为什么这里没有便宜的赢面（量化）**：① 表 ~12k 项 ×16 B ≈ 190 KB ⇒ 基本驻 L2 ⇒ 桶访存惩罚有限；
② 每流形 95 ns 里，两次 `Mat3::from_quat` + 每点的有效质量矩阵/切向基/CFM 组装
（~150–300 cycles/点）才是主体 ⇒ **成本是"约束构建的固有算术与读"**，不是查找；
③ 连"每帧缓存每体旋转矩阵"这条也**不划算**：本场景 36000 体 / 11836 流形 ⇒ 每体仅 0.33 条流形，
缓存反而多算（36000 次 vs 现 23700 次 `from_quat`）——**只有"流形数 ≫ 体数"的稠密场景才赚**。

**⚠️ 方法论（记牢）**：本次还想给 `build_constraint` 加**逐流形计时探针**，被自己的仓库纪律拦住——
`probe::start/us` 自身 ≈25 ns/次，而这里每流形总预算只有 95 ns ⇒ **仪器会主导测量**。
凡"调用密度 >1000 次/步"的区段只能上**零开销整数计数器**或做**不改变动力学的端到端 A/B**
（注意：对 *数学* 步骤做消融会让场景发散 ⇒ 不可比）。

**因此 T4 的下一步不再是"降查找"，而是算法层**（更大范围的引擎设计，未动）：
① **约束构建跨子步共享**（同一 tick 的 2 个子步现在各建一遍；几何在子步间只微变 ⇒
可只更新速度通道，属**行为变化**，需走 ADR-0004 换代 + 全套门）；
② **减少每点的构建量**（延续已有的"参与式降点"思路：近共面点合并/惰性建约束）；
③ 稠密场景下的**每体旋转矩阵缓存**（本场景不赚，金字塔/砖墙那种"流形≫体"的场景才赚）。
判据统一用 `IslandDiag` 的 **µs/流形**（不受机器负载影响）。

### T4 降访存第二刀：**每体旋转矩阵缓存 —— 用估算即否证**（2026-09-21，未写代码）

候选③原本是"每帧（每解算调用）缓存每体的 `Mat3`，省掉每流形两次 `Mat3::from_quat`"。
**先算账**（`arena_bench pyramid`，稠密场景：210 动体、571 流形、2235 接触点、求解 ≈890 µs/步）：
每解算 2×571 = 1142 次转换 vs 缓存后 210 次 ⇒ 省 ~930 次；单次 ≈15–30 ns ⇒ **省 ~14–28 µs，
占求解 1.5–3%** —— 落在本机 ±5–10% 噪声内 ⇒ **不值得做**（与 warm 索引那次同结论）。

**顺带更正一处我自己的口径混淆（撤回一个"16×"的说法）**：我曾把"碎片雨的 95 ns/流形"与
"金字塔的 ~1558 ns/流形"并列，暗示差 16×；核对后发现两者**口径不同**——
前者是 `detail[0]`（**仅约束构建**，且是多组并发下的**累计和**），后者是
`arena_bench` 的**整个求解相位**（含构建/热启动/迭代/散射/warm 回写）÷流形数 ⇒ **不可比**。
**可信的只有"构建占解算的 97%、迭代 3%"这一条**（同源同口径，见上）。

**另：点数也不是判别量**——两场景的**点/流形几乎相同**：
碎片雨 43663/11836 = **3.69**、金字塔 2235/571 = **3.91**（`m1_islands` 的岛并行拆分行现在也打印点数）。

⇒ **T4 剩下的只有行为变化类**（① 约束构建跨子步共享、② 减少每点构建量），二者都要走
ADR-0004 换代 + 全套门；且**动手前需要更干净的分母**（现有读数混了"累计/单帧"与
"并发/单线程"两种口径 ⇒ 先修仪器再改算法，这是本轮最该记住的次序）。

### T4 分母口径已统一（2026-09-21）：**窗口均值的 ns/流形、ns/点**——且"每流形成本"随**工作集**变化 11–18×

**为什么要修**：此前的读数混了三种口径（`last_detail_us` 是**进程内累计**、`IslandDiag` 是**末帧**、
我自己散文里又拿"仅构建"与"整个求解相位"并列）⇒ 在这种分母上做算法改动，改完判不出成效。

**规范指标（只用这一组做跨场景/跨改动比较）**：`IslandDiag` 新增**每解算**（子步）的三段合计
（约束构建/热启动/迭代，**各组之和**，与同帧的 `manifolds`/`points` 归一化）；
`m1_islands` 打印**后半程窗口均值** + 窗口内的流形区间（本仓纪律：决定量必须窗口均值）。

**读数（12000 簇 × 3 体 = 36000 体，8 线程）**：

| 跑法 | 窗口内流形 | 约束构建 | **ns/流形** | ns/点 | 求解扩展比 |
|---|---|---|---|---|---|
| 60 tick（落定期） | 0–11836 | 2101 µs/解算 | **604** | 162 | 2.42× |
| 400 tick（已入睡） | 707–4264 | 57 µs/解算 | **54** | 13.5 | **1.00×** |

⇒ ① **该场景有三个阶段**：下落（几乎无流形）→ 落定峰值 → **睡眠塌缩**（子岛睡眠生效后
流形数骤降：60 tick 末帧 11836 → 400 tick 末帧 **707**）。**末帧单点与全程累计均值都不代表场景成本**
（累计均值被空转前段稀释、末帧又落在睡眠塌缩期）——这正是要窗口均值的原因。
② **每流形成本不是常数，而是工作集的函数**：清醒面热（少数岛反复解）时 **54 ns/流形**，
流式扫 36000 体时 **604–970 ns/流形** ⇒ 差 11–18× ⇒ 任何 µs/流形 比较**必须同时报"阶段 + 体数/流形区间"**，
否则又是在比两种东西。③ **扩展比同样随阶段变**：400 tick 时清醒岛太少、落到并行门槛
（`manifolds < 4096 ⇒ g_count = 1`）以下 ⇒ 两边都串行 ⇒ 读 1.00×
⇒ T4 的比值**必须在落定期测**（文档里的 `12000 60` 正是这个窗口）。

**⇒ 下一步（行为变化类候选）现在有干净的分母了**：① 约束构建跨子步共享；
② 减少每点构建量。判据 = 同窗口的 **ns/流形 / ns/点** + 同批的求解扩展比 + 金样门不回退。

## T5 — 金样对照 ✅ **有独立入口（`gold-sample/`，勿再写"无独立入口"）；入睡列已关闭**（2026-09-21 复测）

**入口**（`RECIPES.md` §）：
```bash
cd "/d/开发/RUST WL/gold-sample"
CARGO_TARGET_DIR=C:/vxl-wl-target-gold cargo run --release -q -- col45  600 16 0.01 4 3.0 30 4
CARGO_TARGET_DIR=C:/vxl-wl-target-gold cargo run --release -q -- pile5  600 16 0.01 4 3.0 30 4
CARGO_TARGET_DIR=C:/vxl-wl-target-gold cargo run --release -q -- tower25 2400 16 0.01 1 3.0 30 16   # 塔要长跑（2400 tick）
```

**当前读数（2026-09-21 复测；配方 = 上列，参考档 `16/4/4`／塔 `16/1/16`）**：

| 场景 | 入睡 | 超阈（线/角/双） | 末态 max\|Δpos\|（自记容差） | 活跃期相对代价 vxl/rapier |
|---|---|---|---|---|
| col45 | **45/45** | 0 / 0 / 0 | **3.4 mm**（容差 7.5）✅ | 12.56× |
| pile5 | **2000/2000** | 0 / 0 / 0 | **4.1 mm**（容差 12.5）✅ | 1.63× |
| tower25 | **2396/2500（95.8%）** | **0 / 1 / 0** | **95 mm**（容差 59.5）⚠️ **超出** | 3.66× |

**⚠️ 陈旧读数已作废（原文留痕）**：本行此前记的是
`位置列 ✅（7.5/12.5/59.5mm 容差内）；速度 ⚠️（col45 0.065、pile5 0.088、tower25 0.283）；
入睡列 ❌（col45 35/45、pile5 1195/2000、tower25 0/2500）` —— 那是**睡眠修复前**的读数：
"入睡 ❌"三格**已全部关闭**（45/45、2000/2000、2396/2500），"速度"列的旧口径（0.065/0.088/0.283）
已无从对应，现改记**活跃期相对代价**（同一跑里 vxl/rapier 的 step 墙钟比）。

**仍开着的两点（都属"已接受的代价"或残余，不是新缺陷）**：
1. **塔的 `Δpos` 超出自己记的容差（95 vs 59.5 mm）** —— **已查清它是有界偏移，不是漂移**：
   同一配方跑 **600 / 1200 / 2400 / 4800 tick 四点，`max|Δpos|` 全部 = 0.0950 m、mean 0.0571–0.0574**
   ⇒ **前 600 tick 内就冻结**（参照体落位后位姿不再变 ⇒ 该"与 rapier 的分歧"度量随即冻结；
   harness 注释本就写明"发散是预期，看量级"）。⇒ **建议按 ADR-0004 重设容差**（值确定性且
   与 tick 无关 ⇒ 是很好的冻结基线），而不是当回归追——追它等于改引擎去贴 rapier 的数值路径。
   ⚠️ 口径注意：该度量只覆盖 **n=16 个参照体**（不是全部 2500 体）。
2. **塔尾仍清醒**（2400 tick：104/2500，`|ω| ≈ 0.04–0.05`，**正压角阈 0.05**；
   4800 tick：**35/2500**、超阈仅 角3、入睡升到 **2465/2500（98.6%）**）⇒ 该场景**没有任何一帧
   是全睡的**（harness"入睡面"行据此写"vxl 全程清醒"）；即 P1 残余的最后一段：角向残差贴着门线，
   且**随时间继续收敛**（不是卡住）。

## P5 — 默认档 `trimesh` 末态能量异常 Σv² ≈ 1.0×10⁵ J ✅ **已收官（2026-09-21）**：**不是引擎缺陷**

> **收官结论（三行）**：① 那 ~10/200 个"落穿"的体是**滚出 ±45 m 地形边缘**后自由落体
> （离场瞬间速度仅 9–14 m/s，见 ⑲）——场景**没有边界**、体里 ~134 个是**会滚的球/圆柱**，
> 属物理行为；② `Σv²` 因此是"掉出去几个体"的度量（10×100² = 10⁵ 整份来自离场体），
> 建议把越界体剔除或给场景加边界；③ 引擎侧唯一被量化出的代价是 **静置站立 ~2 cm**
> （皮肤带口径，⑰(c) 有一行候选修法 + 双向验收），以及"9.4 cm 悬空""球埋 1.09 m"
> **两个读数都被否证为测量口径错误**（⑰/⑱）。
> **留痕价值**：本条线四次自曝口径错误（`h(中心)` 当参考、单点采样当静置、`--steps` 漏传、
> 拿投放点的 h 比会滚的体）⇒ 凡"体在错误高度/位置"必须用**与位置无关**的接触/间隙判据复核。

**怎么发现的**：做「子块睡眠」落地的**默认档长跑复核**时顺带看到——`arena_bench trimesh --steps 3000`
（默认档 `iterations=3 inner=1 substeps=2`、200 体、90×90 m 正弦地形）末态 **Σv² ≈ 1.0×10⁵ J**
⇒ 200 体平均 `|v| ≈ 22 m/s`。这与"末态应趋于静置"的直觉不符，故按纪律先做 A/B 再报。

**受控 A/B（本仓既定方法：旧提交只读 worktree + 独立 target 目录）**：

| 状态 | 末态 Σv² | 清醒 | 流形 |
|---|---|---|---|
| **今天之前**（`f41e5b8`，worktree） | **100 076** | 91/200 | 103 |
| 粘着门开、子块睡眠**关** | **120 068** | 79/200 | 78 |
| 粘着门 + 子块睡眠**开**（当前落地态） | **100 065** | 78/200 | 89 |

⇒ ① **该异常是既有的**（今天之前就是 10 万 J）⇒ **非本会话改动所致**；
② **当前落地态与今天之前几乎相同**（100 065 vs 100 076）⇒ **净无回归**；
③ ⚠️ 但**中间态**（只开粘着门）曾抬到 120 068（+20%），子块睡眠又把它带回
   ⇒ 两次改动在该场景**相互作用**；且我做粘着门落地时**没跑这个场景**（当时覆盖的是
   金样三场景 + `m0_gates` + arena + pyramid）⇒ **教训：默认档长跑场景应作为落地固定项，逐场景跑全**。

**现状：已收官（2026-09-21）——结论是**场景缺边界 + 会滚的体**，不是引擎缺陷（见文首"收官结论"与 ⑲）。**
**⚠️ 以下 ①–⑯ 是当时的推进过程（含四处被否证的口径错误），保留为留痕，不要当结论读；**
**结论以文首三行 + ⑰(代价侧) + ⑱(球线索否证) + ⑲(原症状收口) 为准。**

**判别方法与读数**（`arena_bench` 加了逐体 y 分布 + 最低/最快体打印；见该 example 的 `bench()`：
「y 分布 / 最低 5 体 / 最快 5 体」）：

```
y 分布：min -4201.078 | p05 -3.222 | p50 -1.762 | p95 -3.448 | max 3.980
最低 5 体：y ≈ -4000, |v| 100.00, x/z 到 -145/176（**远在地形 90×90 之外**）
```

- **投放是干净的**（读码核实）：x/z ∈ **±15**（地形 ±45）、y 从 **14.0** 起 ⇒ 全部在 footprint 内、
  从地形上方落下 ⇒ **不是场景/投放问题，是引擎侧**。
- **大多数体是正常的**：地形高度 `h` 在谷底可到 ≈ −2.7 ⇒ `p05 −3.22 / p50 −1.76` 与
  "停在坡谷上"（`y ≈ h + r`）**自洽**（`max 3.980` ≈ 地形局部最高）⇒ 大部分体正确停住。
- **缺陷部分**：少数体落在 **y ≈ −4000 m、`|v| = 100`（速度钳制值）、位置跑到地形之外**
  ⇒ **穿网后无限自由落体**。由 `Σv² = 100 065` 与 `v² = 10 000` 反推 ⇒ **约 10/200 体穿网（≈5%）**。
- **机理候选已被最小复现推进（2026-09-21）**：新增探针
  `crates/vxl-phys/examples/trimesh_escape_probe.rs`（**同一套地形**、单盒、四个落点对照）。
  跑法：`cargo run --release -q -p vxl-phys --example trimesh_escape_probe`。读数：

  | 落点（格距 3.750 m） | 地面 h | 期望静置 y | 末态 y | **沉降** |
  |---|---|---|---|---|
  | ① 四边形形心 | 1.962 | 2.282 | 1.996 | **+0.286** |
  | ② 对角线中点 | 1.690 | 2.010 | 1.703 | **+0.307** |
  | ③ **格点（顶点）正上方** | 1.400 | 1.720 | 0.982 | **+0.738** |
  | ④ 边中点 | 1.889 | 2.209 | 1.642 | **+0.567** |

  ⇒ ① **四个落点全部沉降**，且形心处的沉降量 ≈ **盒的半高（0.32）**——这与"**按中心点**对网格
  求接触、而非按**支撑面**"的语义相符（中心几乎正好落在地表）；
  ② **顶点/边中点明显更差**（+0.74 / +0.57，其中顶点处**低于地面 0.42 m**）⇒
  **边/顶点退化被证实**（与 R.2/R.3 的"面平局"弱点同族）；
  ③ ⇒ 200 体场景的"穿网 + 无限落体"很可能是**这个沉降的下游后果**（等效地表比真实低 0.3–0.4 m，
  叠加多体挤压后从缝里漏下去），而**不是**独立的第二种缺陷。
  ④ **"按体中心求接触"这一假设已被否证**（同日尺寸扫描，探针【B】段）：

     | 形状 | 半尺寸 | 沉降 | 沉降/半尺寸 |
     |---|---|---|---|
     | 盒(半高) | 0.12 | +0.157 | 1.31 |
     | 盒(半高) | 0.24 | +0.197 | 0.82 |
     | 盒(半高) | 0.48 | +0.417 | 0.87 |
     | 盒(半高) | 0.80 | **+0.196** | 0.24（**更大反而更少**） |
     | 球(半径) | 0.20 | **+1.407** | 7.03 |
     | 球(半径) | 0.40 | **+1.371** | 3.43 |
     | 球(半径) | 0.70 | **+1.342** | 1.92 |

     ⇒ ① 沉降**不**随半尺寸等比变化、且**非单调** ⇒ **体中心假设否证**；
     ② **球更严重**：沉降 ≈ **1.4 m 且与半径无关** ⇒ 球**停在地表之下 1.3–1.4 m**；
     ③ 探针【A】两次运行的数值**不可比**（我改了投放高度 `h+2.0` → `h+2.0+半尺寸`）
     —— 但这本身是信息：**静止位置对投放冲击敏感** ⇒ 该接触**不是干净的"表面"**。
     ⚠️ 方法论记录：改探针的自变量时必须**同时固定其余条件**，否则连自己上一轮的读数都不能比。

  ⑤ **"去穿透受迭代预算限制（软接触）"这一假设也被否证**（同日探针【C】段）：
     同批落点换到**参考配方**（`iters 16 / inner 1 / substeps 16`）：

     | 形状 | 半尺寸 | 默认档沉降 | **参考配方沉降** |
     |---|---|---|---|
     | 盒(半高) | 0.24 | +0.197 | **+0.471** |
     | 盒(半高) | 0.48 | +0.417 | +0.386 |
     | 盒(半高) | 0.80 | +0.196 | +0.279 |
     | 球(半径) | 0.20 | +1.407 | **+1.430** |
     | 球(半径) | 0.40 | +1.371 | +1.379 |
     | 球(半径) | 0.70 | +1.342 | +1.347 |

     ⇒ **球的沉降几乎逐位不变** ⇒ **不是迭代预算/收敛问题**；盒的沉降也没缩小。
     ⇒ 收敛到：**几何/压入量本身算错**（窄相侧），而不是求解侧。

  ⑥ ⚠️ **探针自身的一处标注错误（自曝）**：我把"四边形形心"当作**内部**落点，
     但格点取 `(0,0)`–`(3.75,3.75)` 时，正方形形心 `(1.875,1.875)` 的 `x+z = 3.75`
     **正好落在对角线（三角形边界）上** ⇒ ①其实是**边退化**案例；真正的内部案例是②。
     修正后的关键读数：**即使在三角形内部（②），盒也半埋在三角形平面之下**
     （末态 y 1.703，而该处三角形平面 ≈ 1.661、真实 `h` 1.690 ⇒ 相对**平面**仍低 ~0.28 m）。

  ⑦ **机理收窄**：压入量**不随尺寸等比**（否证体中心）、**不随迭代预算变化**（否证收敛论）、
     但在**形状**上差异巨大（球固定 ~1.4 m、盒 0.2–0.4 m）⇒ 指向**三角网路径里按形状分派的
     接触/压入量计算**（例如球→网格那一路的支撑/深度算错），而**不是**全局的皮肤/容差问题。

  ⑧ ⚠️ **"盒的沉降"基本是探针的口径错误（自曝，同日带接触复核）**：让探针在**不睡**的状态下
     打印实际接触（`PROBE_CONTACTS=1`，`sleep_time=1e9`；**发现：睡眠体不做检测 ⇒ 末态 0 条流形**，
     所以必须不睡才能看到接触），①（盒半高 0.32、末态 y 1.9993）的真实接触是：

     ```
     manifold(0,1) n=(-0.247, 0.969, -0.023)
       p=(1.10,1.72,1.56) depth -0.0043   p=(1.24,1.77,2.18) depth -0.0013
       p=(0.49,1.57,1.71) depth -0.0036   p=(0.63,1.62,2.33) depth -0.0006
     ```

     ⇒ ① 接触点落在 y ≈ **1.57–1.77** ⇒ 盒子 0.64 m 的足迹内**地形高度本身变化 ≈ 0.2 m**（斜坡）；
     ② 四条深度都是 **−0.0006…−0.0043**（极小、在 skin 带内）⇒ **接触判定正确**，盒就是**贴斜面**停住；
     ③ ⇒ **"沉降 0.28" 是拿 `h(体中心)` 当参考算的，在斜面上这个参考不成立** ⇒ **本探针的口径错误**，
     **不是引擎缺陷**。
     ⇒ **方法论**：探针的"期望静置高度"必须用**该体足迹下的接触平面**，不能用标称中心的 `h`。

  ⑨ **因此 P5 收窄为两条仍然成立的线索**：
     ① **球那一路**：固定 ~1.4 m 的异常**无法用斜坡解释**（r=0.2 的球足迹仅 0.4 m）——**主线索**；
     ② **约 10/200 体落穿到 y ≈ −4000 m、`|v|` 顶到钳制值、跑出地形之外**——有 y 分布证据支撑，
     仍是真缺陷（很可能是下沉到足够深之后从网格缝里漏下去）。

  ⑩ ⚠️ **"落穿"这条也可能是我 over-read（已有数据即可推进，2026-09-21）**：
     逃逸体的**水平位置**（x,z 达 −145 / 176，远超地形 ±45）是硬证据——**自由落体是竖直的**，
     体一旦开始下落其 (x,z) 就固定。故：
     - 若**竖直穿网** ⇒ 其 (x,z) 应等于穿网处坐标（|x|,|z| ≤ 45）；
     - 实测 **|x| 远大于 45** ⇒ **它开始下落时就带水平速度**（13 m/s 量级，正是"滑下坡 →
       从边缘甩出"的形态）。
     ⇒ **最简解释＝滚下斜坡后从地形边缘飞出、然后无限自由落体**（场景无围栏），
     **不必然是引擎缺陷**。（严格说"边滑边穿"不能排除；但⑧已证盒的接触判定正确，
     故"甩出边缘"更省假设。）
     **决定性判别（下一步，便宜）**：跑一次带**逐体监控**的复刻——每步检查是否出现
     "**在 footprint 之内且 y < h(x,z) − 余量**"的事件：有 ⇒ 穿网成立；
     只有"跨过边界后下落" ⇒ 本条应改为**场景/判据问题**（无围栏），并回到
     "'末态 Σv²' 不适合作该场景的判据"。
  ⑪ 🔎 **球那一路有了决定性读数（探针【D】段，2026-09-21）**：真内部点 `(0.94,0.94)`、
     不睡（`sleep_time=1e9`）：

     > ⚠️ **本条读数已被 ⑱ 否证为测量口径错误**（"末态 y" 与 "投放点的 h" 是两个地方；
     > 球在粗网格上会滚 17 m 级）。保留原文只为留痕，**不要据此推结论**。

     ```
     球 r=0.20：末态 y 0.599、地面 h 1.690 ⇒ **球心在地面下 1.09 m**
       接触 manifold(0,1) n=(0.192,0.977,-0.095)  p=(-25.33, 0.41, -4.02)  depth +0.0073  feat 492
     ```

     ⇒ **球被一条"自以为贴着"（`depth = +0.0073`）的接触按住、停在**地表之下** 1.09 m**；
     而**盒**（14 点查询）的接触全部落在**本地**（y ≈ 1.57–1.77）、深度 −0.006…−0.0006 ⇒ **正确**。
     ⇒ **形状差异就出在这里**：点/盒查询找到本地面、**球查询找到的接触状态与几何不自洽**。

     ⚠️ **我读不透的一层（不当作结论）**：打印出的该接触点距球心 **26.7 m**，而 `depth` 只有 0.007
     ——按我读到的 `depth = r − (center−q)·n` 算不出这个组合
     ⇒ **`InteropContact.point` / Manifold 点的语义与我的假设不同**（可能是锚点/投影后的点），
     这一层**必须再读码核实**才能下结论。

  ⑫ **P5 剩余（供立项）**：唯一还站着的真线索＝**"球→网格"这一路的接触是否指向正确三角形**
     （`contacts_sphere` → `closest(center, radius+skin)` → 桶剪枝 + 最近三角形）。

     > ⚠️ **已被 ⑱ 否证**：球那一路在平网格上 clearance 恰好 0、在粗起伏网上 clearance ≈ −1 cm
     > （贴住），"指向错误三角形"没有观测支持。**P5 剩余线索改见 ⑱ 末尾那条**（默认档把约 10 体
     > 加速到钳制速度）。
     已知：① `closest()` 的桶剪枝**按桶 AABB**；② 球用 `radius + skin` 作 `max_d`；
     ③ 现象是"球心低于地表 1 米余、接触仍报贴着"。**待核**：`closest` 在该形态下究竟返回了哪个三角形。

  ⑭ 🎯 **根因定位（2026-09-21，纯读码 + 已有数据）：`sd` 是投影量 + 桶剪枝半径太小 ⇒ 自锁**

     > ⚠️ **此"根因"的支撑读数已被 ⑱ 否证**（球没有自锁，只是在滚）。下面这段读码分析作为
     > **"`sd` 是投影量、剪枝用 AABB"这一事实描述**仍然成立，但**不能再当作已观测缺陷的解释**；
     > 是否真有自锁形态，需另立可判别探针（⑱ 的 clearance 口径）在**人为把体放进网格内部**时复验。

     `vxl-phys-terrain/src/mesh.rs` 的球查询：
     ```rust
     let Some((_, q, n, ti)) = self.closest(center, radius + skin) else { return true };
     let sd = (center - q).dot(n);      // ← **投影量**：不是到最近点的距离
     let depth = radius - sd;           // 远三角形只要 (center−q) 近似垂直于 n，sd 就小 ⇒ depth≈0
     if depth < -skin { return true; }
     ```
     两条合起来构成闭环：
     1. **`sd` 是投影量** ⇒ 一条**远处**的三角形只要几何"擦着"，就能给出 `depth ≈ 0` 的**有效接触**；
     2. **桶剪枝**（`closest` 内"桶 AABB 距查询点 > `max_d` ⇒ 整桶跳过"）用的
        `max_d = radius + skin`（≈0.22 m）；而**体陷在地表之下时，真正托着它的那张三角形
        所在桶与其 AABB 相距约 1 m ≫ max_d ⇒ 被剪掉** ⇒ 候选里只剩"擦着"的远三角形。
     ⇒ **幽灵支撑**：球被一条远方的接触按住，停在地表下 1.09 m 且**永不被顶出**（自锁）。
     ⇒ 这同时解释了 P5 最初那个症状（体陷进三角网后不再回来）。

     ⚠️ **且该剪枝的"语义等价"论证是错的**（代码注释原文）：
     "两个调用方（点/球）本就丢弃 `depth < −skin`（等价 `dist > max_d`）" ——
     但调用方丢弃用的是 **`sd`（投影量）**，**不是 `dist`**；两者只在"q 是垂足"时等价，
     而**恰恰在"体陷在网格里"时不等价** ⇒ 剪枝会把唯一能救它的那张面剪掉。

     **修法方向（须单独立项 + 逐位门禁）**：凡"体可能陷在网格内"的查询，
     剪枝/接受都必须**以距离为界**（例如同时要求 `|center − q| ≤ radius + skin`，
     或把深度改用**平面距离**并另加距离上界）；仅靠 `sd` 判据在几何上是**不健全的**
     （远处擦着的三角形可任意给出小 `sd`）。

  ⑯ 🏁 **定性翻转（2026-09-21，用网页测试当裁判）：这不是"别人没有的 bug"，是"不同的取舍"**

  按"先拿 9 引擎对拍当裁判"（把问题交给自检，而不是先改引擎）新增探针
  `shape-trimesh-sunk`「陷网球体能否顶出」：把半径 0.45 的球**直接生成在平坦三角网内部**
  （球心在地表下 0.20 m），看它能否被推回 `y ≈ r`。

  | 引擎 | pass/degraded/**fail** | 该探针 |
  |---|---|---|
  | **vxl-phys (RUST WL)** | **20 / 0 / 0** | **pass（顶出）** |
  | Rapier 3D | 19 / 0 / 1 | fail：y = **−66.37**（直接掉穿） |
  | Jolt Physics | 13 / 6 / 1 | fail |
  | NVIDIA PhysX 5 | 13 / 6 / 1 | fail |
  | Bullet (ammo.js) | 16 / 3 / 1 | fail |
  | Havok | 15 / 4 / 1 | fail |
  | Crashcat | 18 / 1 / 1 | fail |
  | cannon-es | 15 / 4 / 1 | fail（y = −82.77） |
  | Oimo.js | 10 / 9 / 1 | fail |

  ⇒ **9 个引擎里只有我们能从网格内部恢复，其余 8 个一律掉穿。**
  ⇒ **⑭ 的"根因"必须重新定性**：`sd` 取**投影量**（远三角形也能给出 `depth≈0`）
  正是**"从网格内部恢复"能力的来源**——它不健全，但**有正面作用**；
  别的引擎要求真正的最近面，找到就没有接触 ⇒ 掉穿。
  ⇒ **取舍**：**可恢复性**（我们能顶出）↔ **静置高度准确性**（可能停在幽灵支撑上、低 ~1 m）。
  bench 那个症状（球停在地表下 1.09 m）是后者的代价。
  ⇒ **⚠️ 我先前那个修法方向（接受判据加距离上界）会把我们变成和其他 8 个一样掉穿——更糟，弃。**
  真正该做的（若要做）：**保留可恢复性**，同时让查询在"体陷在网格里"时**也能找到真正的最近面**
  （在现有候选之外补一次以真实距离为准的搜索，取更可信者）。**优先级：低**（现状在跨引擎对拍里是优势项）。

  **physarena 侧改动（该目录无 git）**：仅在 `src/core/selftest.ts` 的探针数组里**追加**一条
  （`shape-trimesh-sunk`，带注释块）；**回退 = 删掉那一块**（无其它改动；`npm run build` 重建）。
  这条探针是本仓相对 8 个引擎的**差异化证据**，建议常驻。

  ⑰ 🎯 **代价侧量化（2026-09-21）：⑯ 那条"静置高度准确性"的代价 ≈ 2 cm（皮肤带）——
     既不是米级，也不是我先读到的 9.4 cm：那个数是**瞬态**。**

  **（a）新探针 `shape-trimesh-hilly`「起伏三角网静置高度」**（arena，±4 m / 8×8 格 / 格距 1 m /
  同一 H；盒半高 0.35 从 `H(0,0)+2.5` 投放）。判据写了**五版**才站住，每版都是我自己的一种口径错误：

  | 版 | 判据 | 结果 | 错在哪 |
  |---|---|---|---|
  | 1/2 | 体中心高度 vs 标高 | 斜停误判 | 中心高度不是接触量 |
  | 3 | 8 角点里**最高**角点 | **9 引擎全挂** | 起伏面上刚性盒不可能每角贴地 |
  | 4 | 8 角点里**最低**角点 | vxl 0.094 | 分不开"悬空"与"跨凹/压凸" |
  | 5 | **底面** 7×7 | vxl 0.485 | 盒子可能翻过来躺在**任何一面**上 |
  | 6 | **全身 6 面 × 7×7（294 点）最小 (y − H)** | 采用 | —— |

  ⇒ **只有"全身采样 + 有没有任何一处贴地"这一个口径**在几何上站得住（刚性凸体在起伏面上
  **本来就不会多点贴地**，那 9.4 cm 一度看着像真悬空）。这条口径与 arena 探针同源，已定稿常驻。

  **（b）300 tick 读数是瞬态，不是静置**：Rust 侧**逐字同场景**探针
  `crates/vxl-phys/examples/trimesh_rest_probe.rs`（±4 m/8×8、同一 H、同一三角化顺序、同一半高、
  `PhysConfig::default()`、按 arena 的 `vxl_body_material` 给**两个体**都上 μ=0.9）给出轨迹：

  ```
  t= 25 全身间隙 +1.2123  |v| 4.09      t=150 +0.3553  |v| 1.09
  t= 50 −0.5196 |v| 4.27                t=175 +0.0794  |v| 1.84
  t= 75 +0.3879 |v| 1.05                t=225 +0.0312  |v| 0.088
  t=100 +0.0910 |v| 1.76                t=300 +0.0289  |v| 0.064
  ```

  ⇒ 盒子在**下坡翻滚**（间隙在 ±0.5 m 间摆、|v| 1–4 m/s），t≈225 才安静，之后**缓慢爬行**
  （|v| 在 0.008–0.064 之间摆、一直 `清醒`）。**arena 的 300 tick 正好落在这段翻滚里** ⇒ 0.094 是
  翻滚中的瞬时采样（arena 逐位确定性 ⇒ 三次跑都读同一个 0.094，**不等于**它已静置）。
  换 **900 tick** 窗口后**同一探针直接通过**（vxl **21/0/0** 全绿）；诊断档把门限压到 0.005 时
  **vxl 一次都没报** ⇒ 静置时其全身最小间隙 ∈ [−0.03, +0.005]，与 Rapier/Jolt/PhysX/Bullet/Havok/
  Crashcat 同档；该探针只剩 Oimo（悬 0.169）与 cannon-es（穿网 −1103）不合格。

  ⚠️ **方法论（本会话第 N 次同类）**：**振荡量的单点采样不可作为"静置"读数**（与"决定量必须窗口均值"
  同族）；探针的 `steps` 就是采样窗口，起伏地面 + 2.5 m 投放必须给足沉降窗口（本探针定稿 900 tick）。

  **（c）真代价 ≈ 2 cm，且是**契约**问题**：⑤ 贴面投放对照（底面离地 0.05/0.5/2.5 m 投放、无翻滚）
  的静置间隙＝**+0.0206 / +0.0139 / +0.0189 m**；⑥ 平地三角网＝**+0.035 m**。
  来源是 `contacts_point` 的 `depth = skin − sd`：盒的采样点**高出地表 skin 以内**时报的是
  **正深度（=穿透）**，位置求解把它顶到 `sd ≈ skin` ⇒ **站着高出约一个 skin**。
  而**球那一路没有这个偏移**（`depth = radius − sd`，静置等价 `sd = radius` ⇒ **正好贴住**）——
  这解释了为什么"球更严重/更轻"这类读数在形状间不一致。
  ⚠️ `interop.rs` 的契约写明 **正 depth = 穿透**，故点/盒这一路**偏离契约恰好一个 skin**。
  **候选修法（一行）**：`contacts_point` 里 `depth = skin − sd` → `depth = −sd`（带内判定
  `sd < skin` 不变，**网格内部 `sd < 0` ⇒ depth > 0 仍成立 ⇒ 可恢复性不受影响**）；
  验收是双向的：`shape-trimesh-sunk`（陷球仍顶出）+ 本探针静置间隙 → ≈0。
  ⇒ **已于 2026-09-21 落地（网格路径），见 ⑳**；体素/喷溅同族未动（⑳ 末尾记了同形一行改法）。

  **⑳ ✅ 皮肤带偏置已修（网格路径，2026-09-21）——"正 depth = 穿透"口径归位**

  **依据（先定"哪种口径才对"，再动手）**：同档的**高度场路径**用的是真穿透量：
  `vxl-phys-narrow/src/lib.rs` 的 `poly_heightfield` / `hull_heightfield` 写的是
  `let depth = h − v.y;`（正 = 顶点已低于地面）+ 带内判据 `depth > −skin`（speculative margin）；
  而**球→网格**那一路本来就是 `depth = radius − sd`（贴住时恒为 0，⑱ 已用 clearance 验证）。
  ⇒ 只有**点/盒→网格**这一路写成 `depth = skin − sd`（= 穿透量 + skin），偏离 `interop.rs`
  契约（"正 = 穿透"）也偏离其余各路的同一口径 ⇒ **它是那一个异类，改它**。

  **改动**：`crates/vxl-phys-terrain/src/mesh.rs` 的 `contacts_point`：
  `let depth = skin - sd;` → `let depth = -sd;`（带内判据 `depth < -skin` **一字不改**，
  等价于 `sd < skin`）；模块头与该函数的注释按新口径重写；单元测试补三档
  （面内 = 0、面上方 0.02 = **−0.02**、面里侧 0.03 = **+0.03**）。

  **读数（进程内，`trimesh_rest_probe`，PROBE_TICKS=600）**：

  | 场景 | 修前 | **修后** |
  |---|---|---|
  | ⑥ 平网格（盒从 0.5 m 落到 y=0 面） | y 0.3849（**悬空 3.5 cm**） | **y 0.3500（精确贴住）**、最低采样点 −0.0000、入睡、零漂移 |
  | ⑤ 起伏网贴面投放 0.05/0.5/2.5 m | +0.0206 / +0.0139 / +0.0189 | **−0.0221 / −0.0156 / +0.0047** |

  ⇒ 起伏网上的"站立高度"从"稳定高 1.4–2 cm"变成"在 0 附近小幅摆动"，
  与 Rapier/PhysX/Bullet 那档（−0.004…−0.022）同量级。

  **双向验收（arena 9 引擎矩阵，wasm 重编后）**：
  **vxl-phys 21 通过 / 0 降级 / 0 失败** —— 其中 `shape-trimesh-sunk`「陷网球体能否顶出」
  **仍然通过**（可恢复性未受影响，正是 ⑰(c) 要的双向验收），`shape-trimesh-hilly`
  与平地/球的落体探针亦全过；其余 8 个引擎的读数一字未变。

  **门禁（逐项退出码）**：fmt 0、`cargo test --release` 0（49 个 test 二进制、0 FAILED，
  含 `vxl-phys-terrain` 15 例）、clippy `-D warnings` 0、vocab 0、typos 0；
  行为门：`m0_gates` PASS 且压力哈希 `0x417be20a8e49c9b0436987415ac9961a` **与基线一字不差**、
  `determinism` 一致、`m1_islands` 串行/并行末态哈希逐位一致
  —— 三条都印证"只动了网格路径"（默认档/m0 走的是**盒-盒与高度场**，不经网格）。
  ⚠️ `m1_islands` 的**加速比**本机读到 1.67×（并行跑）/ 2.20×（串行跑）：该场景**不含任何
  地形提供者**（纯盒体），且末态哈希与改动前逐位一致 ⇒ 与本次改动无关，是本机负载敏感
  （本机基线本就 ~2.29× < 3× 门），已按旧记录归因。

  **㉑ ✅ 同族复查（体素路径，2026-09-21）——盒/球本来就对，**外壳顶点采样**那一路确有偏置，已修**

  先量后改（新探针 `crates/vxl-phys/examples/voxel_rest_probe.rs`：8×2×8 格、边长 0.5、
  顶面 y = 1.0 的体素地板，盒/球/包各三档从面上 0.5 m 落下、600 tick）：

  | 形状（同一块地板） | 修前离地间隙 | **修后** |
  |---|---|---|
  | 盒 半高 0.25 / 0.35 / 0.50 | −0.0006 / −0.0004 / −0.0006 | 同（**本来就贴住**） |
  | 球 r 0.25 / 0.35 / 0.50 | −0.0000 / −0.0000 / −0.0043 | 同（**本来就贴住**） |
  | **外壳 半高 0.25 / 0.35** | **+0.0220 / +0.0220** | **−0.0002 / −0.0002** |
  | 高度场对照（盒 0.35，已知正确口径） | −0.0033 | 同 |

  ⇒ ① 我原先在 ⑳ 里写的"体素点/盒同样偏置"**只对了一半**：`contacts_box_voxel` 本来就写
  `depth = −d`（真穿透，测试注释"穿透 0.1 ⇒ depth ≈ 0.1"即此），球那路是 `radius − d`
  ——**盒与球在体素地形上一直是对的**（这两行读数即为证据）；
  ② 有偏置的是 **`contacts_point_voxel`**（`depth = skin − sdf`），而它的真实消费方是
  **外壳顶点采样**（`vxl-phys-narrow/src/lib.rs` 的 `Shape::ConvexHull vs provider` 逐顶点查询）
  ⇒ 实测**包体在体素地面上悬空 0.0220 m**（≈ band），与网格盒体那个 2–3.5 cm 同族同量级；
  ⇒ 这也意味着 arena 那边"**圆柱→凸包降级**"的形状落在地形上会整体抬高 ~2 cm。

  **改动**：`voxel.rs::contacts_point_voxel` 一行 `skin - d` → `-d`（带内判据 `depth < -skin`
  不变 ⇒ 等价 `sdf < skin`，与 `contacts_box_voxel` 和网格路径同一口径）。

  **流体不受影响（读码 + 实跑双证）**：`vxl-phys-fluid` 的两处 provider 查询都**不读 `depth`**
  ——它用返回的 `point`/`normal` **自己算** `sdf = (pi − c.point)·n`（line ≈411/553），
  且过滤上界就是管道半径 `self.h`（旧口径给 2h 的带、被它滤回 h；新口径正好是 h）。
  实跑 `dam_break`（体素盆 + 504 粒水）修前/修后读数**逐字相同**：
  `终态 x∈[-0.75,0.75] 最低 y=1.000`、单 tick 2.28 → 2.27 ms。

  **门禁**：fmt 0、test 0（49 个 test 二进制 / 0 FAILED，含 vxl-phys-terrain 15 例）、
  clippy `-D warnings` 0、vocab 0、typos 0；行为门 `m0_gates` PASS 且压力哈希一字不差、
  `determinism` `0x711be572cfe0e7eefb2cf51550fd4dd5` 一致（两条都不经体素路径）。
  **arena**：自检/适配器里**没有体素地形**（`grep voxel` 无命中）⇒ 无需重跑矩阵。

  **㉒ ✅ 喷溅域同族复查（2026-09-21）——同形偏置存在且已修；顺带发现球在该域不稳（另立线索）**

  新探针 `crates/vxl-phys/examples/splat_rest_probe.rs`：平场（y=0 面上 0.25 m 铺各向同性核
  σ=0.5、幅值 1.0、iso=0.5，中心等值面 y=1.3143）；参考面**独立**——用场自己的公开 `sdf()`
  沿 y 二分求零点，且**逐角点各算各的**（不再拿体中心的标高当参考）。

  | 形状 | 修前最小离面间隙 | **修后** |
  |---|---|---|
  | 盒 半高 0.25 / 0.35 | **+0.0086 / +0.0115** | **−0.0045 / −0.0029**（贴住） |
  | 球 r 0.25 / 0.35（对照，走 `radius − f`） | +0.0562（|v| 0.53）/ −0.2489（|v| 0.17） | **逐位相同** ✓ |

  ⇒ ① 喷溅域**盒查询是 14 点采样走点查询**（与网格同形、与体素不同）⇒ 继承了同形偏置，
  实测悬空 ~1 cm（比网格的 2–3.5 cm 小，因为 `f` 是"到等值面距离 / |∇σ|"，不是精确度量距离）；
  ② 改法仍是同形一行：`contacts_point` 的 `depth = skin − f` → `−f`（带内判据不变
  ⇒ 等价 `f < skin`）；③ **球那两列修前修后逐位相同**——它们走 `contacts_sphere`，
  正好当"只动了点/盒这一路"的不变量校验。

  ⚠️ **但球在该域本身不稳（新线索，与本次改动无关）**：r=0.25 停不住（|v| 0.53、清醒、离面 +0.056），
  r=0.35 **沉到等值面下 0.249 m** 且仍有余速 ⇒ 球在喷溅场上既不贴住也不静置。
  这属于**喷溅域的球接触行为**，不是口径问题（口径修完它一个字都没变）——
  待单独立项：先用同一探针扫半径/核参数，判"是场太软/梯度不稳"还是"球那一路的接触算错"。

  **门禁**：fmt 0、test 0（49 个 test 二进制 / 0 FAILED）、clippy `-D warnings` 0、vocab 0、typos 0；
  行为门 `m0_gates` PASS 且压力哈希 `0x417be20a8e49c9b0436987415ac9961a` 一字不差、
  `determinism` `0x711be572cfe0e7eefb2cf51550fd4dd5` 一致。

  **至此四块域的静置口径全部对齐**（盒/球都贴地，不再有 ~1–3.5 cm 的虚假悬空）：
  网格 `−sd` ✅、体素点 `−sdf` ✅ / 体素盒 `−d`（本来就对）✅、高度场 `h − v.y`（本来就对）✅、
  喷溅 `−f` ✅。

  **（d）摩擦/滚动判别（零成本对照，均已落地在探针里）**：
  ⑦ 均匀 11.3° 斜面（平网格 + 盒子**预旋贴合**投放，μ=0.9）⇒ **滑移 0.0000 m、t=100 即入睡**
  ⇒ **斜面上的提供者摩擦是好的**；起伏网上那 2.4–2.5 m 位移是**曲面上的翻滚/滚动**（物理行为）。
  ⚠️ 附带观察（**另立线索，不混进 P5**）：起伏网上盒体 15 s 内**始终不睡**（|v| 在 0.008–0.064
  间摆）⇒ 与 P1「塔不入睡」同族，出现在**提供者地形**上。
  ✅ **已把它映射回既有取舍（2026-09-21，不必再单独立项）**：这正是
  `vxl-phys-solver` 里 `DRIFT_BIAS_SCALE` 注释早就写下的**已知代价**——
  "满强度还会在 5 层堆留下 ~0.09 m/s 永久微抖（不入睡）"，而该标度**不能关**：
  注释里的消融数据是"关闭时塔蠕动在 t≈1400 持续升级为**弹射**（t=2000 |v| 97、盒飞出世界）"，
  且 `DRIFT_DEADZONE`（1 mm 死区）也做过对照（利 5 层堆 0.09→0.05、**害塔** 0.27→0.29）。
  ⇒ 这是**为塔验收刻意选的取舍**，不是没发现的缺陷。
  **本轮新增的只是范围**：该现象同样出现在**曲面提供者地形**上（本探针读数 |v| 0.008–0.064、
  15 s 不入睡、μ=0.9 的 11° 坡），而那条注释只记了"5 层堆"。**不建议**为此改标度（会换回塔弹射）。

  **（e）P5 本体的结论**：见 ⑱（球线索否证）与 ⑲（原症状＝**场景无边界**，非引擎缺陷）
  ——P5 到此**三条线索全部收口**；⑯ 里"静置高度准确性"的代价已量化到 ~2 cm，9.4 cm 那个读数
  被否证为瞬态（本条即为该否证的补丁，按"否证也要回写"的纪律）。

  ⑲ 🏁 **P5 原症状收口（2026-09-21）：那 ~10 个"落穿"的体是**滚出地形边缘**，不是穿网、不是被打飞。**

  先修一个我自己的**调用错误**（否则这条线会以"不可复现"被误判为陈旧读数）：
  `arena_bench` **有** `--steps N`，语义是"基准窗口之后再推 N 步"；我漏了这个 flag，
  于是量到的是**长跑之前**的末态（Σv² = 2218.6，与 P5 记的 1e5 差 45×）。
  ⚠️ **方法论**：A/B 时报"读数不复现"，先核**命令行是否逐字一致**（含那些看起来是装饰的 flag）。
  补上 `--steps 3000` 后逐位复现 P5 的原读数：**Σv² = 100065.0382**（P5 记 100 065）✓。

  然后在 `bench()` 的 extra-steps 循环里加**零引擎改动**的"离场轨迹"：
  记录每个体**第一次落到 y < −5**（地形最低约 −4.1）时的**上一 tick 速度**——
  那是它"离开台面"的瞬时状态，与它后来在哪无关。读数（HEAD，`trimesh --steps 3000`）：

  ```
  离场事件 10 起：
    第  245 步  |v| 13.759  v=(-1.539,-12.011,+6.535)  位置=( +4.73, -4.92, +53.39)
    第  276 步  |v| 13.342  v=(-6.559,-10.097,+5.748)  位置=(-21.03, -4.94, +51.48)
    第  408 步  |v| 11.561  v=(+7.366, -7.951,+4.021)  位置=(+46.41, -4.90, +48.61)
    …（10 起，|v| 全在 9.0–13.8）
  ```

  ⇒ ① **离场点全在 |x| 或 |z| ≈ 46–53 m** —— 即**刚出 ±45 m 地形边缘**；
  ② **离场速度只有 9–14 m/s**，其中竖直分量 −8…−12（自由落体累积）、水平分量仅 2–7
  ⇒ **不是接触注入能量的打飞**（打飞应在离场点就几十上百 m/s）；
  ③ 场景构成解释了这一切：`scene_trimesh_terrain` 的 200 体是 `i % 3` 的**球 / 盒 / 圆柱**，
  即 **~134 个会滚的体**，落在地形梯度最大仅 **|∇h| ≈ 0.34（18.6°）**的起伏面上——
  **滚体在无滚动阻力的引擎里本来就会一直滚到边界**，而这块 90×90 m 地形**没有边界墙/地板**
  ⇒ 滚出去就开始无限自由落体，`|v|` 顶到钳制值 100（那是**落体 4 s 后**的事，不是当场被打飞）。
  ⇒ ④ 因此 **Σv² ≈ 1e5 是"掉了几个体"的度量，不是引擎质量度量**：
  10 体 × 100² = 10⁵ **恰好整份来自离场体**——这也解释了为什么这个数对引擎改动不敏感
  （P5 表里 100 076 → 100 065，以及 `--iters 16` 只变到 2195.8 的那份读数同源）。

  **P5 结论（收官）**：
  - **不是引擎缺陷**：原症状由**场景缺边界** + **可滚体**造成，属物理行为；
  - 引擎侧真正被量化出的代价只有 **静置站立 ~2 cm（皮肤带，⑰）**，且它是**契约口径**问题
    （`contacts_point` 的 `depth = skin − sd`）——**已修，见 ⑳**；
  - ✅ **探针/基准侧已落地（2026-09-21）**：`arena_bench::bench()` 现在同时报
    **总量**与**有效量**（剔除 y ≤ −5 的越界体）并给出越界体数：
    `末态动能 Σv²=140092.7322（**有效 92.7332**，越界 14 体）`
    ⇒ 引擎质量读数应看**有效量**（92.7 而不是 1.4e5）；总量保留以便与历史读数（100 065 那一档）对比。
    ⚠️ 越界体数 10 → 14 的变化来自 ⑳ 的网格 `depth` 口径修正（静置几何变了 ~2 cm ⇒
    滚动轨迹变），离场机制未变（仍在 |x|/|z| ≈ 46–53 m、|v| 9–13 = 自由落体 + 滚动）。
    若要彻底干净，可再给该场景加边界（墙/浅盆）——属场景改动，未做。

  ⑱ 🧹 **⑪/⑫/⑭ 那条"球→网格"主线索**被否证**（2026-09-21，独立仪器）——又是我的参考系错误。**
  新探针 `crates/vxl-phys/examples/trimesh_sphere_rest_probe.rs`：同一套 90 m/24 段地形、
  同一四个落点、r = 0.2/0.4/0.7、贴面投放（`h + r + 0.05`）、600 tick，但把判据从
  "末态 y vs **投放点**的 h" 换成**与位置无关**的

  `clearance = d_min(球心→全部三角形的最小距离) − r`，其中 `d_min` 由探针**自己暴力枚举**
  全部三角形算出（不复用被测代码的最近面查询 ⇒ 独立仪器）。读数：

  | 场景 | clearance | 旧口径 Δ | 末态 (x, z) | 清醒 / |v| |
  |---|---|---|---|---|
  | 粗起伏网 ①②③④ × r=0.2/0.4/0.7（12 例） | **−0.009 … −0.012 m** | −0.392 … −0.586 m | 全在 (~−5, ~−32)（**滚了 ~17 m**） | true / 2.4–3.2 |
  | 平网格 r=0.2/0.4/0.7（对照） | **−0.0000**（精确贴住、入睡） | −0.0000 | 原地 | false / 0.0000 |

  ⇒ ① **球只是贴着**（沉 1 cm，在皮肤带内），在**顶点正上方、边中点**这些"退化候选"落法上同样如此
  ⇒ **没有"幽灵支撑自锁"**，也没有"埋在地表下 1.09 m"；
  ② **旧口径精确复现了那个伪影**：球在 3.75 m 格距的坡上**会滚**（本例滚了 ~17 m），
  而【D】段拿**投放点**的 `h` 当参考 ⇒ 14.06 那一栏的 −1.09 m ≈ 本例的 −0.39…−0.59 m 同一件错事，
  只是投放高度更大（2.0 m）滚得更远；
  ③ **球那一路在平网格上是精确的**（clearance 恰好 0、当场入睡）⇒ `depth = radius − sd`
  这条（无皮肤带偏移）本来就是对的，与 ⑰(c) 的形状差异说明一致。

  ⚠️ **方法论（本线索第三次同类，必须点名）**：P5 这条线上三个"体跑到不该在的位置"的读数
  （盒沉降 0.28、起伏网悬空 0.094、球埋 1.09 m）**全都是"拿别处的高度当参考"**，不是引擎缺陷。
  凡是"体在错误高度/位置"的量，都必须用**接触/间隙**类与位置无关的判据
  （`clearance = d_min − r`、全身采样最小间隙、接触集本身的 depth）来复核。

  **P5 收窄到唯一还开着的线索**：默认档 trimesh 200 体末态 Σv² ≈ 1e5 J —— 由 `v² = 10⁴`（|v| 顶到
  钳制值 100）反推 ⇒ **约 10 体被加速到钳制速度**（Σv² ≈ 12×10⁴），且它们(x,z) 跑到 −145/176、
  远在 ±45 m 地形之外（⑩）。⇒ 症结是**"谁把这 10 个体加速到 100 m/s"**（接触求解的能量注入），
  而**不是**"体从网格缝里漏下去"——落穿是下游后果（甩出地形边缘后自由落体）。
  下一步该查：默认档（iters 3 / substeps 2）下 mesh 接触的**单 tick 压入量 → 位置修正冲量**是否过大，
  以及与 `m0_gates` 压力场景（同一档位）能量口径的对照。




## P6 — 喷溅域（高斯喷溅）：**球**要么完全不被顶出、要么被固定 3 m/s 弹出（**新**，2026-09-21 派生发现）

**怎么发现的**：做 P5 ㉒（喷溅域 depth 口径对齐）时，同一探针里盒贴住（间隙 −0.003）而
**球**读数反常，遂按纪律先加判别段、不当作"球接触算错"。

**读数**（`cargo run --release -q -p vxl-phys --example splat_rest_probe`；平场 = y=0 面按
0.25 m 铺各向同性核、幅值 1.0、iso 0.5；参考面由场自己的 `sdf()` 二分求得，逐点各算）：

| 段 | 现象 |
|---|---|
| ② 落体静置 | 盒 半高 0.25/0.35 ⇒ 最小离面间隙 **−0.0045 / −0.0029**（贴住，已修口径）；**球** r=0.25 ⇒ **+0.0562 且 \|v\| 0.53**（停不住）、r=0.35 ⇒ **−0.2489 且 \|v\| 0.17**（沉进面下 0.25 m） |
| ③ σ 扫描（r=0.35） | σ=0.5：间隙 −0.2489、\|v\| 0.17；σ=0.75：间隙 **−0.0040**（贴着）但 \|v\| **1.0**（还在滚）。⚠️ σ≥1.0 各行**无效**——我的二分参考上界 2.0 饱和了（参考面已顶出量程），不作读数 |
| ④ **深埋顶出**（关重力、零初速、60 tick） | 初埋 0.02 / 0.10 / 0.20 m ⇒ **上移全部 +0.0000、\|v\| 0.0000**（**完全没有任何顶出力**）；初埋 **0.30 m** ⇒ 上移 **+3.0000 m**、\|v\| **3.0000**（**恒定 3 m/s 弹出**，无重力下匀速上升） |

⇒ ① 三种埋深（0.02–0.20 m）下球**既不接触也不被顶出**——不是"力太小"，是**接触根本没生成**；
② 再深一点（0.30 m）却给一个**恰好 3.0 m/s** 的弹出（60 tick 位移正好 3.0000 m 且速度不衰减
⇒ 是个**固定量级**的分离冲量，不是按穿透量算的）；③ 于是"球停在面下 0.25 m"就说得通了：
它落在"不生成接触"的带上，自由停在凹处。

**机理已定位（2026-09-21，读码 + 直接查询证死）**：`GaussianSplatField::density_grad` 里有**逐核截断**——

```rust
let a = Self::alpha(s, &ax, p);   // 马氏距离²（未截断）
if a > self.cut { continue; }      // ← **超出截断半径的核整体跳过**（不是衰减到 0）
sum += e;  g += inner * (-e);
```

⇒ 当查询点到**所有**核的距离都超过 `σ·√cut` 时：`sum = 0`、`g = 0` ⇒ 走 `sdf()` 的退化分支
`f = (iso − s)^0.5 = √0.5 = **+0.7071**` ⇒ **符号翻成"外侧"**，于是
`depth = radius − f = 0.35 − 0.7071 < −skin` ⇒ **不生成任何接触**。

**直接查询证据**（探针 ④ 段，`flat_field(0.5)`、球 r=0.35、关重力）：

| 初埋 | `sdf(center)` | 接触 | 行为（睡/不睡一致） |
|---|---|---|---|
| 0.02 m | **+0.7071**（= √iso，即 **s=0、g=0** 的退化值） | **无** | 上移 +0.0000、\|v\| 0.0000 |
| 0.10 m | **+0.7071** | **无** | 上移 +0.0000、\|v\| 0.0000 |
| 0.20 m | **+1.2020** | **无** | 上移 +0.0000、\|v\| 0.0000 |
| 0.30 m | +0.0808 | depth **+0.2692**、n=(0,1,0)、feat 1 | 上移 **+3.0000**、\|v\| **3.0000** |

⇒ ① **"不顶出"不是力小、也不是睡眠**（`sleep_time = 1e9` 下逐位相同；也排除了"睡在半空"）；
② 是**截断造成的"密度空洞"**：本场核在 y=0、σ=0.5、iso=0.5 ⇒ 等值面 y≈1.31，
而截断半径 ≈ σ·√cut ⇒ **埋深 ≤ ~0.19 m 的那一段读不到任何核**（越深反而越有密度，
0.30 m 处离核更近 ⇒ 有密度 ⇒ 生成接触）；
③ 那记 **3.0000 m/s** = `0.05 m / dt`（0.05 = 单 tick 去穿透上限）⇒ 一个**被上限截断**的去穿透冲量，
之后球已飞离接触带 ⇒ 无重力下匀速上升、**过冲到等值面上方 2.7 m**。

**定性**：这是**表示层**问题，不是一行口径问题——被截断的高斯场**无法表示"深处的内部"**：
超出截断半径后场里既没有密度也没有方向，`sdf` 只能给出一个"外侧"的近似值。
**处置建议（待你定，不建议在这种域里顺手改核心表示）**：
(a) **把语义写成契约**（与网格的薄壳语义同族）：喷溅域是**壳**——只保证等值面附近的接触，
深穿透不在契约内（探针 ④ 段即该契约的测试）；
(b) 若要让深穿透也能恢复，需要**另一条"是否被场包裹"的判据**（例如从点向外的射线/包围核计数），
而不是 `sdf` 的符号 —— 那是喷溅域的一次设计改动，应单独立项。

⚠️ **同一条截断也影响我的参考面**：探针里"等值面高度"的二分收敛点可能落在**截断悬崖**
（0.7071 → 负值的跳变）上，而不是平滑的 iso 穿越 ⇒ ② 段那些"盒贴住"的读数要按
**同一截断场自洽**来读（盒的接触也来自同一个场），不可直接当作"与真实几何一致"。此点已在探针注释里标注。

**现状**：**已修（2026-09-21，见下）；仅剩"球沉入 ≈0.4·r"这一条属场语义，另立。**

### P6 修复（2026-09-21）：`cut` 是**基础参数**，3σ 对多核场不够——改 4σ 并把三处耦合一起改

**根因的量化形式（先立判据）**：截断半径 = `σ·√cut`；**多核叠加**的场里等值面会浮到接近
截断半径处（平铺一层核时面在 ≈1.31·σ·√cut），于是**等值面上方的有效余量**
= `σ·(√cut − 1.31)`。实测平场（σ=0.5、cut=9）：面 1.3143、截断半径 1.5 ⇒ **余量只有 0.186 m**
< 球半径 0.25/0.35 ⇒ **体心落进"读不到任何核（σ=0、∇σ=0）"的死区** ⇒ `sdf()` 走退化分支
给出 `√iso = +0.7071`（正号 = 外侧）⇒ 不生成接触 ⇒ 球在场上**不可能稳定静置**
（落下去→被推开→再落，极限环；实测 |v| 0.2–0.5 永不静置）。盒正常是因为它的**角点**在面上、
落在有效区内——**这也是"盒对球错"的真正来源**（不是球那一路算错）。

**独立参考测出的收敛（探针 ⑤ 段；无截断参考由探针自己按同一批核参数求和）**：

| cut | √cut | 有截断等值面 | Δ(有−无截断) | 面上余量 | 200k 查询 |
|---|---|---|---|---|---|
| 4 | 2.00 | −0.9682 | **−2.3678** | +1.97 | 339 ms |
| 9（旧默认） | 3.00 | 1.3143 | **−0.0852**（面被压低 8.5 cm） | +0.186 | 567 ms |
| **16（新默认）** | 4.00 | 1.3963 | **−0.0032**（3 mm） | +0.604 | 1112 ms（1.96×） |
| 25 | 5.00 | 1.3995 | −0.0000 | +1.10 | 1881 ms |

无截断真值 = **1.3995**。⇒ 3σ 同时犯两个错：**面被压低 8.5 cm** + **面上余量不足**；
4σ 把面误差压到 3 mm、余量给到 0.60 m，代价 ~2×（该域文档本就定位"演示与中等规模"）。

**改动（`vxl-phys-splat`，四处一起动——这就是"高耦合"那面）**：
① `new()` 的 `cut: 9.0` → **16.0**；② `density_grad` 的逐核截断（判据本身）；
③ `rebuild_grid`：`bin` 与逐核登记范围 `max_s*3.0` → `max_s*cut.sqrt()`（否则网格会漏核）；
④ `world_bounds`：`×3.0` → `×cut.sqrt()`（否则界盒会截断场）。
模块头新增**余量规则 + 耦合面清单**；单元测试把"截断半径外的密度为 0"从写死的 `3σ = 1.5`
改成**按场自己的 `cut` 参数**断言（测契约而不是测常量——它正是在这次改动里红的）。

**修后读数（探针）**：盒 **−0.0011 / −0.0010 m**（贴住、入睡）；**球不再漂移、能静置入睡**
（极限环消失）。

### P6 续修（2026-09-21）：`sdf` 近场**牛顿投影**——球"沉 0.4·r"也修掉了

**先把误差规律量出来**（探针 ⑥ 段；真实距离由无截断参考沿竖直二分求）：

| 位置 | 面外 0.10 / 0.20 / 0.40 m | 面内 0.10 / 0.20 / 0.40 m |
|---|---|---|
| `f/真实` | **1.40 / 1.99 / 5.48**（偏大，远处达 310×） | **0.79 / 0.67 / 0.53**（偏小） |

⇒ 一阶估计 `f = (iso − σ)/|∇σ|` **只在面上精确**（f ≈ 0），离开面两个方向都偏——因为
`|∇σ|` 随远离核而变小。于是球停在 `f(心) = r`、而面外 f 偏大 ⇒ 心停得比 r 近 ⇒ **下半球过面**；
**盒没事是因为它的角点落在面上**（f ≈ 0）——同一件事解释了"盒对球错"。

**改动**：`sdf()` 在 `|f₀| ≤ NEAR_BAND(0.5 m)` 带内做**牛顿投影**（沿 `∇σ` 迭代走 `f`、
取**净位移**为距离、**符号取首步**），带外/深埋直接返回一阶值 ⇒ 成本可控（每迭代 = 一遍核求和）。
**迭代次数按实测选**（探针 ②⑤ 段）：

| 方案 | 球沉入（r=0.25 / 0.35） | 200k 近场查询（含 4σ 截断） |
|---|---|---|
| 不细化 | −0.0930 / −0.1670 | 1112 ms |
| 2 次迭代 | −0.0530 / −0.1165 | 3049 ms |
| **3 次迭代（采用）** | **−0.0213 / −0.0129** | 4130 ms |

⇒ 3 次把沉入压到**皮肤带量级（1–2 cm，与盒同档）**，只比 2 次贵 1.35×；盒读数**逐位不变**
（它走点查询、f 在面上本就精确）。⚠️ 球在起伏面上的 |v| 仍会短暂不为零（σ=0.5 核按 0.25 m 铺
出的等值面本身有起伏 ⇒ 球会滚）——这是**场几何**，不是接触口径。

**门禁**：fmt 0、clippy `-D warnings` 0、`cargo test --release` 0（50 个 test 二进制 / 0 FAILED，
含 `vxl-phys-splat` 7 例）、vocab 0、typos 0；行为门 `m0_gates` 压力哈希
`0x417be20a8e49c9b0436987415ac9961a` 一字不差、`determinism` `0x711be572cfe0e7eefb2cf51550fd4dd5`
一致（两条都不经喷溅域）。arena 不涉及喷溅域（`grep splat src/` 无命中）⇒ 无需重跑矩阵。



T3 SAT/缓存 ✅、T4 并行 5.08–5.69× + 逐位一致 ✅、T6 8A 行 p95 0.166ms ✅、
M0 门禁 ✅、确定性域（ADR 0004）✅。
