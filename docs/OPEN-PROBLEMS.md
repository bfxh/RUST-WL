# 未解问题台账（OPEN-PROBLEMS.md）

> M1 剩余的全部开口问题。每条：**现状 → 已排除（链 EXPERIMENTS.md）→ 候选入口 →
> 验收判据（三边并列，缺一不可）**。解决即移入 M1-PLAN 落地段（+adr 如构成决策）。
> **M2 启动条件：本表 P1/P2/P4 全部 ✅（P3 核口径后有结论）**
> ——纪律：任一任务不过即停在上一个可用状态。

## P1 — T1 求解稳定性：125 体复现的能量泵（最高优先级）

**现状**：125 体复现（5×5×5 留缝角点接触极限环）单 tick dE 峰值 148.9、
125/125 不睡；泵源已定位 = 「近邻回退匹配给出的 warm 冲量本身」（关闭它 dE −89%），
但塔依赖同一机制。落地修复（回退半径 1×skin，ADR 0003）后塔/堆族改善，
**泵未消**（dE 148.9 不变 ⇒ 有害匹配在 1cm 内）。

**已排除**（11 项 + 两条硬限定）：
1. 距离类判别式全部失效——有害匹配与塔需要的匹配**同在 1cm 内**（半径收紧分不开、
   零化又崩塔）；
2. 全局开关不可能——三场景对回退暖启动需求梯度 **tower > m0_gates ≫ col45/pile5**，
   任何全局形态都已实测两极（见 S10/S12/S13）。

**候选入口（非距离、自适应信号）**：
- ① **depth0/法向一致性门**：错配常见于「换了一对面的点」⇒ 候选烘焙 `depth0` 与
  当前裁剪深度差超限即拒配（按新接触重烘焙）。← **下一轮首选**（便宜、信号强、
  轴对齐盒堆里也有效，不依赖旋转场景）。
- ② 切向/法向局部帧一致性（候选 `t1/t2` 与当前点夹角）——注意纯轴对齐盒堆里
  信号弱，须在含旋转场景上先验。
- ③ 帧间冲量幅值突变门（候选暖冲量与近几帧均值差一个量级 ⇒ 拒）。

**验收（三边并列，另加一条硬约束）**：
- 125 体复现 **dE ≤20 且末态入睡**；
- col45/pile5 **保持全睡水平**（当前 35/45、1195/2000；S13 探针显示上限可到
  45/45、1825/2000——达到即超预期）；
- tower25 **塔身完整且不退化**（当前 |v| 0.283、max|Δpos| 0.0595）；
- **m0_gates 末态活跃必须保持 0（全睡）**——S13 正是败在这里（0→75），
  这是本项目最看重的健康不变量。
- 门禁：四哈希（m0_gates 预期按行为变化更新，须显式记录）+ 金样容差表。

## P2 — T2 宽相峰值：33.95 vs ≤15ms（树 6.86 vs ≤5ms）

**现状**：均值 12.94 达标；峰 = 查询 25.57（缓存翻转帧全量重查）+ 树 6.86
（每清醒体 18 层祖先链 refit，重建分支从未触发——阈值 68.9，tree_h 恒 18）。

**已排除**：树形两版（W3/W4）、失效策略（W1）、边距杠杆（W2/W7）、
fix_upwards 提前退出（W5，两次补丁都红——先最小复现钉不变量再动生产）。

**候选入口**：
1. `fix_upwards` 提前退出**重做**：先在最小复现（两节点树 + 三节点旋转）钉住
   「就地生长路径 × fix_upwards × balance」的真实不变量，再动生产代码；
   预期树峰 12.2→~5ms（当年量级校验 36 MFlop/帧 ≈ 10ms ✓）。
2. 候选过滤的访存局部性：生产 32ns/条 vs 隔离 4ns——候选表/体盒顺序访问布局
   （与 SoA 是同一笔投入，勿分两轮做）。
3. 叶盒紧致度（同 8 体叶盒越小候选越少）——仅在前两项后评估。

**验收**：8B 沉降期任一 tick 宽相 ≤15ms（树 ≤5ms）。

## P3 — 8B 最低档口径：单 tick ≈254ms 中解算占 85%

**现状（结构性，非待优化）**：解算 = 23ms 固定 + 3.88ms/迭代（16 迭代）；
岛级并行已 5.1–5.7× 吃满（10 万小岛）；SIMD 剩余路线上界 ≤10%（P5–P7）；
**≥30 FPS 在 M1 约束集内不可达**。

**候选入口**：与 V2 §11 原文核「8B 最低档」判定口径（整 tick FPS，还是相位级？
本文各任务验收行都是相位级）。若必须整 tick：唯一大杠杆 = 迭代次数 16→8
（省 ~31ms）——但塔稳定性研究说 16 是配方，**属物理取舍，须裁决**，不是工程优化。

## P4 — T3 窄相峰值：28.68 vs ≤25ms（最接近达标）

**现状**：均 11.05 ✓；SSE2 SAT + 体轴缓存已落地（逐位透明）；剩余 13% 触
「标量算术 + 访存」地板（减轴/改迭代序都属行为变化）。

**候选入口**：核口径（同 P3）；或 4 点裁剪专用路径（逐位透明要求下再挖）。

## T5 — 金样对照（受 T1 制约，无独立入口）

位置列 ✅（7.5/12.5/59.5mm 容差内）；速度 ⚠️（col45 0.065、pile5 0.088、
tower25 0.283）；入睡列 ❌（col45 35/45、pile5 1195/2000、tower25 0/2500）。
**随 P1 解锁**；P1 的验收三边已包含 T5 不回退约束。

## 已达标（只读，勿重开）

T3 SAT/缓存 ✅、T4 并行 5.08–5.69× + 逐位一致 ✅、T6 8A 行 p95 0.166ms ✅、
M0 门禁 ✅、确定性域（ADR 0004）✅。
