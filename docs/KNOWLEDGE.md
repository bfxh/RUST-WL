# 可迁移知识（KNOWLEDGE.md）

> 这里的东西换一个引擎/里程碑仍然成立：方法论（怎么设计实验、怎么避免读数陷阱）
> 与量化模型（成本结构式子），加文献锚点。具体路线的成败记录在 `EXPERIMENTS.md`。

## 1. 方法论（怎么干活才不白干）

### 1.1 能量审计（判定「真缺陷 vs 未收敛」的硬标尺）
`dE = ΔKE + ΔPE = 非重力功 = 接触功`。库仑摩擦必须耗散 ⇒ **摩擦通道的净 dE 期望 ≤0**；
采样净值为正（125 体复现：+19.7 J，正 52.7/负 33.0）即证明「摩擦模式泵能量」是
真缺陷，把定性判断升级为定量证据，直接排除「收敛不足」类解释（iters 32/64 只降
能量不消环的观测与之互证）。

### 1.2 两行判定法（分离「语义不成立」vs「实现有 bug」，成本差两个数量级）
判定「A 与 B 是否真的独立/等价」不必构建新机制——**颠倒一个既有循环**（两行）即成
判定实验。实例：怀疑「岛间独立 ⇒ 交错应透明」被否 ⇒ 把逐岛处理顺序整体颠倒 ⇒
四哈希逐位不变 ⇒ 独立在行为上成立 ⇒ 交错版的差异必是 lane 代码 bug（后自检证实：
步数 17/16）。先写 100 行 lane 机再排查，成本是 50 倍。

### 1.3 单旋钮消融 + 探针即回退
每轮只动一个旋钮，固定其余全部（同配方同窗口）；探针均为两行级改动，测完即回退，
负面结果进 EXPERIMENTS.md。「无实测净收益不引入」的纪律使约 60% 的尝试轮不产生
代码增量——负面结果同样是产出。

### 1.4 先量再改；上界先行
开工前先给候选路线一个**实测上界**（不是估计）：岛批量交错 = 等价但 −3% 反向；
同岛 4-lane = 上界 ≤10% ⇒ 都不必写 400 行。成本结构先测（iters 扫描三点共线），
靶子（3.88ms/迭代）来自拟合而非直觉。

### 1.5 归因修正要显式
两次「结果与预期相反」都产出了更深的结论：泵不在漂移目标而在 warm 冲量本身
（S11）；树峰不在重建而在 refit（W6）。预期落空时第一动作是把「预期 − 实测」
写成新的可判定假设，而不是换一个旋钮再试。

### 1.6 结构改动先在最小复现钉不变量
`fix_upwards` 提前退出：推理上完备（含「旋转感知」补丁）仍连红 4/6 结构测试，
根因未定位即回退。教训：树/不变式类代码，先在两节点/三节点最小复现上钉住
「就地生长 × fix_upwards × balance」的真实耦合，再动生产。

### 1.7 读数陷阱（防重走）
- **累计 vs 每 tick**：`PhaseTimings` 默认累计——逐相位分析必须每 tick
  `reset_timings()`；`m1_scale` 里 `broad` 列曾是累计而子列是每 tick，按累计差分
  会得出负的树耗时（两次踩坑：第十九段、第五段收口）。
- **管道吞退出码**：`cmd | tail && next` 会让红灯静默通过——输出重定向到文件 +
  显式 `$?`（ADR 0005）。
- **文档结论有保质期**：「skin 0.02 全睡」「横距 0.51 全睡」都是旧引擎版本的结论，
  引擎大改后必须重验（T1 消融里两条旧结论被实测推翻）。旧结论引用时带日期。

## 2. 量化模型（实测标定）

| 模型 | 式子 | 标定数据 |
|---|---|---|
| 解算成本 | `T_solve = 23.0ms + 3.88ms × iters` | iters 4/16/32 三点共线，残差 <1%；138ns/点·迭代（22.5 万点） |
| 查询成本（隔离） | `T_query ≈ 候选数 × 4ns` | 宽叶版候选 +172 万 ⇒ +6.94ms ✓ |
| 候选过滤（生产） | ≈ **32ns/条**（冷数组 4.8MB + 竞技场写读；缓存命中体不遍历） | BVH8 版反推 |
| 树 refit 成本 | 10 万清醒体 × 18 层 × ~20 flops ≈ 36 MFlop/帧 ≈ 10ms | 实测树峰 12.2ms 同量级 |
| 重建阈值 | `3·log2(n)+16`；n=20.24 万 ⇒ 68.9，tree_h 恒 18 ⇒ **从不触发** | 120 tick 全程 |
| 并行地板 | 窄相 3.2×、解算 4.7×、查询 3.7×（1→8 线程，访存受限正常水平） | §6 重测 |

推论链：查询峰的主导项是**候选量与过滤局部性**（非遍历层数——两版宽树证伪）；
解算的主导项是**访存延迟**（非算术——矩阵缓存 +11% 证伪）；固定成本占 27% 上限。

## 3. 暖启动与摩擦泵（T1 机制摘要，细节在 M1-PLAN §T1）

- **机制链**：错配锚点 ⇒ 帧间伪「漂移」（两点真实错位）⇒ `trhs = drift/dt`
  成为大速度指令 ⇒ 摩擦实现指令 ⇒ 注能。三独立证据：μ=0 全睡（锥容量 0 实现不了
  指令）、关锥投影 dE +30×（无钳制时指令被更彻底实现）、关回退匹配 dE −89%。
- **通道拆分**：有害成分切向显著（零化切向 → col45 45/45、pile5 1825 且塔完好；
  零化全部 → 塔崩）；但门槛场景依赖回退暖启动比谁都硬（m0_gates 0→75）。
- **需求梯度**：tower > m0_gates ≫ col45/pile5 ⇒ 全局开关不可行，判别式必须
  按接触状态自适应（候选入口见 OPEN-PROBLEMS P1）。

## 4. 确定性域与文献锚点

**本域定义**（ADR 0004）：同机同编译器 release 逐位（四哈希门禁）；跨平台不承诺
（M1 内）。与 Rapier 官方口径一致（locally deterministic；`enhanced-determinism`
跨机、与 SIMD 互斥、lane 宽度自成确定性域）——我们超出它的部分是**把逐位一致性
当作所有重构的预言机**（含并行），Rapier 仅作对拍金样。

| 文献 | 对应本仓结论 |
|---|---|
| Catto, *Iterative Dynamics*（GDC 2005） | 顺序冲量求解的基础形态 |
| Catto, *Sequential Impulses*（GDC 2006）/ Box2D-Lite | 暖启动 + 特征 ID 匹配（ADR 0003 的上游） |
| Catto, *Contact Manifolds*（GDC 2007） | 裁剪点漂移 ⇒ 特征匹配常落空 ⇒ 回退匹配的必要性（塔依赖） |
| Catto, *Soft Constraints*（GDC 2011） | erp/cfm 软接触形态——已与 Rapier `SpringCoefficients` 逐式核对 |
| Catto, *Understanding Constraints*（GDC 2014）/ *Numerical Methods*（GDC 2015） | 摩擦锥投影在压能量（S7）与 2×2 联立摩擦的方向依据 |
| Rapier docs, *Determinism* | 确定性域定义的金标准参照 |

**注意**：Rapier 源码里「separated points are solved rigidly」注释与其代码相反，
以代码为准（vendored 0.35.3 逐行核对时的发现）。
